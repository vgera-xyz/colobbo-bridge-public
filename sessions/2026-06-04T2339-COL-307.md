---
title: "COL-307 connected-records: T3b VERIFIED + T4 flat-grid model + COL-341 answer + PR #250 review-fixed + T5 WO summary-grid (PR #270 stacked); plus TD-005 hook fix"
tags: [bridge, session, col-307, col-341, t3b, t4, t5, connected-records, work-order-grid, work-package-grid, kernel, bidirectionaledge, partition-alias, claim-junction, flat-vs-recursive, sql-injection, pr-250, pr-270, stacked-pr, ai-review-triage, td-005, block-console-logs, api-node]
last_updated: 2026-06-04
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-307
status: active
---

# COL-307 connected-records — T3b verified, T4 flat-grid model, COL-341 answer, PR #250 review-fixed, T5 (WO grid) PR #270 stacked

## Summary

Long multi-phase session, almost entirely **api_node product work** directed from the agent-system repo, plus one agent-system hook fix. Verified + advanced the COL-307 connected-records feature end-to-end: turned **T3b (claim cross-DB fix) UNVERIFIED → VERIFIED** against live PostgreSQL; ran the **T4 grid EXPLAINs** and discovered the WP rollup is a **flat `workPackageId` fetch** (not recursive) — mooting last session's worktable/spool scaling worry; answered Jignesh's **COL-341** "drop hierarchy rows" question (safe — read side derives from `tblWorkOrder` columns + WO-level links); **triaged + fixed PR #250's** AI-ensemble + reviewer feedback (5 real fixes, pushed `59fc57c`, all checks green bar advisory lint); and **built T5** — the WorkOrder summary-grid endpoint — opened as **stacked PR #270** (base = the #250 branch), AI-triaged (1 security fix), comment left for Jignesh. All durable work lives in the api_node PRs; this repo carries this bridge + the TD-005 hook commit.

## TD-005 hook fix (agent-system; commit `7f4081ce92`, main)

Reverted a Raku-website-contaminated change to `hooks/global/block-console-logs.py` (a `scripts/` blanket-skip that contradicted TD-005 + `coding-standards.md`); kept only the legitimate **first-commit-baseline exemption** (skip the gate when no HEAD exists, so the baseline snapshot isn't flagged). Verified in isolated temp repos: TD-005 still blocks `console.*` in production `.js` (exit 2); first-commit + test-file paths allowed. Deployed via `deploy.sh`; byte-identical to `~/.claude/hooks/`. `coding-standards.md` + memory left untouched — TD-005 + the grandfather policy stand.

## COL-307 connected-records arc (api_node)

### T3b — claim cross-DB fix → VERIFIED (correct on real data)
Verified the MSSQL→PG claim-count fix against live dev PG (`vgera_ro`, read-only). Evidence: `tblclaim.workorderid` 0 nulls / 195; 0 multi-WO claims; per-WO **flat vs junction (`getClaimsByWorkOrderId`) divergence = EMPTY** → kernel count == canonical oracle for 100% of WOs. **Latent flag (parked):** kernel counts via the *primary FK* `claim.workOrderId`, while the codebase's canonical relation is the `tblclaim_workorder` junction — agree today (no multi-WO claims) but would undercount once multi-WO claims exist. **Junction-alignment is a known, deliberately-PARKED follow-up** (zero live impact; defer; not filed as a blocker). Also surfaced: 5 orphan junction rows (data-hygiene, harmless to counts).

### T4 — grid EXPLAIN → flat-grid model confirmed, deferral mooted
SHOWPLAN denied for `vgera_ro`; used `STATISTICS IO`/`TIME` + the live index inventory. **Decisive correction of last session:** the prior M2/M3 anchored recursion on the *denormalised* `workPackageId` set AND descended `parentWorkOrderId` → double-counted (the inflated "1457-WO/31k-worktable" figure was an artifact). Re-measured correctly: **flat WP rollup (`workPackageId` seek) = 3 ms, 0 worktable**; corrected recursive = 139 ms / 22.8k worktable — **same answer** (16 act/188 insp for WP 10). The shipped grid is **already flat** (`tblWorkOrder.workPackageId` column, no recursion), so the worktable/tempdb scaling risk was an artifact → **grid deferral mooted**. Indexes the read paths need already exist in the DB (`IX_tblWorkOrder_Company_WorkPackage`, `NCINDEX_WorkOrder_01`, `IX_tblWorkOrder_ParentWorkOrderId`, `IX_tblLinkModule_Source/Target`) — but **not declared in the Drizzle schema** (drift to reconcile). WO-anchor recursion (details page) is bounded + ship-able (28 ms). Data-integrity note: `workPackageId` denormalisation is present but **inconsistent** with the `parentWorkOrderId` hierarchy (flat vs recursive WO-sets diverge ±10–85%) — but the divergent WOs are link-less, so the rollup *answer* matches; operator confirmed prod model is sound (dev-data noise).

### COL-341 answer for Jignesh
The COL-307 read side resolves WP→WO via `tblWorkOrder.workPackageId` and reads `tblLinkModule` only at WO/Inspection level (`LINK_GRAPH`); the recursive `parentWorkOrderId` CTE is panel-only, not the grid. Neither the grid nor `create-inspections` (web-angular) does a child-WO rollup via WP-level rows. **Verdict: simplifying COL-341 to primary-row-only is SAFE** — Account + parent-WO + WorkPackage hierarchy rows have no go-forward reader (read-time recursion / the column re-derive them). COL-341's expansion rows are write-amplification with no live reader.

### PR #250 (backend) — review triage + fixes (commit `59fc57c`)
Triaged the AI-ensemble + Jignesh's blocking review. **5 real fixes:** removed the dead MSSQL `tblClaim` schema definition (claims are PG; no importer); reconciled `tblAction`/`tblInspection` against `INFORMATION_SCHEMA` (id/companyId/status exact-match → byState correct; refuted "silent zeros"); **DB-confirmed `ACTION_STATUS` A/I/C** against live `tblAction` (A=261/I=46/C=27); corrected stale MSSQL claim test seeds → PG; dropped an unnecessary `claimRows` cast. **Dismissed with reasons:** the `getUserDetails` "unguarded crash" (caller wraps it → 500, verified false positive) + minimal req-type/test-seed style. tsc 0 / jest 40/40. All checks green except the **new advisory `lint-changed`** (CS-397 Tier-1 gate, just merged) which fails on **pre-existing repo-wide eslint debt across the PR's merge-base diff** — not introduced here, advisory (no required checks on `development`), not blocking. Author comment posted.

### T5 — WorkOrder summary-grid endpoint (PR #270, STACKED on #250)
`GET /api/work-orders/summary-grid` — faithful mirror of the WP grid to the WorkOrder anchor. **Branching: verified the kernel is ONLY on the #250 branch (absent on `development`)** → operator chose to branch off the #250 tip (`59fc57c`) and open a **stacked PR (base = `col-307-work-order-context-enrichment-backend`)** so the diff is only T5's 7 files; #250 merges first, then T5 rebases onto development. **Reuse:** new `aggregateGroupedByWorkOrder` (partition by `woId`), `workOrderGridService` (candidate seam = `runWorkOrderView` + shared `mergeScopeIntoFilters`), controller + routes; `bidirectionalEdge` gained an **optional `partitionAlias`** (default `'workPackageId'` → WP grid compiled-SQL byte-identical; WO grid passes `'workOrderId'`); `aggregateGroupedByWorkPackage` body untouched. Claim omitted (mirror; deferral note baked in: both grids add claims post-PG-migration). **AI-review triage on #270:** 1 fix — Claude caught `partitionAlias`→`sql.raw` as an injection surface (free `string`); constrained to `'workPackageId' | 'workOrderId'` literal union (`f123dc2`), full suite re-run to re-prove the WP path. 7 dismissed (all faithful mirrors of the shipped WP grid, or a misread). tsc 0 / jest 51/51. All #270 checks green (no `lint-changed` on a stacked base). Author comment posted.

## Commits

- **colobbo-agent-system:** `7f4081ce92` fix(hooks): skip TD-005 console gate on repo's first commit; this bridge + active-plan + daily-log + canonical_pages (handoff).
- **api_node (product, this session):** `59fc57c` (PR #250 review fixes) · `6345ae7` (T5 build) · `f123dc2` (T5 partitionAlias injection fix). T3b/T4/COL-341 work was read-and-report (no commits).

## What's next

- **#250 → Jignesh's review/merge** (Dev Done; advisory lint red is pre-existing debt). **#270 (T5) is stacked — review/merge #250 first**, then rebase #270 onto development + retarget.
- **PARKED:** claim junction-alignment (kernel primary-FK → `tblclaim_workorder`); zero live impact until multi-WO claims exist.
- **Follow-ups (unfiled):** reconcile the Drizzle schema to declare the existing MSSQL indexes (T4 finding); `tblWorkOrder.workPackageId` denormalisation-vs-hierarchy integrity (operator-confirmed prod-sound; dev-noise); api_node holistic `no-explicit-any` eslint debt (the lint-changed source); orphan `tblclaim_workorder` rows cleanup.
- The two follow-up ticket DRAFTS (junction-alignment + orphan cleanup) staged earlier remain **drafts, not posted** — operator posts.

## Verification notes

- T3b: live PG counts (read-only, COUNT/`\d` only). T4: live MSSQL `STATISTICS IO`/`TIME` + `sp_helpindex` (SHOWPLAN denied). PR #250: tsc 0 / jest 40/40. T5: tsc 0 / jest 51/51 (WP + C1 re-proven after the shared `bidirectionalEdge` edit, twice — partitionAlias add + the union-type fix).

## See also

- [[2026-06-04T0523-COL-307|prior bridge — T3 grid + T3b claim fix (this session's base)]]
- [[../../strategy/active-plan|active-plan]] — connected-records track updated this handoff
- [api_node PR #250](https://github.com/Colobbo/api_node/pull/250) — COL-307 backend (review-fixed, Dev Done)
- [api_node PR #270](https://github.com/Colobbo/api_node/pull/270) — T5 WO summary-grid (stacked on #250)
- [COL-307](https://linear.app/colobbo/issue/COL-307) · [COL-341](https://linear.app/colobbo/issue/COL-341) · [COL-206](https://linear.app/colobbo/issue/COL-206) (claims→PG)
