---
title: "CS-410 riders: Sentry vars-vs-secret split fleet-wide + dormant web/mobile ceremonies + 9 org variables provisioned"
tags: [bridge, session, cs-410, sentry, actions-variables, ceremony-workflows, dormant, appflow, rider]
last_updated: 2026-07-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-410
amended:
  - timestamp: "2026-07-19T08:19:00Z"
    reason: "Appflow premise retracted — operator TIER-1: not in use; build path pending Jaydeep; CS-307 Fastlane end-state"
status: active
---

## Summary

Two post-handoff riders on CS-410, same UTC day as the main adoption session ([[2026-07-19T0638-CS-410|bridge]]).

**Rider 1 — vars-vs-secret split.** Operator declared Sentry identifiers live as Actions *variables* (token stays a secret). Verification found the operator's manual variable save **never landed** (org `total_count: 0`, all repo + env scopes empty) — only `SENTRY_AUTH_TOKEN` had arrived as an org secret. Applied the reference switch anyway (correct end-state): api_node PR **#383** (new — `sentry-release.yml`'s 5 identifier refs `secrets.*`→`vars.*`; the only file in any repo with real refs, per all-branch grep), #382 amended (cd-dev hardcoded org → `vars.SENTRY_ORG` + wider skip-guard), #633 amended (header docs). First grep "proof" emitted **false zeros** (`| bc` choked on `ref:file:count`, `|| true`-class swallow) — caught because integration/main *should* have shown 5; redone with plain line counts. The bash-jq-reflexes false-PASS class, live.

**Rider 2 — scheme + provisioning + dormant ceremonies.** The generic `SENTRY_PROJECT` name was api_node-specific in disguise → adopted `SENTRY_PROJECT_<SURFACE>_<ENV>` fleet-wide; **created all 9 org variables myself via `gh api POST` and verified by readback** (`total_count: 9`, visibility all; values match live Sentry slugs re-fetched via MCP). Updated #383 (`SENTRY_PROJECT`→`SENTRY_PROJECT_API_PROD`; all 7 gates green) and #382 (dev slugs → `vars.SENTRY_PROJECT_API_DEV/_NOTIFICATION_DEV`, guard covers all identifiers). **web-angular #633**: `build-prod.yml` rebuilt as the full parameterised ceremony (release create + set-commits → `ng build` direct → debug-ID upload → finalize + deploy record; production/development input flips project var, deploy env, AND which generated env file is stamped) — **dormant**, dispatch-only, auto tag-trigger committed commented-out behind 3 inline enable-conditions; also fixed the pre-existing **double sourcemap upload** and moved npm-script `--org/--project` flags to env (local fallback via `SENTRY_PROPERTIES=sentry.properties`). **Colobbo-Mobile PR #381** (new): release **bookkeeping-only** workflow — evidence-based refusal to build sourcemap-upload theatre (Appflow builds the shipped bundle with no debug-ID injection; a GHA rebuild can't be trusted to match) — creates/finalizes the exact native auto-derived release names (`appId@version+build` per platform, env input flips project + appId). Appflow-side upload path documented in the runbook as the enable route. Runbook updated with the full variable table + per-repo ceremony state (`d17f96fc09`).

## Commits / PRs

- api_node: PR **#383** `fix/sentry-vars-split` (`ede5610`→`373a9cb`, 7/7 gates green); #382 `feature/cd-dev-ssm` +`1a79d4c`,`52c15ac`.
- web-angular: #633 `fix/sentry-build-prod-stamp` +`9f2d0db8`,`c04f3101`.
- Colobbo-Mobile: PR **#381** `feature/sentry-release-ceremony` (new).
- agent-system: `0f3953d4af` + `d17f96fc09` (runbook riders 1+2).

## Verification notes

- Variables: `gh api /orgs/Colobbo/actions/variables` readback = 9, values = live Sentry slugs (MCP re-verified pre-creation). **Trust only readback** — the operator's earlier save had silently not landed.
- actionlint (1.7.12, installed via brew) clean on every touched workflow.
- Grep proof (plain `wc -l`, no `bc`): zero old-name/secrets-identifier refs in ANY file a CS-410 branch touches; the two remaining hit-sets are inherited base copies of `sentry-release.yml` on branches whose diffs are **proven disjoint** from it (`git diff --name-only` vs merge-base) — erased at #383's merge, unresurrectable by the other merges. Token refs intact everywhere (`secrets.SENTRY_AUTH_TOKEN`).

## Enable-conditions (dormant workflows)

- **web ceremony (#633)**: (1) `environment.prod.ts` provisioning answered — @pjtech18, on the PR; (2) one supervised dispatch run; (3) first release PR lands (main carries gates). Then uncomment the tag trigger.
- **mobile bookkeeping (#381)**: (1) mobile release line starts (≥ v8.3.0 tag + ruleset applied to repo); (2) Appflow-side sourcemap upload wired (operator + @pjtech18); (3) one supervised dispatch at a store submission. No auto-trigger committed — store submissions are inherently manual.
- **api_node CD ceremony (#382)**: unchanged 5 blockers (SSM — Siddhesh; OIDC role, repo vars — operator; IIS drain — Jignesh/Siddhesh) — Sentry vars are now provisioned, so that blocker is CLEARED.

## What's next

- Reviews: #383 + #382 (@jthakkar1981); #633 + mobile #380/#381 (@pjtech18).
- Migration critical path (COL-205/COL-306, due 2026-07-31) — still THE focus, untouched by riders.

## Addendum 2026-07-19T0819 — Appflow premise retracted — operator TIER-1: not in use; build path pending Jaydeep; CS-307 Fastlane end-state

Operator correction (TIER-1, overrides this entry's filesystem inference): **Appflow is NOT in use** — `appflow.config.json` was dead code from an abandoned attempt, as were the Capgo docs remnants. This entry's claims that "Appflow builds the shipped bundle" and that sourcemap upload is "Appflow-side" are therefore **retracted**. Corrected state: store-bundle build path is being confirmed with Jaydeep; sourcemap upload belongs with the real build (interim) and with the **CS-307 Fastlane pipeline** (decided end-state, blocked on Nx monorepo); CS-308 = backward-compat + flags; CS-309 = OTA evaluation, deferred. The #381 workflow's bookkeeping-only design **survives unchanged** — its logic never depended on WHERE the real build runs, only on the fact that it isn't GHA. Cleanup shipped: Colobbo-Mobile PR #382 (removes `appflow.config.json` + stale Capgo docs; `@capawesome/*` plugins kept as live false-positives), #381 header comments rewritten (comments-only, diff-verified), runbook mobile section corrected.

## See also

- [[2026-07-19T0638-CS-410|main CS-410 session bridge]]
- [[../../architecture/release-process|release-process runbook]]
