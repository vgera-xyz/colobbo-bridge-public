---
title: "CS-373 M8 batch — remaining 7 consumers migrated to tolerant-reader; M8 COMPLETE (9 of 9); tags v2.51.0 + v2.52.0"
tags: [bridge, session, cs-373, cs-359, cs-362, cs-406, m8, tolerant-reader, consumed-fields, batch-migration, hybrid-gate, v2.51.0, v2.52.0]
last_updated: 2026-07-09
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-373
status: active
---

# CS-373 M8 batch — 7 consumers migrated; M8 COMPLETE (9 of 9 on tolerant-reader)

## Summary

Executed the full M8 batch in one session (planned as 3): migrated the remaining **7 consumer Lobes** (Architect → Backend → Database → Test Writer → Doc Writer → Security → Code Reviewer, operator-fixed order) from `consumes_<X>_schema` semver version-gates to `consumed_fields` tolerant-reader form, using the Frontend/Mobile pattern verbatim. **M8 is COMPLETE — all 9 consumers migrated; 20 of 21 Gate-2 registry entries validate via invariant (δ)**; the only semver entry left is `security-arch` (operator-decided deferral → [CS-406](https://linear.app/colobbo/issue/CS-406)); the two ADR-037 §D7 migration sentinels are value-preserved in EXCLUSIONS. Shipped as 7 per-Lobe commits + 2 tags — **v2.51.0** (mechanical five) and **v2.52.0** (Security + Code Reviewer hybrid) — both tag runs GREEN on all 21 gates (runs 28988966167 + 28989561860). **CS-359 Fix-1 is now fleet-complete; CS-362 is moot** (transitions surfaced to operator via Linear comments, not auto-fired, per TIER-1-verify discipline).

## What was done

1. **Plan** (approved via /rplan + plan-mode): 3 Explore agents grounded the pattern/consumers/infra; operator locked 3 decisions — security-arch pin DEFERRED (fixture packs reference no ArchResult domain field; δ forbids empty), CR consumed_fields BROAD (all non-envelope domain fields; borderline advisories/capability/feedback_iteration_context excluded — CR reads none), 2-tag shape (tag boundary at the risk boundary; CC final call, operator-delegated). Plan file: `~/.claude/plans/cs-373-m8-batch-migrate-unified-creek.md`.
2. **T0 baseline**: full suite green pre-batch (5 scripts + 11 harnesses); deleted stray `doc-writer-lobe/test-fixtures/cases 2.json` (untracked iCloud-collision duplicate, byte-identical, not globbed by run.sh).
3. **Mechanical five** (one atomic commit each): architect `dee15eb0c5` (0.2.0; plan: affected_repos, file_mapping, task_breakdown, dependencies, feature_summary; NEW fixture), backend `7c8213cb58` (0.4.0; arch: components, interface_contracts; fixture flipped), database `6e5b287bdb` (0.2.0; arch: components, **interface_contracts** — corrected the exploration table's `file_mapping` claim by live grep; backend: components; both fixtures flipped, backend one proves consumption via `backend_component_id`), test-writer `c4efb8229f` (0.4.0; review: workspace_ref, lobes_locked_final; arch: components, file_mapping), doc-writer `f654acd44f` (0.2.0; review: workspace_ref, lobes_locked_final; arch: components — `ArchResult.architecture_deltas[]` NOT declared, field doesn't exist in the Architect schema; documented in §Compatibility).
4. **Tag v2.51.0** (`65ed6284af`): full pre-tag checklist green; ratified the five + the 58-commit backlog; CI 21/21 in 1m27s.
5. **Security hybrid** `8a6532db04` (0.2.0): backend+database pins → consumed_fields ([components] each — live fixture-pack derivation: 36+7 JSONPaths all rooted at `$.components`; arch's only reference is the `applies_to_mapping_only` no-op marker → arch=∅ confirmed). Two-regime Step-1 gate (arch + migration semver LIVE; backend + database tolerant-reader). **[CS-406](https://linear.app/colobbo/issue/CS-406) filed** (ground `security_checklist` or drop the relationship). Migration sentinel value byte-identical (gained a trailing comma from key reordering only).
6. **Code Reviewer hybrid** `88b4dab425` (0.4.0, LAST by design): 7 pins → BROAD consumed_fields (operator-approved lists, computed live from producer schemas minus envelope/meta). **Semantic rework**: `<upstream_role>` derivation + Step-1.2 workspace-resolution iteration now derive from consumed_fields keys ∪ migration sentinel — derived key SET provably unchanged (same 8 roles), CS-146 caller contract preserved. New `tolerant-reader-future-all-upstreams` fixture; all 6 CR fixtures green.
7. **Tag v2.52.0** (`23801bf420`): second full pre-tag checklist green; CI 21/21 GREEN.
8. **Fixture coverage decision executed (FULL)**: every migrated Lobe carries a `tolerant-reader-future-*` runtime fixture — flipped where an incompat fixture existed (backend, database), clone-and-bump where none (architect, test, docs, security, CR). Dead `incompatible_*_schema` enums retained everywhere; ZERO output-schema bumps → zero downstream cascade.

## Corrections / verify-catches (the recurring M8 lesson held)

- **Database consumed_fields**: exploration table said arch: [components, file_mapping] — live grep showed NO `ArchResult.file_mapping` read; it passes `interface_contracts` through verbatim (Step 4). Declared [components, interface_contracts].
- **Doc-writer `architecture_deltas`**: LOBE.md references `ArchResult.architecture_deltas[]` — a field that does NOT exist in the Architect output schema (hedged prose, can never fire). Not declared; documented in §Compatibility for when Architect ships it.
- **Security arch=∅**: pre-computed derivation re-confirmed live before the glaze edit.
- **CR had TWO pin-name derivation sites**, not one (upstream_role doc + Step-1.2 workspace iteration) — both reworked.
- **Concurrent-push race ×2** (graphify cron commits landed on origin during both tag pushes): resolved by **merge, never rebase** (rebase would orphan the tagged SHA); both tags confirmed ancestors of main.

## Verification

Per-Lobe before every commit: token-grep clean → Gate 2 (δ) → own harness → Gates 14/1/19/13 (+9 for backend/database) → sentinel diff gate (security/CR). Full pre-tag checklist ×2 (5 scripts + 11 harnesses + resnap idempotency). Both tag runs 21/21 GREEN. Gate 2 final state: 20 δ-form + 1 semver (security-arch) + 2 EXCLUSIONS (migration sentinels).

## What's next

- **[CS-406](https://linear.app/colobbo/issue/CS-406)** (Backlog/Medium): security-arch — ground a real read (`security_checklist` candidate) or drop the relationship (registry edit).
- **CS-359**: Fix-1 COMPLETE fleet-wide — operator decides ticket state (Fix-2 orchestrator-routing remains Phase-2+ scope in the same ticket). Comment posted.
- **CS-362**: structurally moot (tolerant-reader subsumes compat-pin verification) — operator decides close. Comment posted.
- **Migration critical path is now unblocked as THE next focus**: COL-205 batch-1 via COL-306 (PR #261 awaiting Jignesh; ~18 more table rows pending).
- CS-373 stays Done (umbrella); M8 was its final Phase-1 milestone — **Phase 1 of the derived architecture sequence is complete**; Phase 2 (Performance Lobe, Design Lobe, CS-324 dispatch gate, CS-322) is next on the architecture track.
