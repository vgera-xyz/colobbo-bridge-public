---
title: "Post-0925 closing arc — Graphify CI-token fix + Colobbo-Web regen + CS-350"
tags: [bridge, session, graphify, ci-token, fine-grained-pat, colobbo-web, dotnet-migration, ipmmasterapi, cs-350, cs-338, cs-133, issue-26, claude-code-60128, col-79, second-handoff]
last_updated: 2026-05-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-350
status: active
---

# Post-0925 closing arc — Graphify CI-token fix + Colobbo-Web regen

## Summary

**Second handoff for 2026-05-19.** The 0925 handoff
(`wiki/bridge/sessions/2026-05-19T0925-COL-79.md`, commit `873b06953`)
closed the COL-79-smoke → substrate version-self-report arc (Pieces
A/B/C). This entry captures the post-0925 closing arc: a Graphify
Colobbo-Web regen that surfaced a CI-token permission gap, the
operator-side fix, the successful regen, and the tracked follow-on.
**COL-79 the feature is still parked** (CS-346) — unchanged by this arc.

## What shipped (post-0925)

1. **403 root-caused.** A Colobbo-Web Graphify regen via
   `graphify-refresh.yml` failed `403 — Write access to repository not
   granted` on `Colobbo/Colobbo-Web`. Investigation: a single secret
   `PRODUCT_REPO_READ_TOKEN` (fine-grained PAT) serves all four product
   repos via one parameterised checkout step; it omitted Colobbo-Web from
   its repo allowlist (works for api_node/web-angular/Colobbo-Mobile).
   This is the structural reason the prior Colobbo-Web graph was a local
   `/private/tmp/cs158-source` one-off (CS-158), not a choice. The
   workflow auto-filed agent-system issue **#26**.

2. **Operator-side fix (settings-only, no code).** Operator widened the
   fine-grained PAT to include `Colobbo/Colobbo-Web` (Contents: Read).
   No workflow edit, no secret rotation, no PR — the cleanest
   least-privilege fix for a codebase decommissioning ~July 31. (Option C
   chosen over Option A local-regen, which was explicitly abandoned.)

3. **Re-dispatch succeeded.** `graphify-refresh.yml -f repo=Colobbo-Web`
   run **26090044399** green end-to-end (checkout, AST refresh, CS-338
   fail-fast gate, commit). Autocommit **`a0ad27e81`**
   (`chore(graphify): refresh Colobbo-Web from HEAD [CS-133]`), graphing
   `Colobbo/Colobbo-Web@dev` HEAD `27aaafd01`.

4. **Verified.** CI provenance restored
   (`/home/runner/.../target-repo/IPMMasterAPI`, 2026-05-19, replacing
   the May-7 `/tmp` one-off); `wiki/` dir now present (was absent);
   `check-graphify-crosslinks.sh graphs/Colobbo-Web` → **PASS**
   (`raw_xcomm=712, community_files=449, populated=289, empty=160`; the
   160 empties are communities with genuinely no cross-edges, not the
   #925 bug — the gate distinguishes). The Architect/Planner Lobe now has
   current .NET IPMMasterAPI structure + cross-community relationships for
   the remaining ~10 weeks of migration.

5. **Issue #26 closed** (`completed`) with a one-line comment referencing
   run 26090044399 + commit `a0ad27e81` + the gate PASS + CS-350.

6. **CS-350 filed** (Colobbo Strategic, Backlog, Low) — "Remove CS-338
   graphify community-annotation workaround now that graphifyy 0.8.13
   fixes #925 upstream." Workaround lives in `graphify-refresh.yml:179`
   and `graphify-refresh-self.yml:112`; redundant-but-harmless post-0.8.13
   (CS-338's own "remove when upstream fixes" condition is now met).
   Good substrate-consistency Friday-batch candidate.

7. **graphifyy 0.8.13 confirmed** as PyPI-latest + locally installed; CI
   auto-carries it via unpinned `pip install graphifyy` (no pin edit
   needed).

8. **GitHub keep-open comment** posted on `anthropics/claude-code#60128`
   (community ack from kcarriedo + AI-generated technical analysis from
   Ilya0527/ALEF Pattern Catalog supports the engineering case; the
   bot's May-21 auto-close pressure documented in writing).

## Commits (post-0925, all CI autocommits — no direct commits this arc)

- `a0ad27e81` — `chore(graphify): refresh Colobbo-Web from HEAD [CS-133]`
  (the regen artifact; CI bot)
- `3700cd580` — `chore(graphify): refresh cibo from 873b069 [CS-240]`
  (cibo self-refresh triggered by the 0925 handoff push; CI bot)
- (this entry) — the only direct commit of this handoff

The Option-C work was investigation (read-only) + an operator GitHub
settings change + CI dispatch + `gh`/Linear API actions — by design no
source edits.

## What's next / follow-ons

- **CS-350** — remove the now-redundant CS-338 workaround on the next
  graphify-workflow edit; keep `check-graphify-crosslinks.sh` (it becomes
  an upstream-regression guard). Friday-batch substrate-consistency
  candidate.
- **PAT expiry watch** — confirm `PRODUCT_REPO_READ_TOKEN` expiry ≥
  ~2026-08-15; if it lapses it breaks all four product-repo refreshes,
  not just Colobbo-Web.
- **Colobbo-Web NOT pipelined** — deliberately excluded from cron +
  drift-monitor (July-31 .NET decommission). Re-dispatch on demand during
  active migration; do not add to the weekly matrix.
- **`anthropics/claude-code#60128`** — bot auto-close pressure dated
  May-21; revisit if it closes despite the keep-open rationale.
- **COL-79** — still parked pending CS-346 brownfield codegen. Untouched.

## See also

- `wiki/bridge/sessions/2026-05-19T0925-COL-79.md` — the 0925
  substrate-arc handoff this continues (Pieces A/B/C)
- `wiki/bridge/sessions/2026-05-19T0550-COL-79.md` — the originating
  COL-79 smoke
- CS-350 (Linear, Colobbo Strategic) — the tracked workaround-removal
  follow-on
