---
title: "CS-410 release-process adoption shipped end-to-end + CS-324 live-fire discharged + v2.59.0"
tags: [bridge, session, cs-410, cs-324, release-process, sentry, tag-rulesets, regression-tests, cd-to-dev, mobile-versioning, v2.59.0]
last_updated: 2026-07-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-410
status: active
---

## Summary

Two-part session. **Part 1 — CS-324 live-fire verify (discharged):** first fresh session since the mid-session deploy armed `validate-lobe-dispatch.py`; a deterministic 5-case pair against the deployed binary plus a real bad `planner` dispatch through the live Agent tool path proved fail-closed (harness blocked with the CS-324 stderr message before the agent ran). The residual caveat from 2026-07-10 is discharged; CS-324 stays Done.

**Part 2 — CS-410 release-process adoption (the session's bulk):** operator-prompted Phase-0-verify-then-build across all product repos, plan-mode with 5 recon subagents, three operator scope-additions added via plan rejection (regression-test convention, tag-format guard, mobile version unification — each "investigate first, fit current reality"). All phases shipped and verified:

- **Runbook** `wiki/architecture/release-process.md` (`ce96dddf9a`): main = prod, MINOR = release, PATCH = hotfix-only, hotfix from main + mandatory back-merge, per-repo real gate lists, regression-test convention, tag format + rulesets, per-env Sentry ceremony (org `colobbo`, paired `*-dev`/`*-prod` projects), mobile 5-point version sync (line starts ≥ v8.3.0), Colobbo-Web frozen line.
- **PRs (4):** api_node [#381](https://github.com/Colobbo/api_node/pull/381) Sentry release-from-env (`SENTRY_RELEASE`/`SENTRY_ENVIRONMENT` precedence, never-stamp-garbage; 33 tests incl. stripPii + init wiring; **all 7 required gates green**) · api_node [#382](https://github.com/Colobbo/api_node/pull/382) CD-to-dev via SSM `AWS-RunPowerShellScript` (STAGED dispatch-only; 5 enable-blockers named w/ owners) · web-angular [#633](https://github.com/Colobbo/web-angular/pull/633) fail-loud build-prod stamping (gitignored env-file preflight, key-based release+environment stamp w/ post-stamp verify, missing `sentry:cleanup` added; all gates green) · Colobbo-Mobile [#380](https://github.com/Colobbo/Colobbo-Mobile/pull/380) release field + env override + **committed `.sentryclirc` token removed** + Dockerfile SENTRY ARGs.
- **Tag rulesets** `semver-tags-only` created on api_node (#19164034) + web-angular (#19164035) — malformed-tag push **rejected live** ("push declined due to repository rule violations"), remote clean.
- **web-angular main branch protection** aligned 3→5 required checks (added `build-strict-templates`, `coverage-changed`).
- **v2.59.0** tagged (`a5adc7e4d8`) — full tag-run CI **SUCCESS** (run 29676109580). Release also fixed two gate-surfaced items: runbook registered in `canonical_pages` (invariant k) and glaze `test.test_frameworks.api_node.primary` jest→vitest drift (invariant ac; fintech glaze already modelled vitest; compose-fixtures byte-identical after — path-discipline held).

## Phase-0 findings of record (verified, not assumed)

- **`SENTRY_AUTH_TOKEN` + all `SENTRY_*` secrets ABSENT** from GitHub org/repo/env scopes → api_node's `sentry-release.yml` non-functional today. Operator TODO: provision as org secrets (token scopes `project:releases` + `org:read`).
- **🔴 Committed Sentry auth token** in Colobbo-Mobile `.sentryclirc` (git history retains it) — removed in #380; **rotation in Sentry dashboard is mandatory regardless** (operator).
- **api_node `f3006ec`** (PR #309) on `main` + 4 release/hotfix branches, **NOT in `development`** — live back-merge gap; recommended one-off back-merge PR (operator decision).
- **Gates-on-main already done** in api_node (#358 + newer gates) and web-angular (post-#604 change); mobile has zero gates ("pending CI session" in runbook). Both repos: several required-check workflow files absent from main's own tree → hotfix-off-main stalls until the first release PR lands them (documented in runbook).
- **SSM verification IAM-blocked** (`cs-agent-cognito` lacks `ssm:DescribeInstanceInformation`); Windows-EC2/pm2/IIS/Memurai topology **undocumented in wiki** — pm2 names in cd-dev.yml marked operator-asserted/UNVERIFIED; IIS-drain open question named in the PR.
- **Sentry projects are per-environment pairs** (`node-api-dev/prod`, `notification-api-dev/prod`, `web-dev/prod`, `mobile-dev/prod`, `dotnet-api-dev/prod`); no pdf-lambda project (and no SDK — untouched by design).
- **No `vX.Y.Z` tag existed in any product repo** (clean slate for the guard); no tag-triggered workflows; org on Team plan → rulesets available.
- **Mobile version drift:** package.json 0.0.1 / constant.ts 1.4.8 / iOS 2.2.5+1 / Android 8.2+52 — the runbook's sync rule unifies at ≥ v8.3.0 next mobile release.

## Commits

- **colobbo-agent-system:** `ce96dddf9a` (runbook + index links), `21eb1ac8ad` (push-race merge), `a5adc7e4d8` (release v2.59.0: VERSION, CHANGELOG, registry restamp, canonical_pages + glaze drift fix), tag `v2.59.0`.
- **api_node `fix/sentry-release-from-env`:** `1a22474` (release-from-env + tests), `f76c0ae` (regex lint debt), `826a5b4` (stripPii + init-wiring tests for mutation gate), `f9a39e2` (strict-safe indexing).
- **api_node `feature/cd-dev-ssm`:** `9c53192` (cd-dev.yml).
- **web-angular `fix/sentry-build-prod-stamp`:** `d26287bb`.
- **Colobbo-Mobile `fix/sentry-release-field`:** `5c26509c`, `b2283014` (token-file removal).

## Verification notes

- CS-324: 5/5 deterministic cases vs deployed binary (byte-identical to source) + live harness block on real dispatch; good-path exit-0 proven at binary level.
- api_node #381 went through a 3-round CI whack-a-mole, each fix expanding the changed-lines set into the next gate: `lint-changed` (pre-existing `no-useless-escape` in PII regexes; behavior-equivalence proven on match samples) → `mutation-changed` (34.62% < 60; fixed with real stripPii/init tests — local Stryker 78.57%/78.10%) → `typecheck-strict-changed` (TS2532 on `mock.calls[0][0]`; fixed both apps, verified with CI's own `tsc-errors-on-changed-lines.mjs`). Final state: all 7 required gates green.
- Tag ruleset: negative live-fire only (malformed tag rejected; nothing landed). Positive path exercised by the next real release tag.
- Mobile typecheck: `src/main.ts` clean; 8 pre-existing `Cannot find name 'async'` errors in old scaffold specs exist identically at base (flagged in PR, untouched).
- v2.59.0: 4 pre-tag scripts green locally; full tag-run CI green.

## Failure log (honest)

- **Stash-pop unstaged the `.sentryclirc` deletion** → first mobile commit shipped without it; caught by `--stat` review; force-push hook-blocked (correctly) → clean second commit instead.
- **CI whack-a-mole ×3 on #381** (above) — root lesson: when a changed-lines gate suite exists, run ALL of its local equivalents (lint, stryker, strict-changed-lines script) before the first push, not just tsc+vitest.
- **`.env.example` PostToolUse hook block** — edit persisted (placeholder-only, no secrets); no bypass needed.
- **`tail -1` on verify scripts** hid a real invariant-(k)/(ac) failure behind decorative output — re-ran with explicit exit codes; reflex: never judge a gate by its last line.
- **zsh `set -- $SPEC` loop quirk** silently emptied a gh-checks loop — replaced with explicit per-repo commands.

## What's next

- **Reviews:** @jthakkar1981 → api_node #381 + #382; @pjtech18 → web-angular #633 (incl. the "where does environment.prod.ts get generated?" open question) + Colobbo-Mobile #380.
- **Operator TODOs:** provision SENTRY org secrets; **rotate the leaked mobile token**; decide api_node main→development back-merge; CD enable-blockers (SSM seating — Siddhesh, OIDC role, repo vars, IIS-drain answer).
- **Migration critical path (COL-205/COL-306) remains THE delivery focus** — untouched this session, deadline 2026-07-31.
- Flagged follow-ups: exact-regex CI backstop for tag rulesets; mobile version-bump sync script; web-angular stale Karma/Jasmine test docs.

## See also

- [[../../architecture/release-process|release-process runbook]]
- [[2026-07-10T1358-CS-320-CS-324-CS-409|prior session]]
- [[../../strategy/active-plan|active plan]]
