---
title: "CS-397 product-repo CI merge gates — investigation + plan + 5 CS tickets + api_node Tier-1 gates built (PR #269, green, not merged)"
tags: [bridge, session, cs-397, cs-398, cs-399, cs-400, cs-401, ci-gates, branch-protection, tsc, eslint, jest, gitleaks, dependabot, semgrep, drizzle, dotnet-freeze, colobbo-web, api-node, pr-269, investigation, verify-first]
last_updated: 2026-06-04
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-397
status: active
---

# CS-397 product-repo CI merge gates — investigation, plan, tickets, first build slice

## Summary

Investigated (verify-first) and planned **blocking CI merge gates for the 3 active product repos** (api_node, web-angular, Colobbo-Mobile; Colobbo-Web = decommissioning), then logged the work as **5 CS tickets** and **built + PR'd the first shippable slice** — the api_node Tier-1 tsc + changed-code eslint gates ([api_node PR #269](https://github.com/Colobbo/api_node/pull/269), both gates **green on the PR**, **NOT merged** — operator approves merges). Ran parallel to the COL-307 connected-records track; closed with `/cibo-handoff --no-plan-update` (canonical plan-state untouched). This is a **product-repo CI** session — distinct from the agent-system's internal `lobe-regression.yml`. All durable work is in Linear (CS-397 tree) + the api_node PR; this repo only carries this bridge record.

## Tickets created (team Colobbo Strategic, nested under CS-301 Track E)

- **CS-397** — parent: "Product-repo CI merge gates (pre-monorepo, changed-code-scoped)". RelatedTo CS-302 (lint overlap, scope untouched), CS-358 (.NET freeze home), CS-375/CS-377 (SAST overlap), CS-22 (security setup).
- **CS-401** — Tier 1: tsc + eslint(changed-code) + jest. Priority High. PR #269 logged as a comment.
- **CS-398** — Tier 2: Dependabot + gitleaks + npm-audit + Semgrep CE (greenfield). Priority Medium.
- **CS-399** — Tier 3 (DEFERRED): Drizzle drift + template/inspection JSON (zod). Priority Low.
- **CS-400** — Enforcement: branch-protection required-checks + Colobbo-Web .NET freeze + posture decisions A/B. Priority High.

## What was built (api_node — product repo, PR #269, base `development`, reviewer jthakkar1981)

Two PR-triggered GitHub Actions workflows, built in an **isolated git worktree off `origin/development`** so the parallel COL-307 checkout (be7c646) was never touched:
- `apps/.github/workflows/ci-typecheck.yml` — `cd apps/api-node && npm ci && npx tsc --noEmit`. **Green (1m0s)** — api-node is genuinely 0-errors in CI.
- `apps/.github/workflows/ci-lint-changed.yml` — merge-base diff (`git diff --diff-filter=ACMR base.sha...HEAD -- 'apps/api-node/**/*.ts'`) → eslint **only changed `.ts`**; runs on all PRs to `development`, **short-circuits to success when no `.ts` changed** (avoids the required-check "waiting forever" gotcha). **Green (12s)** — short-circuit path exercised.
- Commit `4f65a40` (api_node, branch `feature/ci-tier1-gates`). Node 20 LTS in CI (repo has no engines/.nvmrc). **Advisory** until required-check wiring (held).

## Verified baselines (fresh runs / `gh api`, 2026-06-03/04)

- **Branch protection (all 3 integration branches, classic — no rulesets):** 1 required review, `enforce_admins=true`, **no required status checks**. Default branches already = integration branches (COL-260).
- **api_node:** tsc **0 errors** (gateable today); eslint **1,771** (1,767 err) whole-repo → changed-code mandatory; jest **2,492 pass / 3 fail / 2,495** — 3 fails in `src/permissions/__tests__/userScopeDerivation.test.ts` (rank≥20→'me' scope; COL-44/COL-223 territory). #268 jest runtime confirmed on `development` (feat `6162596`, merge `753f271` — resolved the operator's 753f271-vs-6162596 query).
- **web-angular:** tsc **1 error** = `TS6059 design-system.ts not under rootDir 'src'` (tsconfig exclude/relocate, NOT a code bug → Jignesh's area); eslint **2,879** (2,855 err); Karma/Jasmine 114 specs (unrun).
- **Colobbo-Mobile:** can't baseline (no node_modules / no lockfile — blocked on COL-110); **zero CI workflows**; strict:false; Karma/Jasmine 86 specs.

## Verify-first corrections to the original brief

1. **Tier 2 is GREENFIELD, not audit-and-flip.** All 3 repos private; `security_and_analysis` fully **disabled** (Dependabot 404, CodeQL null, secret scanning off). CS-22 + CS-47 marked Done (Mar 2026) but the product-repo wiring verifiably never landed (Linear-status-vs-reality drift). CS-377 confirms "no SAST tool wired today."
2. **CS-49 (ai-review) / CS-50 (weekly-security) cover only api_node + web-angular** (both advisory). Colobbo-Mobile has zero workflows; Colobbo-Web has only graphify-dispatch.
3. **Nuance surfaced during the build:** a `security/snyk (vgera-xyz)` check DOES run on api_node PRs (Snyk app-integration, advisory — "No manifest changes detected"). So Snyk is partially present; the rest of the stack is genuinely absent.
4. **Dedup surfaced an existing cluster** the operator didn't flag: CS-301 (parent) + CS-302 (lint CI, post-monorepo) + CS-358 (.NET freeze) + CS-375/CS-377 (SAST). New work nested under CS-301; CS-302 scope deliberately untouched.

## Held pending operator decision (set up, NOT applied — by design)

No branch-protection mutation was applied this session. Reasons + ready next-steps:
- **api_node required-checks:** cannot wire pre-merge — a required check naming the not-yet-merged workflow would block ALL `development` PRs (incl. parallel COL-307) on "waiting for status". → merge PR #269 first, then `gh api PUT .../branches/development/protection/required_status_checks` adding contexts `typecheck` + `lint-changed`.
- **Decision (A)** `enforce_admins` on new api_node required checks (true=strict, operator blocked too / false=emergency override).
- **Decision (B)** api_node required-review given slow reviewer (keep vs drop + lean on CI).
- **Colobbo-Web .NET freeze:** prepared (`gh api PUT .../Colobbo-Web/branches/dev/protection` with `lock_branch: true`) but **HELD** — operator wants to warn active pushers first. Recent `dev` mergers: **@jthakkar1981 (Jignesh, most frequent), @madhuresh-colobbo (Madhuresh — #367 2026-05-31), @dagaroth00 (notification/websocket), @vgera-xyz**. Freeze `enforce_admins` also flagged.

## Blocked, and on whom

- **web-angular tsc gate** → Jignesh (the 1 `design-system.ts` rootDir tsconfig fix; his repo area — CC did not touch).
- **api_node jest-required** → the 3 `userScopeDerivation` failures (COL-44/COL-223 permission-scope; not fixed blind). jest stays advisory until triaged.
- **Colobbo-Mobile (all gates)** → COL-110 (no lockfile; can't baseline). Mobile also has zero CI workflows.

## What's next

- Operator: decide A/B + the .NET-freeze posture (after warning the dev mergers), then merge PR #269 and wire the api_node required-checks.
- Operator to draft the Slacks from this session's shipped status (CC did not draft them, per instruction).
- Tier 2 (CS-398) / Tier 3 (CS-399) sit below the connected-records feature work; open decision C = defer Tier 2 behind Tier 1 or interleave.

## Process notes

- Linear classifier (auto-mode) correctly **blocked** a first Tier-1 ticket draft that stated the workflows were "Built + PR'd this session" before the PR existed — false-completion-claim-on-a-shared-tracker. Recreated truthfully (CS-401) and added the real PR link as a comment only after PR #269 existed. Lesson: never write past-tense completion claims to a tracker before the artifact exists; create with plan/future-tense, comment the link post-hoc.
- This repo's `git log` hung on its pager mid-handoff (non-TTY); worked around with `git status`/`rev-parse`. No commits were made in colobbo-agent-system this session before handoff (HEAD = ce5eae74a4).

## Commits

- api_node (product repo): `4f65a40` chore(ci): add tsc + changed-code eslint gate workflows for api-node [CS-401] — on `feature/ci-tier1-gates`, PR #269 (not merged).
- colobbo-agent-system: this bridge entry + daily-log + canonical_pages registration (handoff commit).

## See also

- [[../../strategy/active-plan|active-plan]] — canonical plan-state (NOT updated this session; --no-plan-update)
- [[2026-06-03T1041-COL-307|prior bridge — COL-307 kernel]]
- [api_node PR #269](https://github.com/Colobbo/api_node/pull/269) — Tier-1 gates (built, green, not merged)
- [CS-397](https://linear.app/colobbo/issue/CS-397) — parent · [CS-401](https://linear.app/colobbo/issue/CS-401) · [CS-398](https://linear.app/colobbo/issue/CS-398) · [CS-399](https://linear.app/colobbo/issue/CS-399) · [CS-400](https://linear.app/colobbo/issue/CS-400)
- [[../../decisions/adr-034-cs353-plan-state-persistence-boundary|ADR-034]] — plan-state boundary (--no-plan-update)
