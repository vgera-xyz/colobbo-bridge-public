---
title: "Phase-2 routing block — CS-324 dispatch gate (v2.56.0) + CS-407 chain-config externalisation (v2.57.0) + CS-322 headless v1 proven (v2.58.0)"
tags: [bridge, session, cs-324, cs-407, cs-322, cs-359-fix-2, phase-2, routing, dispatch-gate, chain-config, headless, github-actions, adr-012, op-27, v2.56.0, v2.57.0, v2.58.0]
last_updated: 2026-07-09
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-322
status: active
---

# Phase-2 routing block — dispatch gate + chain-config externalisation + headless v1

## Summary

Executed the operator-approved 3-increment Phase-2 routing block end to end, each increment on a CI-green tag, zero product-repo changes, zero new Lobes:

- **Increment 1 — CS-324 dispatch gate** (tag **v2.56.0**, 23 gates, run 29020183816). New `hooks/global/validate-lobe-dispatch.py` (PreToolUse, `Task|Agent` matcher) closes the ADR-012 §10 enforcement gap: fail-closed validation of Lobe sub-agent dispatch input — nonexistent `--input`/`--*-input`/`--*-result` paths, free-text dispatch of composed Lobes (ADR-012 §5 structured-invocation-only), and `--operation` values violating `requested ∈ declared ∩ enabled`. Roster resolution is name-free: exact `glaze.lobes[]` match or suffix-stripped match confirmed by the `_autogenerated: true` compiled-agent marker (discriminates vs freeform siblings `code-reviewer.md`/`test-writer.md`). Operation→atom map ships as additive `operations: {build: produce_plan, audit: produce_findings}` backend LOBE.md frontmatter (compose-verified zero-diff, M6 precedent). 12/12 synthetic-stdin tests; hook never blocks on its own infra failures. **Live-fire pends this handoff's deploy.sh** (settings load at session start). ADR-012 §10 Amendment 1 + hooks-inventory row 14. CS-324 → Done at Step 3a (source-complete; live-fire caveat recorded on ticket).
- **Increment 2 — CS-407 = CS-359 Fix-2 refiled + shipped** (tag **v2.57.0**, 24 gates incl. NEW Gate 24, run 29020745995). PHASE-0 filed [CS-407] after a Pattern-G duplicate search; ticket → Done. `schemas/chain-config-v1.json` (op #27 `dispatch(...)` shared-primitive contract, `chain-ref` nested-chain support, LangGraph-style `on_status` conditional edges) + `chains/build.chain.json` (build sub-chain, encodes S0–S4 exactly) + zero-dep `scripts/validate-chain-config.mjs` (structural rules + referential checks JSON Schema can't express: edge targets, on-disk validator/agent paths, reachability, ready-edge acyclicity; exit contract 0/1/2/3/4) + `lobes/pipeline-fixtures/chain-config/` RED/GREEN harness (**Gate 24**). `/cibo-pipeline` → **v0.6.0**: behaviour-identical rewire, the drift-prone inline stage table REMOVED (single source). CR γ/α adoption deliberately deferred (no Lobe contract edits). feature-pipeline.md §11 updated.
- **Increment 3 — CS-322 v1 bounded scope, PROVEN headless** (tag **v2.58.0** after one tag-move, 24 gates, run 29023015170). `.github/workflows/pipeline-run.yml`: workflow_dispatch → headless CC (`claude -p`, API-key auth, sonnet, 45-min cap) executes the CS-407 chain config with api_node mounted **read-only** (`PRODUCT_REPO_READ_TOKEN`; `COLOBBO_ROOT=$GITHUB_WORKSPACE` resolves the glaze env-indirection so semantic + grounding gates run against real files). **Proof run 29021469473: `outcome=completed`, zero mid-run human intervention** on a 4-AC health-endpoint feature — AND it exercised §6 failure machinery for real: Planner bucket-3 retry-once (monorepo prefix), **Architect genuine substrate bug → [CS-408] filed** (LOBE.md worked example contradicts output-schema enums), Backend grounding advisory, codegen `--dry-run` zero-writes verified (3 greenfield files planned, `index.ts` mount correctly skipped as brownfield-modify). Docs: `wiki/architecture/headless-cc-orchestration.md` (registered in canonical_pages). CS-322 → In Progress; v2 scope stays on ticket (webhook trigger, real emit + PR assembly, Slack/Linear surfacing, 3-parallel proof, cost telemetry, Max-OAuth switch + 5h-cap check).

## Failure log (attempt-driven fixes, all recorded honestly)

1. **CS-322 attempt 1 red**: `ANTHROPIC_API_KEY` repo secret was quote-mangled — the CS-147 cred file wraps values in single quotes; extraction stripped only double quotes → 401. Re-provisioned after a live HTTP-200 key check (the ticket's "org-wide key already configured" claim was stale — no such secret existed; provisioned from the CS-147 cred store under the operator-approved API-key auth decision).
2. **Workflow defects found by attempt 1**: `claude | tee` masked the CLI exit code (pipefail added); untrusted workspace ignored the project settings allow-list (pre-trust via `~/.claude.json`).
3. **v2.58.0 first tag-run red on Gate 1 invariant (k)**: the new wiki page wasn't in `canonical_pages` — my local gate battery had piped every verify script through `| tail -1`, so `set -e` saw tail's exit 0. **Same masking class as the tee bug fixed the same hour.** Fixed (`33226d9b04`), gates re-run pipefail-honest, tag moved via the documented delete-republish procedure.
4. **Artifact gap found by attempt 2**: `scripts/.pipeline-runs/` is dot-hidden and upload-artifact v4 drops hidden files by default → `include-hidden-files: true`.
5. **Push-races ×3** with graphify cron — all resolved merge-not-rebase (rule now proven ×5 fleet-wide); tags verified ancestors of main each time.

## Commits + tags

- `2c56a84aad` feat(hooks): CS-324 Lobe dispatch-input validation gate → tag **v2.56.0**
- `8edd60654b` feat(orchestration): CS-407 externalise pipeline chain-configs → tag **v2.57.0**
- `ca525c2dfa` + `62e35f4ede` + `70a3b893b5` + `33226d9b04` — CS-322 v1 workflow, fixes, proof, invariant-(k) registration → tag **v2.58.0** (moved once, anchored at `33226d9b04`)
- Merges `7605e7e9ba`/`8dc3fc05fc`/`52ff607f1f`/`e5ab43c2fa` — graphify push-race reconciliations

## Verification notes

- All 3 tags CI-green on the full roster (23 → 24 → 24 gates); Gate 24 is new (chain-config integrity, RED case proves 3 drift classes accumulate).
- CS-324 hook: 12/12 synthetic tests (`test/test-validate-lobe-dispatch.py`); compose zero-diff after frontmatter edit; NOT yet live-fired (deploy at this handoff's Step 5 arms it for next session — verify by dispatching a Lobe with a bogus `--input` path and expecting a block).
- Headless proof evidence: workflow run 29021469473 logs + `claude-run.log` artifact; run.json artifact missing on that run (hidden-files gap, fixed after).
- **Meta-lesson (2 instances in one session): never let a pipeline stage mask a load-bearing exit code** — `cmd | tee` and `verify.sh | tail` both need pipefail. Candidate for bash-jq-reflexes if it recurs.

## What's next

- **[CS-408]** — Architect LOBE.md worked-example vs schema-enum contradiction (+ check Gate 4 harness coverage gap). Small substrate ship.
- **CS-322 v2**: webhook trigger, real emit + PR assembly, Slack/Linear surfacing, 3-parallel proof, cost telemetry; Max-OAuth switch + 5h-window cap check before scale-up.
- **CS-324 live-fire check** next session (hook armed by this handoff's deploy).
- **Phase-2 architecture remainder**: ADR-012 Layer-2 activation stays a per-Lobe non-item until multi-op surfaces exist (only Backend has one, already active).
- **Migration critical path (COL-306 batch-1)** remains the highest-deadline-pressure track — untouched this session by design.

## See also

- [[../../decisions/adr-012-lobe-operating-modes|ADR-012]] §10 Amendment 1 — the gate CS-324 implements
- [[../../raku-meta/cs-373-derive-lobes-result|CS-373 derivation]] §10.3/§10.7 — the dependency chain this block executed
- [[../../architecture/headless-cc-orchestration|headless-cc-orchestration]] — the CS-322 v1 doc page
- [[../../architecture/feature-pipeline|feature-pipeline]] §11 — chain-config externalisation note
- [[2026-07-09T0528-CS-373|prior session — Phase-2 kickoff (Performance + Design Lobes)]]
