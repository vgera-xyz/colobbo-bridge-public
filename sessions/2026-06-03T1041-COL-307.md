---
title: "COL-307 connected-records kernel — Phase 1 (Tasks 1-2) built + jest-verified green + pushed to PR #250; test-runtime shipped as its own merged PR #268"
tags: [bridge, session, col-307, col-341, connected-records, kernel, work-orders, work-packages, r2, via-inspection, dedup, jest, test-runtime, api-node, pr-250, pr-268, build, verified]
last_updated: 2026-06-03
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-307
status: active
---

# COL-307 connected-records kernel — Phase 1 built, jest-green, pushed

## Summary

Built **Phase 1 (Tasks 1-2)** of the connected-records kernel in `api_node` (the read-side aggregation that supersedes COL-341's materialise approach), per the byte-verified map [[../../archaeology/connected-records-link-graph|connected-records-link-graph]] and the build plan in [[../../strategy/active-plan|active-plan]]. The kernel is **jest-verified green (3 suites / 28 tests)** and pushed to `col-307-work-order-context-enrichment-backend` (PR #250) at `be7c646`. The test runtime that api-node was missing shipped as its **own** PR ([api_node #268](https://github.com/Colobbo/api_node/pull/268)) and **merged to development first**, then the kernel branch picked it up. All work is in the product repo `api_node` — this bridge is the agent-system knowledge record.

Operator drove this as a plan-mode build with two mid-flight rulings: **R2 unconditional, no flag** (the #250 COL-307 code is a throwaway pipeline-smoke artifact being redone — don't preserve its behaviour/tests), and **test-runtime as a separate merged-first infra PR** (don't bolt deps onto the throwaway branch).

## What was done

1. **Housekeeping (COL-341).** Posted the operator's verbatim approach-revision comment (read-side aggregation over materialise; description left intact for history). The COL-341↔COL-307 relation was **already in place** — verified live, no action (the active-plan had listed it as still-pending; live state had moved).

2. **Task 1 — kernel extraction (byte-verified pure move).** Lifted `resolveDescendantWorkOrderIds` / `resolveLinkedEntityIds` / `aggregateOverWoSet` + `SummaryBlock` from `workOrderSummaryService.ts` into new `apps/api-node/src/services/connectedRecords/kernel.ts`; service re-imports + re-exports `SummaryBlock`; controller + routes untouched. A diff of the moved functions vs origin showed **only reworded comment prose — zero logic-line changes**.

3. **Task 2 — generalise + R2 + dedup (one clean path, no flag).**
   - `resolveLinkedEntityIds(sourceIds, sourceModule, targetModule)` — source-module parameterised.
   - `aggregateOverWoSet` → `aggregateOverAnchorSet(anchorType, anchorIds, authCompanyId)` — `anchorType` is the anchor-agnostic seam (WP rolls up via member WOs upstream; WP-direct an empty no-op today per the map).
   - **R2 (WO→Inspection→Action) unconditional**: connected action set = `dedup(R1-direct ∪ R2-via-inspection)` by record identity (`Set` union). Defensive against the best-effort app-layer link mirror.
   - **Bounded query count** (the N+1 code-shape check): batched `inArray` per hop, never per-WO/per-inspection; descendant resolve is one CTE; **B1 only — no closure table** (B2 deferred to the Task-4 EXPLAIN). Encoded as `select`-call-count assertions (10 with inspections / 6 without).
   - **R2 literals `'Inspection'`/`'Action'` verified** against `actionService.ts:218-220` (production query) + the web-angular `AppModuleName` enum + `create-inspections` write sites — the one silent-undercount risk, closed.

4. **Tests + a pre-existing mock-leak fix.** New `connectedRecords-kernel.test.ts` (order-robust — independent of `Promise.all` interleaving — covering R2 fire/skip, dedup, bounded select-count, Inspection→Action direction). Updated `C1.test.ts` for the new R2 query order; `C1-e2e.test.ts` needed no change. First jest run was RED on two `execute).not.toHaveBeenCalled()` assertions — a **pre-existing latent bug in the never-executed throwaway C1.test.ts** (its `beforeEach` reset the row queues but never cleared the `execute`/`select` jest.fn call history; `C1-e2e` already had the `mockClear`). Fixed (mirrors C1-e2e) → re-run **28/28 green**.

5. **Test runtime → its own PR.** api-node shipped jest tests + `jest.config.ts` + `@types/jest` but **no jest/ts-jest/supertest/ts-node runtime** — tests couldn't execute (locally or CI). Shipped [#268](https://github.com/Colobbo/api_node/pull/268) (jest@29 / ts-jest@29 / supertest@7 / ts-node@10 matching the sibling `notification-service`, `@types/jest` 30→29), **merged to development**. Verified zero prod-dep version changes; the 8 npm-audit advisories are all **pre-existing** (aws-sdk/exceljs/uuid + drizzle-kit→esbuild) — none through the test runtime.

## Verification

- `#268` merge into development confirmed by **content** (development's `package.json` declares the runtime) + ancestry (`6162596` is an ancestor), not just PR-status.
- Merged development into col-307 (clean, no conflicts, `f16c5ee`); all 5 schema tables still exported from `schema.ts`; `npm install` "up to date" (merged lockfile valid).
- `tsc --noEmit` clean repo-wide against merged development.
- `npx jest connectedRecords-kernel C1` → **3 suites, 28/28 green**.
- Push verified in sync: local col-307 = origin col-307 = `be7c646`.

## What's next

- **Next session entry = Task 3** — WP count-by-type grid endpoint `GET /api/work-packages/summary-grid` (paginated, server-side filter/sort on counts), built on the verified kernel via `aggregateOverAnchorSet`. **Not started this session by operator direction.** Then Task 4 (tenant invariant + EXPLAIN), Task 5 (WO grid), Task 6 (PR).

## Carry forward

- **File a dependency-hygiene ticket** for the pre-existing api-node prod-vuln finding (aws-sdk / exceljs / uuid, incl. 1 high) — out of scope for the test-infra PR.
- **Task 4 needs dev-DB read access** (Siddhesh ask in flight) for the rollup-grid EXPLAIN / index-hit confirmation against a prod-like dataset.
- **Watchpoint (CS-320 gremlin class):** a background auto-pull/checkout process drifted the `api_node` working checkout from col-307 to development mid-session (`AUTO_MERGE.lock` race class); restored to col-307, **no work lost**, push verified in sync. Worth confirming the checkout branch at the start of the next api_node session.

## Commits (api_node — product repo)

- `224c686` feat(work-orders): connected-records kernel + R2 rollup [COL-307]
- `f16c5ee` merge origin/development into col-307
- `be7c646` test(work-orders): clear mssql dispatch mocks between C1 tests [COL-307]
- `6162596` chore(api-node): add jest/ts-jest/supertest/ts-node test runtime (PR #268, merged)

## See also

- [[../../archaeology/connected-records-link-graph|connected-records link graph (byte-verified map — canonical)]]
- [[../../strategy/active-plan|active-plan]] — build plan; next-session entry = Task 3
- [[2026-06-03T0715-COL-307|prior bridge — build plan + map persisted]]
- [COL-307](https://linear.app/colobbo/issue/COL-307) — WO summary / context enrichment (backend PR #250)
- [COL-341](https://linear.app/colobbo/issue/COL-341) — LinkModule materialise (superseded by read-side; relation in place)
- [api_node PR #250](https://github.com/Colobbo/api_node/pull/250) — COL-307 backend (kernel landed)
- [api_node PR #268](https://github.com/Colobbo/api_node/pull/268) — test runtime (merged to development)
