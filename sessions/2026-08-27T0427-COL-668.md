---
title: "Session 2026-08-27T0427 — COL-668 Mobile Message SMS: PR-A #576, PR-B #577, web-angular #830 built and open"
tags: [bridge, session, col-668, col-491, sms, mobile-message, notification-service, api-node, web-angular, dedupe]
last_updated: 2026-08-27
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-668
status: active
---

# Session 2026-08-27T0427 — COL-668

**Product work (Colobbo), recorded here because this wiki is the memory corpus.** No agent-system configs changed; `deploy.sh` deliberately not run. Plan executed: [[../../strategy/sms-channel-build-plan|sms-channel-build-plan]].

## Summary

The whole SMS build from the plan is now code, tested, and in three stacked PRs. **PR-A** [api_node #576](https://github.com/Colobbo/api_node/pull/576) — duplicate sends are skipped at dispatch for every channel (PR #516 only stopped duplicate rows), re-sent only when the earlier attempt is `failed` or stale-`pending`; `recipientPhone` joins the dedupe key. **PR-B** [api_node #577](https://github.com/Colobbo/api_node/pull/577) (base = PR-A branch) — the channel: batched Mobile Message client with AU normalisation, `smsDispatch` with the quiet-skip rule, SMS body/gate in the template resolver, phone from the existing `GetUser` lookup, DLR webhook with HMAC verification mounted before `express.json()` under the IIS-proxied prefix, `SmsUsage` + `SmsDeliveryReceipt` schema with a hand-applied SQL migration, and `'sms'` declared beside every `inApp` producer in api-node (work orders, actions, bookings, inspection triggers, schedules, reminder cron as one text per user per run). **web-angular** [#830](https://github.com/Colobbo/web-angular/pull/830) (base = #826 branch) — the Text toggles go live with cost/reach copy. Lee's 2026-08-27 reply was folded into the plan and the Mobile Message artifact: sender-ID timeline is a few business days once both myIDs exist; a dedicated number needs no ACMA step and is the Phase-1 sender; first credit purchase unlocks it plus the 1.6¢ rate and live link shortening.

## Verified, not assumed

- Dev box IIS rule read live over SSM: `^api/(notifications|device-tokens)(.*)` → `localhost:7000/{R:0}` — the webhook path is reachable.
- Dev Postgres (`cs_agent_ro`): `Notifications` has no `recipientPhone`, the #516 `dedupeKey` index exists, 0 of 24 settings rows have Text on; dev MSSQL: `tblUser.phoneNo varchar(20)` and a nine-digit `4XXXXXXXX` form exists in real rows — the normaliser accepts it.
- Mobile Message's published HMAC vector reproduced locally and pinned in the test suite.
- Gates: notification-service 556 tests / tsc / eslint / mutation 83.8 % (CI ranges); api-node tsc / full suite 8,555 pass / eslint / mutation 85.6 %; web-angular full `ng test` 2,145 + 4 / strict tsc on the module / eslint.

## Open verification

- **`SP_UserGet` → `phoneNo`.** `OBJECT_DEFINITION` is NULL for `cs_agent_ro` (no VIEW DEFINITION), so the SP body could not be read. The api-node mapper reads `userData.phoneNo`, so the field name is right; whether it is populated needs one authenticated `GET /api/User/GetUser?id=` on dev. If null, users are skipped quietly and `recipientPhones[]` is the fallback.
- api-node mutation gate: Stryker's dry run trips on an unrelated `exportWorker` spool-reaper test (10 s timeout under instrumentation); the 85.6 % came from a local-only longer `testTimeout`. If `gates-changed` goes red on that test it is not this change.

## Corrections

- The signature-middleware test app initially mounted `express.json()` before the raw route — the exact mis-order the middleware guards against — so every request was rejected; the test app now mirrors `app.ts` and the wrong order is a pinned rejection case.
- The pre-existing request validator 400'd a `recipientPhones`-only request; it now accepts it.
- `.env.example` documentation edit tripped the sensitive-writes hook; the change is variable names with empty values, kept.

## Commits

- colobbo-agent-system: `2a411eeb69` plan ← Lee's reply + Day-0 · `fe84c7fb5f` PR-B status · `a4fc23aa56` §4.10 · this handoff.
- api_node `fix/col-668-dispatch-dedupe`: `5b847af7` → #576. `feature/col-668-sms-channel`: `969fcfab` channel · `33a8efb6` smsDigestBody · `c8a41523` producers → #577.
- web-angular `feature/col-668-text-toggle`: `e2bdfc23` → #830.

## What's next

1. **Operator Day-0** (plan §2, reordered per Lee): sign up, first credit purchase, claim the number, submit the `COLOBBO` sender-ID registration, myID on both sides, reply to Lee for the sandbox, generate the webhook secret, `.env` on dev.
2. Apply `scripts/migration-col668-sms.sql` on dev → `prisma generate` → `/deploy-node dev notification` → sandbox e2e per plan §5.
3. Settle the `SP_UserGet` phone question with one authenticated request.
4. Reviews: #576 → #577 (retarget to `development` when #576 merges), #826 → #830 (retarget to `development-stage`). Reviewer on all three is jthakkar1981 per the brief; web-angular default is pjtech18.
5. Worktrees to remove after merges: `worktrees/col-668-sms` (api_node), `worktrees/col-668-text-toggle` + `notification-settings-redesign` (web-angular).

## See also

- [[../../strategy/sms-channel-build-plan|SMS channel build plan]] · [[2026-08-27T0315-COL-668|previous session]] · [[../../architecture/notification-audit|notification audit]] · [[../../strategy/active-plan|active plan]]
