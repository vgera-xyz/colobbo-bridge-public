---
title: "Session 2026-08-27T0427 — COL-668 Mobile Message SMS: PR-A #576, PR-B #577, web-angular #830 built and open"
tags: [bridge, session, col-668, col-491, sms, mobile-message, notification-service, api-node, web-angular, dedupe]
last_updated: 2026-08-27
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-668
status: active
amended:
  - timestamp: "2026-08-27T09:28:00Z"
  - timestamp: "2026-08-27T10:30:00Z"
  - timestamp: "2026-08-27T11:40:00Z"
  - timestamp: "2026-08-27T13:50:00Z"
    reason: "dev deploy of #585/#586/#587/#836; #840 drawer polish; billing plan + Stripe connected"
    reason: "follow-up PRs #585/#586/#587/#836; plain-SMS decision; remaining tests deferred"
    reason: "live app-path e2e delivered to operator; self-assignment rule + actionCode gap"
    reason: "dev deploy + sandbox e2e passed; node_modules symlink incident (#583)"
---

# Session 2026-08-27T0427 — COL-668

**Product work (Colobbo), recorded here because this wiki is the memory corpus.** No agent-system configs changed; `deploy.sh` deliberately not run. Plan executed: [[../../strategy/sms-channel-build-plan|sms-channel-build-plan]].

## Summary

The whole SMS build from the plan is now code, tested, and in three stacked PRs. **PR-A** [api_node #576](https://github.com/Colobbo/api_node/pull/576) — duplicate sends are skipped at dispatch for every channel (PR #516 only stopped duplicate rows), re-sent only when the earlier attempt is `failed` or stale-`pending`; `recipientPhone` joins the dedupe key. **PR-B** [api_node #577](https://github.com/Colobbo/api_node/pull/577) (base = PR-A branch) — the channel: batched Mobile Message client with AU normalisation, `smsDispatch` with the quiet-skip rule, SMS body/gate in the template resolver, phone from the existing `GetUser` lookup, DLR webhook with HMAC verification mounted before `express.json()` under the IIS-proxied prefix, `SmsUsage` + `SmsDeliveryReceipt` schema with a hand-applied SQL migration, and `'sms'` declared beside every `inApp` producer in api-node (work orders, actions, bookings, inspection triggers, schedules, reminder cron as one text per user per run). **web-angular** [#830](https://github.com/Colobbo/web-angular/pull/830) (base = #826 branch) — the Text toggles go live with cost/reach copy. Lee's 2026-08-27 reply was folded into the plan and the Mobile Message artifact: sender-ID timeline is a few business days once both myIDs exist; a dedicated number needs no ACMA step and is the Phase-1 sender; first credit purchase unlocks it plus the 1.6¢ rate and live link shortening.

## Verified, not assumed

- Dev box IIS rule read live over SSM: `^api/(notifications|device-tokens)(.*)` → `localhost:7000/{R:0}` — the webhook path is reachable.
- Dev Postgres (`cs_agent_ro`): `Notifications` has no `recipientPhone`, the #516 `dedupeKey` index exists, 0 of 24 settings rows have Text on; dev MSSQL: `tblUser.phoneNo varchar(20)` and a nine-digit `4XXXXXXXX` form exists in real rows — the normaliser accepts it.
- Mobile Message's published HMAC vector reproduced locally and pinned in the test suite.
- Gates: notification-service 556 tests / tsc / eslint / mutation 83.8 % (CI ranges); api-node tsc / full suite 8,555 pass / eslint / mutation 85.6 %; web-angular full `ng test` 2,145 + 4 / strict tsc on the module / eslint.

## Open verification

- **`SP_UserGet` → `phoneNo`.** `OBJECT_DEFINITION` is NULL for `cs_agent_ro` (no VIEW DEFINITION), so the SP body could not be read. The api-node mapper reads `userData.phoneNo`, so the field name is right; whether it is populated needs one authenticated `GET /api/User/GetUser?id=` on dev. If null, users are skipped quietly and `recipientPhones[]` is the fallback.
- api-node mutation gate: Stryker's dry run trips on an unrelated `exportWorker` spool-reaper test (10 s timeout under instrumentation); the 85.6 % came from a local-only longer `testTimeout`. If `gates-changed` goes red on that test it is not this change.

## Corrections

- The signature-middleware test app initially mounted `express.json()` before the raw route — the exact mis-order the middleware guards against — so every request was rejected; the test app now mirrors `app.ts` and the wrong order is a pinned rejection case.
- The pre-existing request validator 400'd a `recipientPhones`-only request; it now accepts it.
- `.env.example` documentation edit tripped the sensitive-writes hook; the change is variable names with empty values, kept.

## Commits

- colobbo-agent-system: `2a411eeb69` plan ← Lee's reply + Day-0 · `fe84c7fb5f` PR-B status · `a4fc23aa56` §4.10 · this handoff.
- api_node `fix/col-668-dispatch-dedupe`: `5b847af7` → #576. `feature/col-668-sms-channel`: `969fcfab` channel · `33a8efb6` smsDigestBody · `c8a41523` producers → #577.
- web-angular `feature/col-668-text-toggle`: `e2bdfc23` → #830.

## What's next

1. **Operator Day-0** (plan §2, reordered per Lee): sign up, first credit purchase, claim the number, submit the `COLOBBO` sender-ID registration, myID on both sides, reply to Lee for the sandbox, generate the webhook secret, `.env` on dev.
2. Apply `scripts/migration-col668-sms.sql` on dev → `prisma generate` → `/deploy-node dev notification` → sandbox e2e per plan §5.
3. Settle the `SP_UserGet` phone question with one authenticated request.
4. Reviews: #576 → #577 (retarget to `development` when #576 merges), #826 → #830 (retarget to `development-stage`). Reviewer on all three is jthakkar1981 per the brief; web-angular default is pjtech18.
5. Worktrees to remove after merges: `worktrees/col-668-sms` (api_node), `worktrees/col-668-text-toggle` + `notification-settings-redesign` (web-angular).

## Addendum 2026-08-27T0928 — dev deploy + sandbox e2e passed; node_modules symlink incident (#583)

**Deployed to dev and proven end-to-end.** #576/#577/#583 merged; migration applied on `colobbo_db_dev_notifications`; both node apps rebuilt + restarted (`pm2 save`); web release `20260827-091501` staged and junction-swapped, app pool recycled; served bundle byte-identical; Text column live on dev. Sandbox credentials moved to dev's `.env` over SSM (never via chat). Plan §5 run from the box: valid send → signed DLR → `delivered` (+ `SmsUsage` + receipt); duplicate re-fire → skipped, no call; `…000` → `failed`; landline → no row; Text off → 0 rows. Toggles restored.

**Incident (mine):** `apps/notification-service/node_modules` was committed as a *symlink* in #576 (`.gitignore`'s directory-only `node_modules/` does not match a symlink); on the Windows dev box the checkout turned it into a text file that deleted the real install — caught before restart, fixed by #583 (`git rm --cached` + bare `node_modules` ignore) and `npm ci` on the box. Memory: `feedback-never-symlink-node-modules-in-a-worktree`. Also fixed in the deploy-web skill: app-pool recycle after the swap (IIS kernel cache), chunk names are hashed, the `main-` grep never matched.

**Not applied on dev:** COL-623's `IX_tblWorkOrder_dropdown` (other ticket's index in the same range) — flagged to the operator.

## Addendum 2026-08-27T1030 — live app-path e2e delivered to operator; self-assignment rule + actionCode gap

Dev switched to the **live** Mobile Message account (values moved clipboard → local file → SSM; the env name is `MOBILE_MESSAGE_API_PASSWORD` — my first pass wrote `_API_KEY`, caught by the masked-key listing and fixed before any send). Operator's mobile added to their dev user; Action → Assigned Text on (settings category 25 / template 22). An action assigned to the operator from a second login produced email + in-app + **sms `delivered`** in the same second, through the user→phone lookup — the `SP_UserGet` open item is closed. The operator's first attempt from their own login produced only the notify-to email: `excludeActorFrom{Ids,Emails}` drops the actor on every channel (pre-existing product rule, action + work-order routes). Also found: `{{actionCode}}` empty in the action-assigned payload; settings Edit dialog's Update reverts a just-flipped list toggle. Jignesh DM'd about the unapplied COL-623 index (his #562/#580).

## Addendum 2026-08-27T1140 — follow-up PRs #585/#586/#587/#836; plain-SMS decision; remaining tests deferred

**Live tests on the operator's phone:** link shortening ✓; 242-char text → 2 parts ✓; texts with `·` / emoji / `’` each billed 1 part — **Mobile Message strips non-GSM characters unless `enable_unicode`**, which the client never set. After checking Twilio Smart Encoding + Calendly/field-service practice the operator chose **plain SMS, no Unicode**: `toSmsText()` replaces typographic characters and removes emoji/non-Latin before the send, `max_parts: 3` (api_node **#587**); the preview mirrors the table (web-angular **#836**). Also opened **#585** (every action placeholder — `actionCode` was blank on the first live text) and **#586** (seed copy `·` → `-`, GSM-7 guard test). Dev: Action → Assigned text now `… ({{actionCode}}) … Open it: {{docUrl}}`, Text on; live credentials remain.

**Deferred by the operator:** work-order-from-UI, reminder-digest and booking-customer texts (all need a technician user carrying a phone); the action path is the accepted end-to-end proof. Three toggle reverts tonight were the #836 bug in action. Session also switched accounts mid-way (avatar CA) — edits saved under that login.

## Addendum 2026-08-27T1350 — dev deploy of #585/#586/#587/#836; #840 drawer polish; billing plan + Stripe connected

**Deployed to dev:** api_node `development` 86be4522 (#585 action placeholders, #586 seed copy, #587 plain-SMS transliteration + 3-part cap) with seed re-run; web-angular `development-stage` 86df58ad (#836) release `20260827-122330` — both verified through the public host. All four had pjtech18 + jthakkar1981; mutation gate on #587 raised 47 → 90 % by pinning the replacement table by value; Lighthouse red is a `chrome-error://` infra failure across branches, not required.

**#840 (open, web-angular):** legacy `{token}` placeholders preview; EOI without push/text previews; "personalisation tokens" wording; compact pills for email + pill rows above Push/Text inserting at the cursor; one sample-values note + separated Email/Push/Text sections; Cancel/Update in the header; unsaved-changes prompt (`disableClose` + inline keep/discard). Operator praised the design pass → memory `feedback-ui-polish-standard` ("drawer standard" / "design pass").

**Billing:** operator wants monthly customer usage + billing with Stripe. Plan written: [[../../strategy/sms-billing-build-plan|sms-billing-build-plan]] — allowance per active user pooled per company → 12-month packs → metered overage; Settings → Billing behind a new `billing` permission showing the whole bill (licence + SMS); invoice customers via Stripe `send_invoice`; texts pause at 100 % for card customers only. Stripe MCP connected: live `acct_1SZirQFFa3Ggr1Wd` + sandbox `acct_1SZirbJwv4KNOL6u` (build only in the sandbox; nothing created yet). Open: the standard numbers (§4 recommendation). Flag: existing enterprise pack 3,000 = A$2,700 is 90¢/part.

**Deferred by the operator:** work-order-from-UI, reminder-digest, booking-customer texts. Memory added: `project-mobile-message-plain-sms`.

## See also

- [[../../strategy/sms-channel-build-plan|SMS channel build plan]] · [[2026-08-27T0315-COL-668|previous session]] · [[../../architecture/notification-audit|notification audit]] · [[../../strategy/active-plan|active plan]]
