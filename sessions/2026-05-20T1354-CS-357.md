---
title: "CS-357 + CS-318 ship — Migration Lobe substrate redesign + WorkOrderType validation PR DRAFT"
tags: [bridge, session, cs-357, cs-318, migration-lobe-redesign, adr-035, read-first-body-analysis, move-or-keep, code-translation-mode, modify-existing-files, dependent-sp-recursive-body-read, three-round-2-round-bound-extended-on-evidence, workordertype-validation, api-node-pr-246, v2-39-0, v2-39-2, tag-recovery, propagation-obligation-without-forcing-function]
last_updated: 2026-05-21
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-357
status: active
amended:
  - timestamp: "2026-05-20T15:09:59Z"
    reason: "tag-recovery v2.39.0→.1→.2 + main merge + 6 CS-357 followup tickets filed (CS-360-365)"
---

# CS-357 + CS-318 — Migration Lobe substrate redesign + WorkOrderType validation PR DRAFT

## Summary

Shipped **CS-357** (Migration Lobe substrate redesign — read-first body analysis + MOVE-or-KEEP contract; ADR-035) as `v2.39.0` on `feature/cs357-migration-read-first` branch. Validated end-to-end on **CS-318** (the WorkOrderType GetAll integration case that surfaced the v0.1 substrate's structural defect). The api_node-side PR ships as **DRAFT #246** to `Colobbo/api_node`'s `development` branch, reviewer @jthakkar1981. The chain reached `status: ready` from Code Reviewer **Round 3** (operator-authorized extension beyond the 2-round bound on scoping evidence — dependent-SP body-read verified CONTAINED before build).

## What shipped (CS-357 substrate on `feature/cs357-migration-read-first`)

**ADR-035** authored at `wiki/decisions/adr-035-cs357-migration-lobe-read-first-body-analysis.md` (285 lines). Decision of record. Supersedes ADR-033 §1.D2 (the `bridge_strategy` selector axis on the codegen idiom map). ADR-033 the FILE is NOT edited (immutable-ADR precedent); ADR-033's other decisions (§1.D1 absent-`arch_component_id` resolution chain, §1.D3 parsed `__skip__` sentinel pattern, §1.D4 greenfield-emit baseline, §1.D5 reworked HIGH-D guard, §1.D6 glaze namespace + tier-aware §10.2 gate posture) are **preserved**.

**Migration Lobe v0.2.0**: Step 5 rewritten as "Body Analysis" (was "Bridge Resolution"). Reads the legacy method body for every target via the Read tool; applies a deterministic MOVE-or-KEEP structural-analysis rule (default-to-KEEP on ambiguity). `glaze.migration.architectural_assumption.shape_breakdown` no longer consulted by the Lobe (deprecated for Lobe consumption; documentation-only).

**MigrationResult schema 2.0.0** (MAJOR bump — classifier-semantics shift): 4 new REQUIRED per-target fields documented:
- `code_translation_mode: "move" | "keep"` — the new primary selector axis.
- `legacy_method_signature: string` — the C# signature read from the body.
- `legacy_source_ref: {file_path, line_range}` — body-read provenance.
- `legacy_method_body_summary: {primary_sp, pre_loop_service_calls, in_loop_service_calls, in_loop_filter_attachments, post_loop_attachments, empty_recordset_branch, tsql_procedural_signals, dto_field_list}` — structured analysis output.

Existing `bridge_strategy` field retained with NEW DERIVED semantics: `move → dark-launch`; `keep → parallel-sp`. Downstream test-writer + doc-writer contract preserved (they read the field-value, not the derivation semantics).

**Codegen substrate v2.0** (`agents/global/migration-lobe-codegen.md`):
- Idiom-map key axis shifted: `framework_specific_hints[<target_stack>][<code_translation_mode>]` (was: `[<bridge_strategy>]`). Both `move` + `keep` keys WIRED for `api_node` at v2.0.0; the 4 old keys (`fdw`, `chained-http`, `parallel-sp`, `dark-launch`) dropped from `framework_specific_hints`. ADR-031 §1.D3 parsed-sentinel skip mechanism NOT inherited at Migration v2.0 (Database codegen continues to inherit).
- Multi-file emit: KEEP mode emits 2 files (service + route); MOVE mode emits 3 files (schema + service + route).
- **modify_existing_files capability** (per-file emit_mode matrix, Round-2 build): route file → `modify-append-handler` (insertion before `export default router;` anchor); service file → `modify-append-export` (insertion at end-of-file anchor); index.ts → `append-mount-line` (idempotent). All anchors are insertion-only; no AST parsing; string-anchor + idempotency suffice for the api_node-style modular convention.
- **Dependent-SP recursive body-read** (Round-3 build): per `pre_loop_service_calls / in_loop_service_calls / post_loop_attachments` entry, body-read the named service method to extract `dependent_sp: {name, params}` mirroring the primary_sp shape. Depth-cap at 3 (folds in the #6 transitive-KEEP delegate-recursion fix).

**Glaze edits** (both `colobbo.glaze.json` + `fintech.glaze.example.json` symmetric):
- Dropped old bridge_strategy keys from `framework_specific_hints.api_node`.
- Added `move` + `keep` keys with substantial multi-paragraph idiom strings.
- New entries: `empty_list_convention[<stack>] = "bare-empty-array"`, `entrypoint_path[<stack>] = "apps/api-node/src/index.ts"`, `mount_pattern[<stack>]` (2-line import + app.use template).
- Pattern Q resnap of 63 sibling fixtures (additive glaze edit on `.migration.codegen`).

**§10.2 builder-tier smoke gate** (`lobes/pipeline-fixtures/run.sh`): Migration branch partition assertion shifted from `bridge_strategy` (2 wired / 2 sentinel-skip) to `code_translation_mode` (2 wired / 0 out-of-axis). Schema-version guard added (rejects v1.x MigrationResult inputs). Smoke gate exits 0 with all 6 codegen contract gates.

**All 3 Migration Lobe test-fixtures updated** to schema 2.0.0 with new required fields. `cases.json` version assertions bumped to 2.0.0 / 0.2.0. Per-fixture harness runs 3/3 PASS.

**Wiki updates**: `wiki/raku-meta/migration-lobe-history.md` appended with the v2.0.0 redesign entry. `wiki/decisions/index.md` ADR-035 cross-link added. `glaze.wiki.canonical_pages[]` 168 → 169 (ADR-035 atomically registered; invariant k PASS). `wiki/architecture/feature-pipeline.md` §10.2.1 dated notes extended (CS-330 + CS-357 update blocks documenting the Migration→Backend handoff payload shape — CS-318 AC #2 + AC #6).

**VERSION** 2.38.0 → 2.39.0 (MINOR; additive substrate change + capability extension). `lobes/_registry.json` regenerated (system_version = 2.39.0). `CHANGELOG.md [2.39.0]` entry added.

## CS-318 validation (the 3-round empirical journey)

**Round 1** (the v0.1 substrate first-pass, pre-redesign session): Code Reviewer halted at `escalated_to_architect` with 3 critical/high architect-domain findings (DTO parity gap, empty-recordset semantic inversion, file-decomposition gap) + 5 HIGH/MEDIUM/LOW (auth gap, route unregistered, SP-registry bypass, file-org divergence, console.error). Root cause: name-based pre-classification + no body-read.

**Round 2** (v2.0 substrate first pass): 7 of 8 Round-1 findings RESOLVED. But 1 new HIGH surfaced — the **same SP-name failure class recursed ONE LEVEL DEEPER**: codegen body-read the PRIMARY SP correctly (`SP_WorkOrderTypeGet` vs the stale catalog's `SP_WorkOrderTypeGetByStatus`) but did NOT recursively body-read the DEPENDENT service methods. 5 of 8 dependent SP names guessed correctly by lucky string-mangle, 3 wrong: `SP_WorkOrderTypeSkillGet` → actual `SP_WorkOrderTypeSkillSetDetailsGet`; `SP_WorkOrderTypeMilestoneGet` → actual `SP_MilestoneModuleLinkGet` (different params); `SP_MenuGet` → actual `SP_GetMenuDetails`. Production runtime crash with "Could not find stored procedure" errors.

**Round 3** (operator-authorized extension of the 2-round bound on scoping evidence — read each of the 8 dependent method bodies; verified CONTAINED: all 8 are flat depth-1 single-SP shapes, no nested compositions). Built: LOBE.md Step 5 dependent-SP recursive body-read (depth-cap 3); output-schema.json `dependent_sp` optional field on all 3 entry types in `legacy_method_body_summary`; codegen consumes `dependent_sp.name` verbatim. Re-emitted: deleted Round-2 service.ts (wrong SP names) + re-ran codegen Round-3 LIVE → greenfield-create service.ts with 9/9 verbatim SP names + correct params; route file idempotent-skip (Round-2 modify-append-handler intact); index.ts idempotent-skip (already mounted). **`tsc --noEmit` PASS exit 0.** **Code Reviewer Round 3: `status: ready`, 0 critical / 0 high / 1 LOW** (doc-comment count drift "6 dependent SPs" — actual 8; chore-followup, non-blocking).

The 2-round bound was extended on **evidence** (scope verdict before build), not blindly. Round 3 closed a class-level substrate gap, not a one-shot ticket bug.

## What shipped to api_node (PR DRAFT #246)

**Branch**: `cs-318-workordertype-via-cs357-substrate` off `Colobbo/api_node:development`.
**PR**: https://github.com/Colobbo/api_node/pull/246 (DRAFT, reviewer @jthakkar1981, integration branch `development`).

**Files**:

- `apps/api-node/src/modules/work-order-type/work-order-type.service.ts` (NEW, 253 lines) — composition-shell over 9 SPs: primary `SP_WorkOrderTypeGet` + 4 pre-loop batch (`SP_WorkOrderTypeUDFGet`, `SP_CheckListRulesGet`, `SP_WorkOrderTypeWorkFlowGet`, `SP_WorkOrderTypeWorkFlowDetailsGet`) + 2 in-loop per-row (`SP_WorkOrderTypeSkillSetDetailsGet`, `SP_MilestoneModuleLinkGet`) + 2 post-loop (`SP_FormGet`, `SP_GetMenuDetails`). Empty primary recordset returns bare `[]` per ADR-035 §3 deliberate divergence (Jaydeep-verified bare-`[]` is the api_node GET-list convention).
- `apps/api-node/src/routes/workOrderTypeRoutes.ts` (MODIFIED, +58 lines insertion-only) — 5 new imports + `assertCompanyAccess` helper verbatim-mirror of `riskThresholdRoutes.ts:39-71` + new `GET /work-order-types` handler (`authenticateJWT` + `assertCompanyAccess`; `companyId` from `req.user.selectedCompanyId`). 6 pre-existing SOR-related handlers preserved verbatim.
- `apps/api-node/src/index.ts` UNCHANGED — module already mounted via the existing route file (idempotent skip per ADR-035 §1.D5).

**CI**: 2 pre-existing development-branch tsc errors on `drizzle-orm-mssql` modules (unrelated to CS-318; missing devDeps on the development branch). CS-318 emit itself is clean (`grep workOrderType` on tsc output → 0 hits).

## Telemetry (CS-322 Day-2 input)

Coarse run-level capture per the CS-353 cibo-pipeline v0.2.0 run.json contract. The CS-318 chain produced an enriched MigrationResult v2.0.0 + Round-3 codegen-emit + Code Reviewer Round 3 across 3 attempts:

- **Sub-agent dispatches this session (CS-357 substrate + Round-1 + Round-2 + Round-3 + validation chain)**: 16 sub-agent calls (Migration Lobe x3, migration-lobe-codegen x3 dry-run + 2 live, Code Reviewer Lobe x2). Total wall-time roughly 25-30 min across all dispatches (excluding the substrate authoring + validation steps which were operator-driven inside this CC session).
- **Token cost per endpoint** (estimated): Migration Lobe body-read for WorkOrderType GetAll = ~75k tokens per dispatch (3 dispatches across the 3 rounds = ~225k tokens cumulative). Migration codegen = ~60-100k per dispatch. Code Reviewer = ~90-100k per dispatch with independent legacy-source verification.
- **Round-3 incremental cost** (the dependent-SP body-read): 8 additional method body reads (each ~40-60 lines, ~3-5k tokens per read); marginal cost ~25k tokens per endpoint at depth-1 recursion.
- **Implication for CS-322 sizing**: a full body-read migration emit for WorkOrderType-class compositions runs ~300-400k tokens per endpoint across Migration Lobe + codegen + Reviewer. The 660-endpoint COL-205 runway = ~200-300M tokens cumulative across the full chain, depending on the move/keep distribution + the depth of dependent-SP recursion per endpoint.

## Locked decisions (operator-confirmed; per plan §Phase A4)

1. MOVE-or-KEEP rule definition approved as drafted (deterministic structural-analysis; default to KEEP on ambiguity).
2. `bridge_strategy` kept as DERIVED field (preserves downstream test-writer + doc-writer contract).
3. Drop `fdw` + `chained-http` from `framework_specific_hints` entirely; `move` + `keep` both wired.
4. Route-mount codegen capability shipped in this redesign (narrow append-only `src/index.ts` exception per ADR-035 §1.D5).
5. Empty-recordset legacy stub → recorded-and-overridden per ADR-035 §3 (bare `[]` per modern stack convention; Jaydeep-confirmed).
6. **Round-3 fix authorized on scoping evidence** (operator extended 2-round bound after CONTAINED verdict on dependent-SP body-read).

## Follow-on tickets (filed at handoff)

- **CS-357-followup-A**: structured per-stage `tokens_used` telemetry hook in cibo-pipeline run.json (coarse run-level capture in this bridge entry is the CS-322 Day-2 input; structured per-stage hook deferred).
- **CS-357-followup-B**: batch-optimise the 2 per-row in-loop service calls in the KEEP composition emit (N+1 carry-over per KEEP idiom is intentional; batch-optimisation is a future enhancement).
- **CS-357-followup-C**: structured logger wiring (replace console.error in the codegen emit + module-wide).
- **CS-357-followup-D**: `src/shared/storedProcedures.ts` catalog hygiene sweep (the `WorkOrderType.Get` entry is stale; codegen body-read is canonical; catalog audit deferred).
- **CS-357-followup-E**: AST-grade body analysis (Tree-sitter / Roslyn) for harder cases beyond the current conservative structural-analysis rule.
- **CS-357-followup-F**: removal of `glaze.migration.architectural_assumption.shape_breakdown` (now deprecated for Lobe consumption but still in glaze for documentation).
- **CS-357-followup-G**: cross-Lobe compat-pin range regen for test-writer-lobe + doc-writer-lobe (transparent shift in `bridge_strategy` semantics).
- **CS-357-followup-H**: chore — route file doc-comment count drift ("6 dependent SPs" → "8 dependent SPs"); the Reviewer Round-3 LOW finding.
- **CS-357-followup-I**: `modify-append-export` capability for the service file case (built at Round 2; tested at Round 3 only as the idempotent-skip path — would bite on the NEXT migrated endpoint targeting the same module's service file).
- **CS-357-followup-J**: schema-file modify capability for MOVE-mode emits where the typed-schema already exists (deferred per ADR-035 §1.D5 narrow-exception scope).
- **CS-357-followup-K**: intra-run file-edit batching (multiple targets in one codegen invocation touching the same existing route file).
- **CS-357-followup-L**: conditional-flow SP dispatch capture (body-summary schema extension for `primary_sp_alternates[]`).
- **CS-357-followup-M**: recursive body-analysis to depth N for nested KEEP compositions (current depth-3 cap is sufficient for the WorkOrderType case + the sampled-flat 8 dependent methods; future deeper compositions need lifted cap).
- **CS-357-followup-N**: variant DTO shape capture (conditional row shape); low priority since defaults safely to KEEP.

## Pointers

- **ADR-035**: `wiki/decisions/adr-035-cs357-migration-lobe-read-first-body-analysis.md`
- **Migration Lobe history**: `wiki/raku-meta/migration-lobe-history.md` (v2.0.0 entry appended)
- **Plan file (operator local)**: `~/.claude/plans/ultrathink-start-cs-318-dazzling-ritchie.md` (CS-357 substrate-redesign plan; all phases A-F closed)
- **api_node PR**: https://github.com/Colobbo/api_node/pull/246 (DRAFT; reviewer @jthakkar1981; branch `cs-318-workordertype-via-cs357-substrate` → `development`)
- **Agent-system feature branch**: `vgera-xyz/colobbo-agent-system:feature/cs357-migration-read-first` (3 commits: substrate B-phase, schema-persistence fix, R2+R3 capability extension)
- **Run state (gitignored, local)**: `scripts/.pipeline-runs/CS-318/` — Round-1+2+3 stage JSONs + audit logs
- **Memory entries (CS-357 substrate principles)**: `feedback_legacy_quirks_dont_propagate.md` + `reference_api_node_get_list_empty_convention.md`

## Linear updates (queued — operator may apply via MCP at next session start)

- **CS-357 → In Review** (substrate redesign ready; awaiting tag-run on the feature branch merge to main).
- **CS-318 → In Review** (PR DRAFT open at api_node/development; Jignesh review next; flip draft→ready after his approval + Test Writer parity-test pass).
- **CS-318 ACs**: AC #1, #2, #4, #6, #7 closed by this ship. AC #3 (parity test green) deferred to Test Writer Lobe pass on PR #246. AC #5 (CI gates) runs on PR push.

## Addendum 2026-05-20T1509 — tag-recovery v2.39.0→.1→.2 + main merge + 6 CS-357 followup tickets filed (CS-360-365)

Recorded after the canonical handoff. Three discrete events landed against the same CS-357 ship line.

### 1. Tag-recovery sequence (Pattern S confirmed)

The `v2.39.0` tag pushed at canonical-handoff time failed the 18-gate ratification — not on substrate behaviour, but on **propagation obligations the substrate ship did not co-coordinate**:

- **v2.39.0 → FAIL Gate 2** (run [`26169142313`](https://github.com/vgera-xyz/colobbo-agent-system/actions/runs/26169142313)). `verify-lobe-compat.sh` invariant (γ): MigrationResult `_metadata.current_version = 2.0.0`, but Security + Code Reviewer Lobes both pinned `consumes_migration_schema` at `>=1.0.0 <2.0.0` in both glazes. MAJOR bump without coordinated downstream range bump. PATCH `v2.39.1` bumped both pins to `>=1.0.0 <3.0.0` in `glaze/colobbo.glaze.json` + `glaze/fintech.glaze.example.json`; Pattern Q resnap of 63 non-synthetic fixture glaze-snapshots; VERSION 2.39.0 → 2.39.1; registry regen. Verified the Security + Code Reviewer Lobes do not deep-parse Migration-specific fields (both run fixture-pack analysis over MigrationResult as opaque JSON) — the new required additive fields are structurally invisible to them; range bump is the correct mechanical fix; no Lobe behaviour change needed. Commit `60d97308a`.

- **v2.39.1 → FAIL Gate 14** (run [`26169832105`](https://github.com/vgera-xyz/colobbo-agent-system/actions/runs/26169832105)). `verify-compose-fixtures.sh`: `agents/global/security.md` had drifted from `lobes/security-lobe/LOBE.md` recompose. The Security LOBE.md substitutes `{{glaze.security.compatibility.consumes_migration_schema}}` at 3 token-sites in its compiled text; the v2.39.1 glaze bump needed `bash compose.sh deploy --all` before tag, which I omitted. PATCH `v2.39.2` re-composed the 12 agents (only security.md actually drifted; the other 11 were already in sync because Migration's consumes ranges on Arch/Backend/Database are unchanged at 1.x, and Code Reviewer's LOBE.md does not substitute the range scalar into its compiled text). VERSION 2.39.1 → 2.39.2; registry regen. Commit `c4c162da2`.

- **v2.39.2 → PASS all 18 gates** (run completed clean). `main` fast-forwarded from `d01cf3801` to `c4c162da2` via `git merge --ff-only feature/cs357-migration-read-first`. Graphify webhook fired automatically (`07ee8f7ac chore(graphify): refresh cibo from c4c162d`), so a `git pull --rebase origin main` was needed before pushing the active-plan.md update. Final main HEAD: `b5ab47527`.

Two failed tags retained as historical record per operator policy (no force-push, even on never-ratified tags). The two failure shapes are the canonical regression evidence for CS-360.

### 2. Substrate-gap class identified: propagation-obligation-without-forcing-function

Both Gate 2 and Gate 14 surfaced the same class. A change in artifact X creates an obligation to update sibling artifact Y, but nothing in the change-set OBVIOUSLY looks like a Y-touching trigger. Without a structural check, the obligation is missed until a tag-run catches it.

Two distinct instances of the same class:
- Schema MAJOR bump → all downstream `consumes_*_schema` ranges across the fleet must move.
- Glaze value mutation → the corresponding `agents/global/*.md` compose-output must regenerate.

Class characterisation, with examples beyond this ship:
- `feedback_version_bump_requires_registry_regen` (Gate 19 stamps VERSION into `system_version`).
- `feedback_gate_coverage_invariant` (registry-array entries need CI iterator coupling).
- `feedback_state_drift_class` (5 pre-existing 2026-05-08–09 surfaces of the same convention-not-codified gap).

The CS-357 ship is the 7th surface. Per `feedback_state_drift_class`'s canonical fix shape (ADR + README + script + CI gate + verification cycle), the right move on the 6th surface was a structural gate; we're on the 7th instead. **CS-360 is that structural gate** — a single check at v2.39.X that surfaces ALL stale consumers + ALL post-glaze-edit compose drift in one tag-run.

### 3. Linear followups filed (6 tickets, all in Colobbo Strategic)

The 14-item bridge inventory was filtered to the operator-prioritised subset (G + 5 capability gaps #5/#7/#9/#10/#14). Tickets:

- **CS-360** — Cross-Lobe contract auto-gate (MAJOR-bump consumer-range coordination + post-glaze-edit compose-drift). Medium. Related to CS-357 + CS-359 (the operator-filed architectural Fix 1 / Fix 2 ticket which explicitly anticipates this gate as the cheap interim). This is the CHANGELOG-referenced "CS-357-followup-G".
- **CS-361** — AST-grade body analysis for cases beyond the conservative structural rule. Low. Bridge followup E (#5). Related to CS-357 + COL-205 + CS-358 (the operator-filed migration drift-detection ticket which builds SP source availability — a prerequisite for the SP-body half of this work).
- **CS-362** — Test Writer + Doc Writer Lobe compat-pin range verification post bridge_strategy semantic shift. Medium. Bridge followup G (#7). Related to CS-357 + CS-360 (structurally subsumed once CS-360 ships; CS-362 is the one-time tactical check before then) + CS-359.
- **CS-363** — `modify-append-export` exercise on non-idempotent path. Medium. Bridge followup I (#9). Related to CS-357 + COL-205 (bites batch-1 if it lands a 2-targets-1-module case).
- **CS-364** — `modify-append-schema-table` capability for MOVE-mode emits into existing typed-schema. Medium. Bridge followup J (#10). Related to CS-357 + COL-205 + CS-363 (same `modify_existing_files` capability surface).
- **CS-365** — Variant DTO shape capture in `legacy_method_body_summary`. Low. Bridge followup N (#14). Related to CS-357 + COL-205 + CS-361 (AST-grade reader would detect variants more reliably).

Pre-flight verified the operator had already filed CS-358 (migration drift-detection + .NET-freeze + SP governance) and CS-359 (Lobe coupling simplification — orchestrator-mediated routing + tolerant-reader contracts) from the chat side. CS-360 + CS-362 relate to CS-359; CS-361 relates to CS-358; no duplication. The other bridge followups (A/B/C/D/F/H/K/L/M) remain bridge-prose-only by operator decision.

### 4. Plan-state delta

`wiki/strategy/active-plan.md` `last_updated` bumped to 2026-05-21. Critical path reordered: CS-357 tag-run + main merge moves to #1 DONE; CS-318 Jignesh review cycle moves to #2 (top of queue). Last-session-delta paragraph extended to capture the 2-PATCH tag-recovery sequence + substrate-gap class. Commit `9aa392555` (rebased on top of the graphify webhook refresh; pushed as `b5ab47527`).

### 5. Memory deltas (no new files this addendum)

No new memory files written. The substrate-gap class characterisation surfaced in §2 should ideally crystallise into a single feedback-class memory entry (working title `feedback_propagation_obligation_without_forcing_function`) — deferred to next session to avoid mid-handoff memory authoring.

### 6. Final state at addendum time

- `main` at `b5ab47527` (CS-357 substrate redesign + tag-recovery PATCHes + active-plan.md update + graphify refresh).
- Tags on origin: `v2.39.0` (FAIL Gate 2 historical record), `v2.39.1` (FAIL Gate 14 historical record), `v2.39.2` (PASS all 18 gates — the canonical ratification of CS-357).
- 6 followup tickets in Colobbo Strategic Backlog: CS-360 / CS-361 / CS-362 / CS-363 / CS-364 / CS-365.
- CS-318 PR DRAFT #246 unchanged at `Colobbo/api_node:development`; awaiting Jignesh review.

## See also

- `wiki/decisions/adr-033-cs330-migration-lobe-codegen-boundary.md` (preserved decisions §1.D1/D3/D4/D5/D6; §1.D2 superseded by ADR-035)
- `wiki/decisions/adr-035-cs357-migration-lobe-read-first-body-analysis.md` (decision of record for v2.0.0)
- `wiki/strategy/active-plan.md` (next sessions: CS-318 PR cycle through Jignesh; Test Writer Lobe parity-test emit; CS-322 sizing from this bridge's telemetry)
- Prior bridge: `2026-05-20T0303-CS-353.md` (plan-state persistence + chat-role boundary ship)
