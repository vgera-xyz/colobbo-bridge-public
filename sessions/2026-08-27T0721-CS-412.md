---
title: "CS-412 — headless pipeline emits for real: ship chain, PR assembly, Slack + Linear, webhook bridge; first ramp PR api_node #581 open; v2.63.0 tagged CI-green"
tags: [bridge, session, cs-412, cs-421, col-666, headless-pipeline, emit, pr-assembly, ship-chain, slack, linear, webhook-bridge, adr-040, ramp]
last_updated: 2026-08-27
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-412
status: active
---

# CS-412 — headless pipeline emits for real; first ramp PR open

**Linear:** [CS-412](https://linear.app/colobbo/issue/CS-412) (primary) · [CS-421](https://linear.app/colobbo/issue/CS-421) (Planner defect, child) · [COL-666](https://linear.app/colobbo/issue/COL-666) (ramp ticket, product board)
**Branch:** `cs-412-integration` → merged into `main` (merge, no rebase) · **Tag:** `v2.63.0` at `74981a4456`, lobe-regression CI green
**Decision record:** [[../../decisions/adr-040-cs412-ai-opened-prs-enter-review-flow|ADR-040]]

## Summary

The headless pipeline graduated from dry-run to shipping. Behind an `emit` flag that defaults to off, `pipeline-run.yml` v3 now runs the new **ship chain** (build sub-chain + Code Reviewer L1 + Test Writer + test codegen), writes the generated files into a writable product-repo checkout, opens one pull request against the Glaze integration branch with the Glaze reviewer, announces the outcome in `#colobbo-radar` as Cibo (threaded), and moves the Linear ticket (In Progress → Dev Done). The Linear-webhook → `repository_dispatch` bridge is built and tested; deployment is pending (Vercel API refused project creation).

**The finish line was reached: [api_node PR #581](https://github.com/Colobbo/api_node/pull/581)** — `GET /api/health/ping` route + two vitest/supertest tests, base `development`, reviewer `jthakkar1981`, all required checks green (`typecheck`, `gates-changed`, `pdf-lambda-artifact`, plus ai-review and Snyk), zero suppressions, zero attribution, the deferred `src/index.ts` mount stated plainly in the PR body. COL-666 sits at **Dev Done** with the PR attached. Operator reviewed and commented; Jignesh notified.

It took three emit runs. Each halt was a real defect, fixed at its source, none in the product repo:

| # | Run | Where it stopped | Defect | Fix |
|---|---|---|---|---|
| dry-1 | 32977530335 | Planner, validator exit 4 | `file_mapping.source: "inference"` (enum wants `keyword-inference`), `days_old: null` — LOBE.md never named the enum | Planner prose states the enum + the integer rule ([CS-421](https://linear.app/colobbo/issue/CS-421), `4aebab87fe`) |
| emit-1 | 33036214838 | Code Reviewer `blocked` | Architect/Backend stage Results to `memory/pending-review/` (glaze `human_review.enabled`), Reviewer preflight refuses on pending items — deterministic headless, nobody to approve | `REVIEW_HUMAN_REVIEW` / `TEST_HUMAN_REVIEW=off` added to those LOBE.md (mirrors Architect/Backend); runner sets all four (`919e7fabc7`) |
| emit-1 | same | Slack post + artifact upload | `sed \| head -c` broken pipe under `pipefail`; `:` in codegen audit filenames rejected by upload-artifact — lost `run.json` | head-then-sed; rename `:` before upload; log the full halt record (`f516da6e8a`) |
| emit-2 | 33037348620 | PR assembly refused 5 real files | audit logs record runner-workspace-relative paths (`repos/api_node/…`); assembler accepted only absolute/repo-relative | strip the repo-dir prefix; emit run without a PR now posts as failure (`ed948e8c0b`) |
| emit-3 | 33044126975 | — | 7/7 stages ready, PR #581 opened, $8.20, 16.5 min | — |

## What was built (commits, oldest first)

- `76ad343816` `chains/ship.chain.json` + Emit rule; glaze 1.21.0 (`slack.pipeline_channel`; api_node test path → `apps/api-node/src/__tests__/generated/`); `cibo-pipeline` v0.8.0
- `f11d18c215` `scripts/assemble-pr.sh` (refuses suppressions / base=main / attribution / missing files before any git), `scripts/notify-slack.sh`, `scripts/linear-transition.sh` (states resolved by name on the issue's team) — `test/test-*.sh` against fake `gh`/`curl`
- `e5d228be8e` `pipeline-run.yml` v3 — `emit` flag, writable mount, secret preflight (absent evidence fails), PR / Dev Done / Slack steps
- `7bc9dd1e5b` `bridge/linear-webhook/` Vercel function + 18 unit tests; labels `pipeline` / `pipeline:emit` created on the COL team
- `d99b045cc7` ADR-040; headless page v3; feature-pipeline §4.7 "PR assembly now exists"
- `90a13fa555` VERSION 2.63.0 + registry regen
- `4aebab87fe`, `f516da6e8a`, `919e7fabc7`, `ed948e8c0b` — the four ramp fixes above
- `74981a4456` merge of `origin/main` (canonical_pages conflict: kept both ADR-040 and the COL-668 entries) → pushed as `main`, tagged `v2.63.0`

## Decisions taken (operator, 2026-08-26/27)

PR author = operator PAT for the ramp (bot account before volume) · ramp target `GET /api/health/ping` on a real COL ticket · Reviewer L1-only until volume · tracker states In Progress → Dev Done (no In Review on the product board; Planned stands in for Todo as the bridge trigger) · bridge on Vercel with a single-repo dispatch-only PAT · bridge smoke = dry run only · one emit run, then re-fires approved case by case with the class of each fix explained first (mechanical/prose ⇒ fire; grounding ⇒ pause).

## Open riders

1. **Vercel bridge deploy** — `deploy_to_vercel` returned 403 "no permission to create a project"; needs the project created in the dashboard or `cd bridge/linear-webhook && vercel deploy --prod`, then `LINEAR_WEBHOOK_SECRET` + `GITHUB_DISPATCH_TOKEN` env and the Linear webhook registration. Hardening follow-up: Railway / Cloudflare Worker + GitHub App token.
2. **`assemble-pr.sh` commit scope cosmetic** — PR #581's subject came out `feat(__tests__)` because the first sorted path was a test; prefer non-test paths when deriving the scope.
3. **CS-421 closes with the Planner fix** (`4aebab87fe`); the Gate-3 negative fixture for the enum-drift class is still worth adding.
4. **Reviewer L1 check #2 wording** — LOBE.md compares `BackendResult.endpoint_signature.<route_id>` keysets, but the Backend schema's `endpoint_signature` is a flat `{method, path, auth_required}` object; the deterministic `coherence-check` is the authority so no false positive fired, but the prose is wrong. Note on CS-412.
5. **Dedicated bot GitHub account before volume** (v2 answer on CS-412).
6. Per-feature test-file namespacing (`{feature_id}` in `test_path_conventions`) — known gap in ADR-040.

## Verification

Local: 4 fast verify scripts + frontmatter + pipeline-fixtures + chain-config fixture + Gate 3/20/Test-Writer fixtures + Gate 5 portability (planner, code-reviewer, test-writer) all green on the merged commit. Tag run green. `bash deploy.sh` at the boundary: 137 deployed. Three secrets provisioned on the agent-system repo by the operator (`PRODUCT_REPO_WRITE_TOKEN`, `LINEAR_API_KEY`, `SLACK_BOT_TOKEN` — org secrets do not reach a personal-account repo).

## See also

- [[../../decisions/adr-040-cs412-ai-opened-prs-enter-review-flow|ADR-040]] · [[../../architecture/headless-cc-orchestration|headless-cc-orchestration]] · [[../../architecture/feature-pipeline|feature-pipeline]]
- Prior: [[2026-08-16T1401-CS-322|2026-08-16 CS-322 v2]] (the dry-run boundary this session crossed)
