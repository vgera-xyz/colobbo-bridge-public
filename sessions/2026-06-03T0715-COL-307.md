---
title: "Connected-records rollup — byte-verified link graph PERSISTED + build plan produced (no feature code): WO/WP count-by-type grid extending COL-307; template dropped (creation-only), R2 via-inspection kept (defensive), tenant scoping via anchors, B1 confirmed (covering indexes), kernel-landing resolved (in PR #250)"
tags: [bridge, session, investigation, planning, no-ship, no-feature-code, col-307, col-341, connected-records, link-graph, tbllinkmodule, rollup, archaeology, rplan, byte-verified, tenant-scoping, b1-confirmed, work-packages, work-orders]
last_updated: 2026-06-03
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-307
status: active
---

# Connected-records rollup — byte-verified graph persisted + build plan (no feature code)

## Summary

Continuation of the COL-341 vs COL-307 rollup work (earlier bridge [[2026-06-03T0451-COL-341|2026-06-03T0451-COL-341]] rejected COL-341's materialise approach for read-side aggregation). This session deepened it into a **byte-verified connected-records link graph**, persisted it to `wiki/archaeology/connected-records-link-graph.md` (commit `fa962b3092`, gates 12/13 + glaze invariant-k green), and produced a **6-task build plan via `/rplan`**. **No feature code, no Linear edits, no ship** — the kernel/endpoint code is the next session, entry point = Task 1.

The session twice corrected the template model (chat-tier had it as a rollup link-hop; bytes show it's creation/metadata only) before converging on the operator's consolidated model, which was then **byte-verified route-by-route** (confirm/correct, not rubber-stamp).

## What was done

1. **Archaeology + byte-verification (both repos).** Dispatched Explore agents for breadth, then re-read every load-bearing claim from the bytes. Corrected my own over-rotation onto the template FKs; the operator's final model drops template from the rollup entirely.
2. **Persisted the verified map** `wiki/archaeology/connected-records-link-graph.md` (`fa962b3092`) — registered in `glaze.wiki.canonical_pages`, all wiki gates pass. New `wiki/archaeology/` section.
3. **`/rplan` build plan** produced (6 tasks, WP-first sequencing).
4. **Two forks resolved:** kernel-landing (→ export lands IN PR #250's branch, not a new branch) and index posture (→ two covering bidirectional indexes confirmed live → B1 sufficient, B2 dropped).

## Verified route set (the map is canonical)

- **WO anchor:** R1 direct (WO↔Action, WO↔Inspection — `tblLinkModule` bidirectional) + R2 via-inspection (WO→Inspection→Action, 2 hops — KEPT per nuance N2: the `ensureWorkOrderLinkages` mirror is WO-context-only/best-effort/no-DB-trigger, so R2 is defensive not redundant) + R3 child-WO recursion (`parentWorkOrderId` CTE); Claims via FK `tblClaim.workOrderId`.
- **WP anchor:** rollup-from-member-WOs (`tblWorkOrder.workPackageId` FK grouping → each member WO's subtree); WP direct-term EMPTY (no-op, not foreclosed — WP is already a live link-module value).
- **Template DROPPED** — creation/metadata only (`tblInspection.templateId` FK, WO↔Template creation affordance, Action↔Template template-scoped). A template-linked action belongs to the template, not a WO instance (`create-action.createLink:510-516`).
- **Tenant scoping (IDOR guarantee):** `tblLinkModule` has no `companyId`; scope via anchor entities (WO/WP filtered to caller's company); kernel never queries links in isolation. Already matches COL-307 (`workOrderSummaryService.ts:180,186,299,323` + controller `assertCompanyAccess:429-430`).
- **Perf:** `IX_tblLinkModule_Source` + `IX_tblLinkModule_Target` (covering, both directions) → **B1 confirmed sufficient; B2 not required.**

## Build plan (6 tasks — next session entry = Task 1)

1. **Export/extract COL-307 kernel** (`resolveDescendantWorkOrderIds`/`resolveLinkedEntityIds`/`aggregateOverWoSet`, currently non-exported `workOrderSummaryService.ts:170/209/262`) — **lands IN PR #250's branch (open, mergeable), NOT a new branch** (fork resolved).
2. **Generalise to `aggregateOverAnchorSet`** (own-direct ∪ rollup-from-children, deduped) + add R2 to the Action resolver.
3. **WP anchor + `GET /api/work-packages/summary-grid`** (paginated, server-side filter/sort on counts, user-selectable module types, one in-DB GROUP BY reusing `tracker` `VALID_SORT_COLUMNS`/`buildWhereClause`).
4. **Tenant invariant + WP verify** (tests: per-package accuracy, re-parent, R2-only, dedup, cross-company-leak; **EXPLAIN against a prod-like dataset** — index-hit confirmation).
5. **WO `summary-grid`** (replicate proven kernel/pattern; distinct from COL-307's single-record `/work-orders/summary`).
6. **PR → development / reviewer jthakkar1981** (pre-submit `tsc --noEmit` + `jest`); recommend relating COL-341 ↔ COL-307 in Linear (don't auto-link).

~5.75 dev-days, essentially sequential (kernel gates the grids). Claims = total-count only until sub-state modeled (follow-on).

## What's next

- **Next session:** start at **Task 1** (kernel export in PR #250's branch).
- **Operator parallel-track (long-pole, non-code — start now):** secure a **prod-like dataset or prod-DB read access** for Task 4's EXPLAIN — the synthetic local dev stack cannot prove index hits at scale.
- **Off critical path:** PR #250 review/merge (jthakkar1981) — confirms the in-#250 kernel landing.

## Verification notes

- All route mechanisms re-read from bytes this session (R1 `create-action:501-516`; R2 `SP_ActionReportGet.sql:64-78` + mirror `create-inspections:387-426`; R3 `workOrderSummaryService.ts:170-196`; Claims `schema.ts:194-196`; WP FK `tracker.work-order.service.ts:60`; tenant scoping `workOrderSummaryService.ts:180-323,429-430`; indexes operator-confirmed live).
- Could not verify from disk: real-volume traversal timing (needs prod-like data — folded into Task 4 as an operator dependency).
- Commits this session: `fa962b3092` (archaeology map) + this handoff. Earlier in-session handoff: `aa6cfccbbe` (0451 bridge). No source/feature commits to product repos. No Linear edits.

## See also

- [[../../archaeology/connected-records-link-graph|connected-records link graph (the byte-verified map — canonical)]]
- [[../../strategy/active-plan|active-plan]] — build plan folded in this handoff (next session → Task 1)
- [[2026-06-03T0451-COL-341|prior bridge — COL-341 architecture review (materialise rejected)]]
- [[../../modules/work-orders|work-orders module]]
- [COL-307](https://linear.app/colobbo/issue/COL-307) — WO summary/context enrichment (backend PR #250; the kernel this build extends)
- [COL-341](https://linear.app/colobbo/issue/COL-341) — LinkModule materialise (superseded by the read-side model; relate-recommendation pending)
