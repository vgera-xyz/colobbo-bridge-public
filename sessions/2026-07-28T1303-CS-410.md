---
title: "CS-410 release-process PRs all merged + dev-server ground-truth mapped + onboarding artifact & per-repo RELEASING.md + post-merge suite fix"
tags: [bridge, session, cs-410, release-process, sentry, dev-server, pm2, onboarding, releasing-md, ci-fix, deployment-doc]
last_updated: 2026-07-28
source: memory
confidence: high
linked_issue: CS-410
lobe_version: 1.0.0
status: active
---

## Summary

Long multi-day continuation closing out the CS-410 release-process rollout end-to-end. Spans the PR-merge phase, a live dev-server investigation, a real bug-hunt, and onboarding docs.

- **All 8 CS-410 PRs merged** (operator + Jaydeep, 2026-07-28): mobile #380/#381/#382/#384, api_node #381/#382/#383, web-angular #633. Reviews were reassigned to **Jaydeep** (operator: strongest on Sentry + he integrated notification-service). **api_node CD #382 confirmed safe-to-merge dormant** (dispatch-only, preflight fails without AWS vars). **#633 needed conflict resolution + a Node 20→22 bump** mid-session (dev-stage had advanced; kept the ceremony, took @v7 action bumps).
- **Sentry credential model settled + provisioned:** the leaked mobile `.sentryclirc` **auth token** (org master key, NOT a DSN) removed via #380 — **rotation still pending (operator)**. 9 per-surface **org VARIABLES** created + readback-verified (`SENTRY_ORG` + `SENTRY_PROJECT_{API,NOTIFICATION,WEB,MOBILE}_{DEV,PROD}`); only `SENTRY_AUTH_TOKEN` is a secret. Fleet workflows repointed `secrets.*`→`vars.*` for identifiers (api_node #383/#382/#633 + the new dormant web/mobile ceremonies).
- **Dev API server ground-truth mapped** (Jaydeep on the box) — was undocumented all along. **Windows EC2 + pm2 fork-mode** (NOT the ECS the wiki inferred). Both apps run from one monorepo checkout `C:\Colobbo\back-end-node\node-backend\api_node`: `node-backend` (id 1) ← `apps\api-node` `dist\index.js`; `notification-service` (id 0) ← `apps\notification-service` `dist\src\app.js`. Node 22.17, npm 11.11. Deploy = pull → npm ci → build → pm2 restart. Messy layout (old `node-backup` + deep nesting) → **cleanup folded into CD enablement** (target `C:\Colobbo\api_node`), NOT a manual folder-move (would repoint pm2 → outage). `DEV_APP_PATH` var banked. **All CD workflow (#382) assumptions validated against reality.**
- **Appflow retraction (operator TIER-1):** Appflow NOT in use; `appflow.config.json` + stale Capgo docs were dead code (removed via mobile #382). Corrected the earlier "mobile sourcemaps are a gap" claim — `build:prod` already uploads them; the real coupling is post-rotation the builder must `export SENTRY_AUTH_TOKEN`.
- **Post-merge full-suite fix — web-angular #677 (open, green, awaiting Jaydeep):** the "per-merge" report was red for days (emailing failures) even though all tests pass — 1 unhandled `NG04002` in `user-management.component.spec.ts` (component navigates on init, spec had no router). Fixed with a no-op Router mock. Bundled a **second fix**: `lint-changed` crashed (exit 2) on the `users and teams/` spaced path because `npx eslint $FILES` was unquoted — fixed with a bash array (latent bug for ANY spaced-path file). Both verified: full suite `115 files, 0 errors`; lint green.
- **Onboarding deliverables:** a **published artifact** (new-engineer release/deploy guide — pipeline, hotfix-vs-bugfix, per-repo, Sentry's 3 jobs, first-release sequence, CI-enablement) at claude.ai/code/artifact/7495bf1c-4f17-4513-ad1f-1f888ef3bb50. **Per-repo `RELEASING.md`**: mobile ✅ merged (#384), **api_node #419** + **web-angular #678** open. Reviewer pool documented: api_node = Jaydeep/Siddhesh/Jignesh, web/mobile = Jaydeep.

## Agent-system commits (this slice, all on main, pushed)

- `ce96dddf9a`... (prior) runbook; **v2.59.0 tag**; F1c33ef05d Appflow addendum; `a3f4c85f38` Jaydeep answers; `d17f96fc09` fleet var table; deployment.md ground-truth (`49e8c716e2` merge). All Gates 12/13 green throughout.

## Open PRs (this session) — awaiting reviewer approval

- **web-angular #677** — post-merge NG04002 + lint-gate fix (green; Jaydeep) — **priority: stops red emails**
- **web-angular #678** — RELEASING.md (Jaydeep)
- **api_node #419** — RELEASING.md (Jaydeep/Siddhesh/Jignesh)
- Nothing for the operator to self-merge; all product-repo merges are reviewer-driven.

## Operator TODOs (carried)

1. **Rotate the leaked mobile Sentry auth token** (still in git history; new token → GitHub secret + Jaydeep's `~/.zshrc` → revoke old).
2. **Jignesh's 1-line fix on main** — the duplicate `rxjs` import (`action-list.component.ts`) still un-done last check; web hotfix→main typecheck stays red until then.
3. **First release per repo** (dev→main, back-merge first): api_node **v1.0.0**, mobile **v8.3.0**, web pick a start — this cleans main + activates Sentry automation.
4. **web CD-enablement blocker:** where does `environment.prod.ts` come from in CI (Jaydeep's open question).
5. Migration critical path (COL-205/COL-306) — untouched by all this; still the delivery focus.

## Verification notes

- 9 Sentry vars: `gh api /orgs/Colobbo/actions/variables` readback = 9 (values = live Sentry slugs).
- Tag rulesets on api_node + web-angular: malformed-tag rejection live-fire-proven.
- #677: full suite run with CI's exact runtime env stub, before=1 error / after=0; lint array-splitting tested; actionlint clean.
- Dev-server facts: authoritative from `pm2 describe` (not folder guessing — `node-backup` was a red-herring backup).

## See also

- [[2026-07-20T1247-CS-410|prior slice — deployer build-path answers]]
- [[../../architecture/release-process|release-process runbook]]
- [[../../architecture/deployment|deployment doc — confirmed dev-server + CD checklist]]
