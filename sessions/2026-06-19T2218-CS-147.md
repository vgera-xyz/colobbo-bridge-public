---
title: "CS-147 — deferred deploy.sh operator-run executed + verified; ~/.claude settings now match repo main"
tags: [bridge, session, cs-147, deploy, settings, operational]
last_updated: 2026-06-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-147
status: active
---

# CS-147 — deferred deploy.sh run executed + verified

## Summary

Single, narrow session: ran the **deploy.sh operator-run that the prior CS-147 handoff deferred to a session boundary** (rewriting the running session's own permission/deny rules mid-session is unsafe; settings load at session start). `bash deploy.sh` completed clean — **129 deployed, 91 backed up, 0 skipped** — propagating `settings/global.json` → `~/.claude/settings.json` plus the full agents/skills/rules/hooks/patterns/monitors set and all 4 product-repo configs. The only live-state delta was the settings file (the rest was byte-identical re-push, as the prior session's read-only preflight predicted). **No repo working-tree changes** — deploy.sh writes user-global (`~/.claude/`) + product repos, never this repo's tree; `git status` stayed clean and there were no commits from the deploy itself.

## What was done

- `bash deploy.sh` → 129 deployed / 91 backed up / 0 skipped. 38 agents · 33 skills · 6 shared rules.
- **Verified the load-bearing settings deltas** landed in `~/.claude/settings.json`:
  - `skipDangerousModePermissionPrompt: true` — the live-only drift captured into `settings/global.json` by `4e9e35a4f9` last session, now installed (clean superset, not dropped).
  - Linear MCP write allows present: `save_comment`, `save_issue`, `save_document`.
  - Hook self-protection deny retargeted to `.claude/hooks/**` (from `./hooks/**`).
- Spot-checked a product repo (api_node `CLAUDE.md` freshly written) per `deploy-safety.md` post-deploy verification.

## Verification notes

- Deploy printed an informational warning: **180 untagged commits since `v2.46.3`**. Non-blocking (deploy succeeded); surfaced to operator. This repo's 20 CI gates are tag-triggered (see [[../../../.claude/rules/pre-tag-verify-checklist|pre-tag-verify-checklist]]), so those commits are not yet ratified — `/version` would tag + run the gates if a checkpoint is wanted. No action taken this session.
- New settings take full effect at the **next** session start (this session is still running under the old permission set).

## What's next

- **CS-147 stays In Review** — NOT a Done candidate. Still blocked on the 2 remaining unattended-flip prereqs: **(2) launchd scheduling** + **(3) Sentry REST token in run env**. Prereq (1) non-iCloud run path was satisfied 2026-06-19 (the `~/dev` move).
- Review/merge **api_node PR #282** (Sentry NODE-API-PROD-J S3 fix; `UNVERIFIED — human S3 test required`, @jthakkar1981).
- Optional: `/version` to tag a ratified checkpoint (180 commits since v2.46.3).
- Build the post-raise CI poll at the unattended-flip milestone (deferred per prior bridge).

## See also

- [[2026-06-19T0842-CS-147|prior CS-147 bridge — pre-filter fix shipped + PR #282; deploy preflight (this run's predecessor)]]
- [[2026-06-18T0707-CS-147|CS-147 bridge — pre-PR CI-conformance gate added]]
- [[../../../.claude/rules/deploy-safety|deploy-safety rules]]
