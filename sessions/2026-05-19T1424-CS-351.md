---
title: "CS-351 — Orchestration architecture trajectory (wiki ref + ADR-012 guardrail + strategy pointer)"
tags: [bridge, session, cs-351, orchestration-architecture, adr-012, adr-032, capability-profile, claudemd-guardrail, raku-roadmap, icloud-gremlin, rebase-transient, v2-36-0]
last_updated: 2026-05-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-351
status: active
---

# CS-351 — Orchestration architecture trajectory

## Summary

Shipped **CS-351** as `v2.36.0` (commit `4271d4f09`, tag `v2.36.0`, 18-gate
Lobe Regression **SUCCESS** — no Gate-19 detour: the CS-345 VERSION→registry
lesson was pre-empted). Landed the pre-drafted 4-layer orchestration
trajectory as a canonical wiki reference + a CLAUDE.md guardrail + a
raku-roadmap strategy pointer so MEMORY.md regenerates with the link.
Content was pre-drafted in the CS-351 ticket; landed with **three
verified corrections** (not re-authoring). Two memory-guided detours hit
and resolved deterministically.

## What shipped

- **`wiki/architecture/orchestration-architecture.md`** (new) — 4-layer
  hybrid (capability + workflow + state-machine + Glaze-aware), cutting-edge
  anchors, Colobbo→Raku migration path (Phases 1–4), guardrails, decision
  boundaries. Frontmatter `last_updated: 2026-05-20` shipped verbatim per
  locked decision.
- **`CLAUDE.md`** — Hard **rule 11** (new-Lobes-ship-ADR-012-capability-
  profile guardrail), reformatted to the `N. **Bold lead-in.**` convention
  of rules 1–10. 60 lines (cap 200).
- **`wiki/strategy/raku-roadmap.md`** — `## Orchestration architecture
  trajectory` appended → MEMORY.md regenerated carrying the pointer
  (260 lines; > 200 soft cap is the pre-existing CS-321 condition, < 300).
- **`glaze/colobbo.glaze.json`** — canonical_pages entry (160); invariant
  (k) PASS.
- **`VERSION`** 2.35.0 → 2.36.0; **`lobes/_registry.json`** regen (sole
  `system_version` diff — verified, CS-345 lesson applied proactively).

## Corrections applied (decisions locked with operator)

1. **Two broken wikilink slugs** (Gate-13-blocking) — draft used
   `adr-012-lobe-capability-primitive` → real `adr-012-lobe-operating-modes`;
   `adr-032-lobe-substrate-registry` → real `adr-032-lobe-substrate-layering`.
   The brief flagged only ADR-032; verify-by-grep caught ADR-012 too.
2. **ADR-012 factual inversion** (Decision 1) — draft stated ADR-012 is an
   "8-atom enum: build/audit/migrate/design/test/document/review/plan".
   ADR-012 §3's actual closed enum is `{read_code, write_new_files,
   modify_existing_files, produce_findings, propose_diffs, produce_plan,
   …}`; its title literally "supersedes the operating-mode framing" and the
   8 verbs are the *operations* it rejects as the primitive. Corrected in
   the Layer-1 section + the CLAUDE.md rule to state capability *profile* =
   atoms from the closed vocabulary; verbs decompose into atom bundles.
   Architecture/phases/guardrails unchanged. (`feedback_artifact_rules_over_ticket_prose`.)
3. **"WHA-tier" genericized** (Decision 2) — Layer-4 example → "regulated/
   compliance tier" (WHA is a Colobbo client; the doc's own Guardrail #6
   mandates the fintech-swap test). `Colobbo → Raku` header kept (blessed
   stepping-stone framing).

## Verification

- Gate 12 frontmatter (177 pages) · Gate 13 wikilinks (1364 links) · Raku-meta
  grep on the new doc clean (`jignesh|jaydeep|api_node|dev.colobbo.com|wha-tier`
  → empty) · `verify-glaze-shape.sh` ALL STRUCTURAL INVARIANTS PASS.
- **Lobe Regression Suite (18-gate) SUCCESS** at `v2.36.0` → `4271d4f09`
  (run 26103324369). The tag is the ratification.

## Detours — both resolved via existing memories (deterministic)

1. **Invariant (k) FAIL — not this ship.** `wiki/bridge/sessions/
   2026-05-19T1342-CS-345 2.md`, a macOS/iCloud `" 2"` byte-identical
   untracked duplicate of last session's CS-345 bridge entry, was
   canonical-eligible-but-unregistered. Inspected (untracked + byte-identical
   confirmed), removed only that blocker; canonical entry untouched; (k) →
   PASS. `feedback_macos_space2_duplicate_corrupts_git_refs`.
2. **`git pull --rebase` stalled mid-rebase.** The documented
   `feedback_edit_tool_vs_python3_for_rebase_safety` transient (Edit-tool
   files appear as local changes during rebase checkout — not a real
   revert). Inspected → confirmed commit `c3ab18e9a` intact → `git rebase
   --abort` + clean retry → rebased to `4271d4f09`. Exactly as the memory
   predicted; abort+retry is the deterministic fix.

## What's next

- **CS-351 → Done** (this handoff Step 3a).
- **HIGH systemic follow-up (NEW — needs a ticket):** **204 ` 2.md`
  iCloud-gremlin duplicate files repo-wide** (`lobes/`, `docs/`, `graphs/`,
  `screenshot-service/`, `wiki/`). One just broke CI invariant (k) this
  session; others are latent. Recommend a dedicated ticket: add a
  `.gitignore` pattern for `* 2.md` / `* 2.*` + a one-time audited cleanup
  sweep (inspect tracked-vs-untracked before deleting). This is the
  `feedback_macos_space2_duplicate_corrupts_git_refs` class at scale.
- **MEMORY.md 260 > 200 soft cap** — pre-existing CS-321 condition; CS-351
  added ~6 lines; still < 300 (non-blocking). CS-321 owns compaction.
- **CS-330 (Migration codegen)** is now unblocked by the guardrail/reference
  (the explicit "why now" of CS-351 — discipline before CS-330 launches).

## Pointers

- Plan: `~/.claude/plans/ultrathink-continuing-from-2026-05-19-generic-tide.md`
- New ref: [[../../architecture/orchestration-architecture]]
- Anchors: [[../../decisions/adr-012-lobe-operating-modes]],
  [[../../decisions/adr-028-cs146-pipeline-orchestration-protocol]],
  [[../../decisions/adr-032-lobe-substrate-layering]]
- Prior session: [[2026-05-19T1342-CS-345]]
