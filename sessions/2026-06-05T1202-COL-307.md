---
title: "COL-307 — #250 + #270 both MERGED to development (verified, clean un-stack), branches cleaned, WO-grid endpoint data-path verified on dev DB"
tags: [bridge, session, col-307, t5, work-order-grid, pr-250, pr-270, merge-verification, stacked-pr, branch-cleanup, dev-db-verification, endpoint-verification, connected-records, api-node]
last_updated: 2026-06-05
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: COL-307
status: active
---

# COL-307 — both backend PRs merged to development (verified), branches cleaned, WO-grid endpoint verified on dev data

## Summary

Short follow-up segment closing the loop the 2339 handoff opened ("Jignesh review/merge #250 first, then #270"). Confirmed **both #250 + #270 merged onto `development`** (Jignesh merged them), verified the merge landed cleanly with the stack un-stacked correctly, cleaned up the now-dead local branches, and **verified the WO-grid endpoint's data path against the live dev MSSQL** (counts cross-checked correct). No new colobbo-agent-system commits beyond this handoff; no new api_node commits (the merges were Jignesh's). A transient GitHub-HTTPS outage mid-segment required a few retries before the merge state was readable.

## What was done

### Merge verification (both PRs landed on development)
- **PR #250** (WP grid + review fixes): `state: MERGED`, `base: development`, merge commit `5be85b3`.
- **PR #270** (T5 WO grid, stacked): `state: MERGED`, `base: development` — **GitHub auto-retargeted the base from the #250 branch to `development` when #250 merged**, so it merged cleanly onto development (merge commit `d821072`) — NOT left in a weird stacked state.
- `origin/development` advanced `1648197 → d821072`.
- **Content verified on `development` @ d821072** (`kernel.ts` = 738 lines): WP grid (`workPackageGridService` + `aggregateGroupedByWorkPackage`) + WO grid (`workOrderGridService` + `aggregateGroupedByWorkOrder` + route mounted in `index.ts`) both present; #250 fixes (`tblClaim` removed from MSSQL schema = 0 hits; `ACTION_STATUS` `DB-CONFIRMED`); #270 fix (`partitionAlias` literal union).

### Branch cleanup
- Both feature branches **auto-deleted on origin** at merge (GitHub delete-on-merge). Confirmed via `git ls-remote` (neither exists).
- Local cleanup: switched to `development` + ff'd to `d821072`; **safe-deleted** (`-d`) `col-307-wo-summary-grid` (was `f123dc2`) + `col-307-work-order-context-enrichment-backend` (was `59fc57c`) — both confirmed merged; `git remote prune origin` cleared 5 unrelated dead refs. api_node clone now on a clean `development @ d821072`, no `col-307` branches local or remote-tracking.

### WO-grid endpoint data-path verification (against live dev MSSQL)
Ran `aggregateGroupedByWorkOrder` (exactly as merged) over 30 real company-1 candidate WOs:
- **Executes cleanly**, 30 ms; returns per-WO rows with `woCode` populated, `actionCount` + byState (A/I/C → toDo/inProgress/complete), `inspectionCount`; count-DESC sort + OFFSET/FETCH work.
- **Counts cross-checked CORRECT** against independent queries: inspection counts WO 28095=6, 28227=16, 28243=9 (exact match); the R1∪R2 action dedup for WO 28095 = 4 (exact match); byState splits reconcile with `actionCount`.
- **Caveat — NOT a live HTTP round-trip.** The dev Docker stack is down (no containers/ports) + has the two known pre-E2E gaps (api-node↔local-PG `DATABASE_SSL`, Cognito localhost callback). The auth/controller/routing layer is covered by the 8 passing e2e tests; the candidate seam `runWorkOrderView → SP_WorkOrderView_Run` (pre-existing sibling of the proven `SP_WorkPackageView_Run`) was substituted with a direct WO-id candidate set. Offered to bring up the stack for the literal HTTP call (heavy + gapped) or exercise the SP seam — operator's call pending.

## Commits

- **colobbo-agent-system:** this bridge + active-plan + daily-log + canonical_pages (this handoff). No other CC commits this segment.
- **api_node (development):** `5be85b3` (merge #250), `d821072` (merge #270) — Jignesh-merged; the underlying COL-307 commits (`59fc57c`/`6345ae7`/`f123dc2`/`9580edb`/`76f7630`) are recorded in [[2026-06-04T2339-COL-307|the 2339 bridge]].

## What's next

- **COL-307 backend fully merged to `development` + data-path-verified.** Remaining for full closure: web-angular frontend **PR #522** merge; release/deploy (then COL-307 → Released); optionally the full-HTTP endpoint test once the dev stack's PG-SSL + Cognito gaps clear.
- **PARKED:** claim junction-alignment (primary-FK → `tblclaim_workorder`); zero live impact until multi-WO claims exist.
- **Unfiled follow-ups (carried):** Drizzle-schema index-declaration drift (T4); `workPackageId` denormalisation-vs-hierarchy integrity (operator-confirmed prod-sound); api_node holistic `no-explicit-any` eslint debt; 2 staged ticket DRAFTS (junction-alignment + orphan cleanup) — operator posts.

## Verification notes

- Merge state read via `gh pr view` (after a transient GitHub-HTTPS outage cleared); content via `git show origin/development:<path>`; counts via read-only `sqlcmd` cross-checks (COUNT/aggregate only, no raw customer rows).

## See also

- [[2026-06-04T2339-COL-307|prior bridge — T5 built + stacked PR #270 (this segment's predecessor)]]
- [[../../strategy/active-plan|active-plan]] — connected-records track updated this handoff
- [api_node PR #250](https://github.com/Colobbo/api_node/pull/250) (MERGED) · [api_node PR #270](https://github.com/Colobbo/api_node/pull/270) (MERGED)
- [COL-307](https://linear.app/colobbo/issue/COL-307)
