---
title: "CS-406 — security-arch pin retired (evidence-grounded drop); all 20 registered Gate-2 pins now δ-form; tag v2.53.0"
tags: [bridge, session, cs-406, cs-359, security-lobe, tolerant-reader, pin-retirement, gate-2, registry-edit, v2.53.0]
last_updated: 2026-07-09
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-406
status: active
---

# CS-406 — security-arch pin retired; the fleet's contract layer is uniformly tolerant-reader

## Summary

Shipped the scheduled small substrate ship: the Security Lobe's arch pin — the M8 batch's one deferral — is **retired** (Option 2: DROP). The ground-vs-drop fork was decided by this session's /rthink on filesystem evidence, per the operator's explicit pre-delegation. All evidence re-verified live before the edit: the Ware's fixture packs reference no ArchResult domain field (only the `applies_to_mapping_only` no-op marker, 15×), the LOBE reads only the ADR-007 envelope from ArchResult, and δ forbids an empty declaration. Grounding a `security_checklist` read was rejected as **inventing methodology behavior to justify a gate entry** — inverted causality vs the M8 evidence-grounding convention. Result: **Gate 2 now holds 20 registered entries, ALL δ-form**, + the 2 ADR-037 sentinel EXCLUSIONS — the CS-359 irony class ("a pin guarding a coupling that doesn't exist") is resolved fleet-wide. Tag **v2.53.0** CI-green on all 21 gates (run 28992270842); it also ratifies the post-v2.52.0 docs-only commits. **CS-406 → Done** (In Progress at session start, TIER-1-verified transition at close).

## What was done

1. **Evidence pass** (re-verified live, not trusted from the ticket): fixture-pack aggregation → arch's only JSONPath is the no-op marker; LOBE.md grep → envelope-only reads; Gate-2 mechanics → completeness forcing-function satisfied by deleting the glaze pin + REGISTRY entry together.
2. **Decision: DROP** — reasoning recorded in the commit body + CHANGELOG. Reversal recipe documented in three places (REGISTRY tombstone comment, LOBE.md §Inputs, LOBE.md §Compatibility): evidence-grounded `consumed_fields.arch` in both glazes + a new `security-arch` REGISTRY entry, if a future fixture pack targets ArchResult domain fields.
3. **Execution** (`ffd2fc2ae0`): `consumes_arch_schema` deleted from both glazes; `security-arch` REGISTRY line removed (annotated tombstone); security-lobe 0.2.0 → 0.3.0 with a **three-regime Step-1 gate** (migration semver sentinel / arch envelope-only no-gate / backend+database tolerant-reader); `incompatible_arch_schema` joins the dead enums; fixture case description updated; ArchResult REMAINS a required input.
4. **Release** (`723182e18c` + tag v2.53.0): full pre-tag checklist green (5 scripts + 11 harnesses + resnap idempotency); registry-regen stamped 2.53.0; CI 21/21 GREEN.

## Verification

Straggler sweep for `security.compatibility.consumes_arch_schema` across scripts/lobes/glaze/agents: CLEAN (only the annotated tombstone). Gate 2 output: "Registry entries: 20 (excluded sentinels: 2)", security-backend/database δ-form. All 11 Lobe harnesses green. Tag run 28992270842: 21/21.

## What's next

- **Migration critical path (COL-306 batch-1)** is the delivery focus — unchanged.
- **Phase-2 kickoff = Performance Lobe** via `/cibo-new-lobe` (first real run) — the architecture track's next ship.
- CS-359 Fix-2 (orchestrator-mediated routing): refile as its own ticket when Phase-2 scheduling reaches it.
- Operator: run `bash deploy.sh` at the next session boundary to propagate the recomposed security agent.
