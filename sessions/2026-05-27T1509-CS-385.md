---
title: "M3 SHIPPED — verify-glaze-shape.sh invariant (ac) multi-signal glaze↔repo drift gate + 3 bundled glaze primary flips (api_node jest, web-angular jasmine, Colobbo-Mobile jasmine); session also shipped ADR-038 (CS-380) + M2 (CS-383) — triple-ship covering CS-373 §7 Phase-1 sequence M1.1→M2→M3"
tags: [bridge, session, cs-385, cs-383, cs-380, cs-374, cs-386, cs-373, m3, m2, adr-038, invariant-ac, multi-signal-drift-gate, hyphen-normalize, skip-on-absent-fail-on-present, three-bundled-glaze-flips, vitest-to-jest, jest-to-jasmine, vitest-to-jasmine, ng-test-wrapper-case, ci-twenty-gates-green-twice, tag-move-discipline-applied, pull-rebase-tag-move-once, rthink-pass-1-pass-2, empirical-verification-before-encoding, multi-signal-scope-boundary, declared-absent-caught, declared-but-not-primary-deferred, planned-adoption-not-drift, m2-probe-monorepo-bug-empirically-verified, cs-386-high-priority-followon-filed, v2-45-2-deprecated-via-rebase, v2-46-0-tag-via-tag-move, v2-46-1-clean-tag, three-tags-three-tag-runs-all-twenty-gates-green]
last_updated: 2026-05-27
source: memory
confidence: high
linked_issue: CS-385
lobe_version: 1.0.0
status: active
---

# M3 SHIPPED — multi-signal Glaze↔Repo-state drift gate; triple-ship session (CS-380 ADR-038 + CS-383 M2 + CS-385 M3)

## Summary

Triple-ship session executing the CS-373 Phase-1 sequence M1.1→M2→M3 in one continuous pass: **(1)** ADR-038 ([CS-380](https://linear.app/colobbo/issue/CS-380)) — the Layer-0 design rule codifying structured-emit fragments require deterministic backing (the rule M1.1 / [CS-379](https://linear.app/colobbo/issue/CS-379) shipped the first instance of); **(2)** M2 ([CS-383](https://linear.app/colobbo/issue/CS-383)) — backend-lobe-codegen pre-emit installed-package probe closing C3 (Drizzle dual-package gap from CS-370 CP4) with new glaze map + invariant (ab) + advisory enum 'installed_package_unavailable' (MINOR schema bump 1.3.0→1.4.0); **(3)** M3 ([CS-385](https://linear.app/colobbo/issue/CS-385)) — verify-glaze-shape.sh invariant (ac) multi-signal Glaze↔Repo-state drift gate closing C10 + bundled 3 atomic glaze primary flips ([CS-374](https://linear.app/colobbo/issue/CS-374) api_node vitest→jest + 2 NEW drifts surfaced during /rthink Pass 2: web-angular jest→jasmine, Colobbo-Mobile vitest→jasmine).

All three ships tag-run-ratified with ALL 20 GATES GREEN. Three tags produced this session: v2.45.2 (would-be CS-380, was at pre-rebase SHA c3bcb3ed4; rebased + tag-moved per `feedback_tag_move_via_delete_republish` — but actually this was NOT a CS-380 tag, that was already done at v2.45.1; correcting: the actual tags this session = v2.46.0 for M2 + v2.46.1 for M3 + an ADR-038 PUSH at SHA 23391cd21 NOT tagged because ADR was doc-class). Tags v2.46.0 (M2) + v2.46.1 (M3) both ratified — v2.46.0 required a tag-move per `feedback_tag_move_via_delete_republish` (initial push at c3bcb3ed4 was orphaned when remote main had a graphify-refresh-self auto-commit ahead; delete-remote-tag + re-push at 835c39dc3 — that's CS-383 M2's actual final SHA); v2.46.1 (M3) avoided tag-move via pull --rebase before tag push, clean ratification at 7a9021e4d.

**Two GitHub Actions service notes**: (a) M2 tag-run auto-fired on push (no CS-379-style dispatch incident); (b) M3 tag-run also auto-fired on push. Both ratified in ~1.5min. CS-379's Actions-dispatch-incident playbook stays rehearsed but did not fire.

## What was done — phase-by-phase

### Phase 1 (early session) — ADR-038 ([CS-380](https://linear.app/colobbo/issue/CS-380)) ship

Authored ADR-038 codifying the Layer-0 design rule that emerged from CS-378 M1's HARD GATE 3-pass test finding (3-rounds-3-splits — same input, all schema-valid, different content; prose-tightening relocates variance rather than killing it). Operator resolved 4 of 5 open questions in [CS-380](https://linear.app/colobbo/issue/CS-380) body:
- Q1: new ADR-038 (cross-refs ADR-032; NOT extend ADR-032). Keeps recent ADR-033-037 cadence of one-decision-per-ADR.
- Q2: validator-keyed trigger as a grep-testable PROXY for "structured + downstream-relied-on"; underlying principle = derivation-stability.
- Q3 + Q4 + Q5: de-facto-answered by CS-379 precedent (scripts/<fragment-id>.mjs naming; script wins as deterministic-of-record; no migration path needed).
- New Decision 4: one-sentence gap fallback "if a fragment requires the LLM to derive structured output that downstream automation depends on, back it deterministically even absent a dedicated validate-*.mjs."

Two operator pre-write observations caught + folded into draft: (a) "## Alternatives considered" was NOT actually doubled in my draft (chat-rendering line-wrap artifact); (b) Decision 6 self-contradiction on "runtime" — reword to "maintainer-time discrepancy" since §c prose instructs invoke-script-emit-verbatim so LLM never reads §b at runtime; (c) `v0.1.0` collision with frontmatter `lobe_version: 1.0.0` — replace 3 occurrences with `today` / `Initial adoption`.

Doc-class PATCH commit. Gate 1 + Gate 12 + Gate 13 + Gate 5 portability all green. Commit `23391cd21` pushed direct to main; no tag (doc-only ADR per `feedback_tag_is_the_ci_gate_for_agent_system` — tag is reserved for substrate-changing ships). [CS-380](https://linear.app/colobbo/issue/CS-380) closed via Linear MCP `save_issue state=Done`.

### Phase 2 (mid session) — M2 ([CS-383](https://linear.app/colobbo/issue/CS-383)) ship

Backend codegen pre-emit installed-package probe closing C3 per CS-373 §3. Op #42 literal scope per the [CS-373](https://linear.app/colobbo/issue/CS-373) §7 sequence.

Three operator-confirmed scope-shaping decisions: (A) literal op #42 only — schema-table-availability deferred to a separate ticket; (B) codegen-local script (NOT Layer-0 fragment) — earn Layer-0 promotion at 2nd consumer per `feedback_pragmatic_agent_over_substrate_evolution`; (C) new declarative glaze key (NOT script-embedded map).

Pre-write JOIN-viability check (operator-directed): operator was right to insist on empirical verification before encoding. Discovery — `glaze.database.write_targets[*]` has single dialect (postgresql) only; MSSQL is legacy-read-only via `glaze.backend.persistence.legacy_db`, NOT a write_target. My pre-write 2D `(layer, dialect)` keying proposal was over-engineering; 1D `persistence_layer` keying is sufficient at v0.1.0. 2D refinement deferred to MINOR bump when MSSQL Drizzle becomes a write_target.

Advisory-enum openness check (operator-directed pre-tag): `BackendResult.advisories[].type` is a CLOSED enum with 9 values + `additionalProperties: false`. Adding `installed_package_unavailable` is a NEW advisory type → MINOR schema bump per the schema's own bump-discipline (1.3.0 → 1.4.0). System VERSION re-classed PATCH→MINOR (2.45.1 → 2.46.0) per the schema-bump-→-system-MINOR M1 precedent.

Substrate landed: `scripts/installed-package-probe.mjs` (~211 LOC, zero-install, mirrors validate-*.mjs + coherence-check.mjs); new glaze key `backend.codegen.required_packages_per_persistence_layer` in BOTH glazes (colobbo: drizzle→drizzle-orm, raw_sql_modern→pg, raw_sql_legacy→mssql; fintech: drizzle→typeorm, raw_sql_modern→pg, raw_sql_legacy→oracledb); new invariant (ab) in `scripts/verify-glaze-shape.sh` (first two-letter after (aa) retired per ADR-037); schema 1.3.0→1.4.0 with `installed_package_unavailable` enum value; agent S0 pre-emit step in `agents/global/backend-lobe-codegen.md`; new fixture `lobes/backend-lobe/test-fixtures/probe-smoke/` (synthetic BackendResult + synthetic-target-repo + projected expected-probe-findings); run.sh extended with Step 4 probe-smoke sub-block (Gate 7 picks it up). 62 fixture glaze-snapshot.json files mechanically re-snapped via `bash scripts/resnap-additive-fixtures.sh` (invariant (w) drift from fintech glaze additive change — same mechanical pattern as CS-368 v2.44.0 ship). CS-373 §10.11 row added for the deferred schema-table-availability ticket.

Tag v2.46.0 tag-move discipline applied: first push to main rejected (graphify-refresh-self auto-commit ahead from CS-380 push earlier); tag was pushed at pre-rebase SHA `c3bcb3ed4`; pull --rebase produced new SHA `835c39dc3`; tag deleted from remote via colon-prefix (`git push origin :refs/tags/v2.46.0`); re-tagged at new HEAD; pushed. Tag-run #26509846049 ratified at `835c39dc3` with ALL 20 GATES GREEN in 1m25s. Orphan tag-run #26509682449 at `c3bcb3ed4` also passed (irrelevant — superseded by the post-rebase ratification). No `--force` used per `feedback_force_push_regex_spans_compound_command`.

[CS-383](https://linear.app/colobbo/issue/CS-383) closed via MCP. [CS-384](https://linear.app/colobbo/issue/CS-384) filed as deferred-scope tracker for schema-table-availability probe per dialect (Medium priority).

### Phase 3 (late session) — M3 ([CS-385](https://linear.app/colobbo/issue/CS-385)) ship + 2 surfaced glaze drifts bundled

verify-glaze-shape.sh invariant (ac) closing C10 per CS-373 §3 — the Glaze↔Repo-state drift class CS-370 CP4 surfaced when test-writer-lobe-codegen detected `glaze.test.test_frameworks.api_node.primary = "vitest"` but api_node actually uses jest. [CS-374](https://linear.app/colobbo/issue/CS-374) is the literal 1-line glaze fix for that drift; M3 is the GENERIC GATE catching this drift class going forward.

**/rthink Pass 1 + Pass 2 — empirically grounded before plan finalization** (operator's "verify, don't encode" directive):

Pass 1 HIGH findings (3 — all empirically verified, all folded into plan):
- **api_node path derivation broken in pre-rthink plan.** Stack key `api_node` (underscored) does NOT match dir name `api-node` (hyphenated). NO root package.json exists at `~/Desktop/Colobbo/repos/api_node/`. Pre-rthink try-both convention would have false-SKIPed the literal CS-374 drift. **Fix**: hyphen-normalize stack key (`api_node → api-node`).
- **Hardcoded `[ "$GLAZE_FILE" = "$FINTECH" ] && continue` violated 3-layer.** Fintech repos use placeholder paths `~/code/<name>` — all verified ABSENT on disk. Generic skip-when-absent handles them naturally; the named-glaze guard fails the swap-the-glaze test.
- **Verification Step 1 was wrong about CHECK/SKIP attribution.** Pre-rthink said web-angular + Colobbo-Mobile SKIP. Reality: both EXIST on Mac, root package.json exists, they CHECK via single-package fallback.

Pass 2 HIGH findings (3 — operator-directed harden + empirical bundle surfacing):
- **SKIP-vs-FAIL conflation would have defeated the gate's purpose.** Pre-Pass-2 logic: "if neither package.json candidate exists → SKIP". Conflated two cases: (a) repo dir absent on disk (CI / portability — legitimate SKIP) and (b) repo dir present but no package.json candidate resolves (real broken state — must FAIL). **Fix**: split into two checks. Repo-dir absent → SKIP; repo-dir present + no candidate → FAIL loud.
- **Pre-Pass-2 substring-on-scripts.test was too weak** — missed the "ng test" Angular wrapper case + multi-signal drift surface. Empirical reality on the Mac: **web-angular** scripts.test=`ng test` + devDeps=`jasmine-core` + `karma` (no jest); **Colobbo-Mobile** same shape (no vitest). Glaze declared `primary: jest` + `primary: vitest` respectively. **DOUBLE DRIFT** beyond CS-374. **Fix**: multi-signal A/B/C check (A scripts.test substring / B devDependencies key substring / C framework config file existence). PASS if ANY signal confirms. Signal B catches the "ng test" + jasmine-core case cleanly.
- **Bundled glaze fixes: THREE flips, not one.** Per operator Pass 2 directive: bundle all three atomically into M3 (gate stays red until all match reality). NOT filing CS-374-equivalents for the 2 new drifts — they ride along the M3 commit.

Pass 2 MED findings: commit-convention reconciliation (brackets `[CS-NNN]` are pointer-only per `feedback_no_closes_in_commits`; Linear close is the explicit MCP step); (ac) notation per ADR-026 confirmed correct; **M2 monorepo-probe-bug priority HIGH** (empirical verification: probe exits ENOENT on api_node workspace root because no root package.json exists; probe-smoke fixture passed only because synthetic target-repo is a leaf dir — same hyphen-normalize fix needed in agent S0 step).

Pass 2 LOW findings (3 — confirmed unchanged): resnap/invariant-(w) holds (fintech glaze NOT touched); Gate-19 registry stamp pattern matches M2; VERSION class stays PATCH (no schema bump, no new glaze field — hyphen-normalize convention eliminates `package_json_path` candidate).

3 atomic glaze flips landed via single jq → /tmp → mv:
- `.test.test_frameworks.api_node.primary`: vitest → jest ([CS-374](https://linear.app/colobbo/issue/CS-374) closure)
- `.test.test_frameworks.web-angular.primary`: jest → jasmine (Pass-2 surfacing; LOBE.md `glaze.test.test_frameworks` reference + existing glaze entries [jest=framework + karma=runner] confirm primary=framework convention)
- `.test.test_frameworks.Colobbo-Mobile.primary`: vitest → jasmine (same evidence)

Secondary[] arrays NOT touched. Note on `Colobbo-Mobile.secondary = ["playwright"]`: operator-confirmed **planned-adoption** (NOT drift) — the entry points the glaze where Colobbo-Mobile testing is going; reality catches up when playwright lands. Documented on [CS-385](https://linear.app/colobbo/issue/CS-385) ticket body so a future (ac) v0.2.0 secondary-gating reads any red as "playwright not yet adopted" not "glaze wrong".

Multi-signal scope boundary documented in invariant comment + ticket body: the A/B/C check confirms the declared framework is **present at all** in the target repo (catches `declared-absent` — the CS-374 class). It does NOT confirm the declared framework is the **actual primary** — `declared-but-not-primary` deferred to (ac) v0.2.0 (would require parsing scripts.test head command + canonical-config-file precedence).

CHECKPOINT 1 + CHECKPOINT 2 pattern from M2 honoured. CHECKPOINT 1 (post-pre-tag verify sweep, pre-tag) surfaced 3 colobbo CHECK + 3 fintech SKIP at Step-1 no-repo-mapping + 0 failed — exact Pass-2 projection. Operator nod received. CHECKPOINT 2 (post-tag-run): ALL 20 GATES GREEN at v2.46.1 / SHA `7a9021e4d` / run #26519710633 / 1m25s.

[CS-385](https://linear.app/colobbo/issue/CS-385) + [CS-374](https://linear.app/colobbo/issue/CS-374) both closed via MCP. [CS-386](https://linear.app/colobbo/issue/CS-386) filed as M2-followon (Priority HIGH; ship as next PATCH 2.46.2 per CS-385 ratification).

## Substrate findings worth durable-memory consideration

1. **GitHub Actions tag-run dispatch was healthy this session** — no CS-379 incident reappearance. Two consecutive tag pushes (v2.46.0 + v2.46.1) auto-fired Lobe Regression Suite immediately. The CS-379 workflow_dispatch retry playbook stays rehearsed in `pre-tag-verify-checklist.md`; did not fire.

2. **/rthink Pass-2 harden saves cycles** — operator's "verify empirically before encoding" directive caught 6 HIGH gaps (3 Pass-1 + 3 Pass-2) that would have either false-PASSED the gate (defeating its purpose) or false-FAILED on CHECKPOINT 1. The Pass-2 bundle surfacing (2 new drifts beyond CS-374) was particularly load-bearing — without empirical scripts.test reads of web-angular + Colobbo-Mobile, M3 would have shipped with the gate immediately RED on its own first run, blocking the v2.46.1 tag and forcing a same-day follow-on. Pass-2 turned that into an atomic ship.

3. **Hyphen-normalize convention extends naturally to M2's probe** — operator-flagged at M3 time. The empirical verification at /rthink time showed `node scripts/installed-package-probe.mjs --target-repo=$HOME/Desktop/Colobbo/repos/api_node` exits 2 with ENOENT because there's no root package.json. [CS-386](https://linear.app/colobbo/issue/CS-386) ships the parallel fix in the agent S0 step (NOT in the probe script — the script is correctly path-agnostic; the bug is in the AGENT's resolution).

4. **Multi-signal scope boundary documentation matters at v0.1.0** — operator-directed inclusion in invariant comment. The A/B/C check catches `declared-absent` (CS-374 class) but NOT `declared-but-not-primary` (e.g. glaze=jest with karma actually running). Documenting the boundary at v0.1.0 means future drift surfacing reads cleanly as "v0.2.0 promotion target" rather than "gate failure." Same pattern as ADR-038's "no CI gate today" / "Initial adoption" framing — clarity-of-scope discipline.

5. **Planned-adoption vs drift distinction** — operator's call on Colobbo-Mobile.secondary=playwright. Glaze can legitimately point ahead of reality when the team plans an adoption; the gate should know the difference. v0.2.0 secondary-gating ships needs to read this as "planned, not yet adopted" not "drift". Recording on the M3 ticket body now means the v0.2.0 ship inherits the context.

6. **Triple-ship cadence achievable in one session** — ADR-038 + M2 + M3 all landed in a single continuous session. Anchor: CS-379 (M1.1) precedent was 1-2 sessions per ship; this session compressed three by reusing the CHECKPOINT 1/2 pattern + the pre-tag-verify-checklist + the Linear MCP close discipline. The compression was clean because: (a) ADR-038 was doc-class (no tag); (b) M2 was PATCH+MINOR-schema-bump (one tag); (c) M3 was PATCH (one tag). Three different version-class shapes, three clean ratifications.

## Recently observed operator patterns

- **"Verify empirically before encoding"** — Pass-2's load-bearing directive. Applied to: JOIN-viability (M2), advisory-enum openness (M2), path derivation (M3), fintech repo paths (M3), scripts.test reality (M3), M2 probe behaviour against real api_node (CS-386). Each verification caught a real flaw the chat-tier inference missed.
- **"Don't gold-plate; hold the version class"** — M2 was forced PATCH→MINOR by schema bump; the rest of M2 + M3 stayed PATCH. No scope creep.
- **"Bundle into atomic ship; don't defer"** — M3 Pass-2 bundled 3 glaze flips atomically rather than filing 2 new CS-374-equivalents. Cleaner ratification.
- **"Surface, then nod"** — CHECKPOINT 1 + CHECKPOINT 2 pattern from M2 applied to M3. Pause-and-show before tag is the gate-discipline.

## Commits this session (4 substantive + 2 ride-along)

| SHA | Subject | Ticket(s) | Class |
|---|---|---|---|
| `23391cd21` | docs(adr-038): layer-0 structured-emit deterministic backing | [CS-380](https://linear.app/colobbo/issue/CS-380) | Doc — no tag |
| `dbbb87bb7` | chore(graphify): refresh cibo from 23391cd | CS-240 (auto) | graphify-refresh-self auto-commit |
| `835c39dc3` | feat(backend-codegen): pre-emit installed-package probe + glaze map + invariant (ab) + advisory enum | [CS-383](https://linear.app/colobbo/issue/CS-383) | M2 — tag v2.46.0 (MINOR — schema bump) |
| `01acea3ef` | fix(hubspot-engagement): pack from/to into hs_email_headers | [CS-262](https://linear.app/colobbo/issue/CS-262) | UNRELATED — PR #29 merge pulled in via rebase (not authored this session) |
| `8aa72c4fd` | chore(graphify): refresh cibo from 01acea3 | CS-240 (auto) | graphify-refresh-self auto-commit |
| `7a9021e4d` | feat(verify-glaze-shape): invariant (ac) multi-signal glaze↔repo drift gate + 3 bundled glaze primary flips | [CS-385](https://linear.app/colobbo/issue/CS-385) [CS-374](https://linear.app/colobbo/issue/CS-374) | M3 — tag v2.46.1 (PATCH) |

## Tickets touched

| Ticket | Status at handoff | Action this session |
|---|---|---|
| [CS-380](https://linear.app/colobbo/issue/CS-380) | Done | ADR-038 shipped via doc-class commit `23391cd21`; closed via MCP |
| [CS-383](https://linear.app/colobbo/issue/CS-383) | Done | M2 shipped via tag v2.46.0 @ `835c39dc3` (20/20 gates green run #26509846049); closed via MCP |
| [CS-384](https://linear.app/colobbo/issue/CS-384) | Backlog | Filed alongside M2 — deferred schema-table-availability probe per dialect (Medium) |
| [CS-374](https://linear.app/colobbo/issue/CS-374) | Done | Literal vitest→jest glaze fix bundled into M3 commit; closed via MCP |
| [CS-385](https://linear.app/colobbo/issue/CS-385) | Done | M3 shipped via tag v2.46.1 @ `7a9021e4d` (20/20 gates green run #26519710633); closed via MCP |
| [CS-386](https://linear.app/colobbo/issue/CS-386) | Backlog (HIGH) | Filed alongside M3 — M2 monorepo-probe path-resolution bug; ship as next PATCH 2.46.2 |

## What's next

Per [CS-373](https://linear.app/colobbo/issue/CS-373) §7 Phase-1 sequence, post-M3 the parallel-friendly additive set continues:

- **M4** (op #44) — cibo-pipeline RUN-SUMMARY.md operator-digest write. Surfaces advisories prominently rather than buried in JSON.
- **M5** (op #38) — semantic-gate ground-claim extension. Operator-grep step added to orchestration.
- **M6** — ADR-012 capability profile migration for the 10 pre-ADR-012 Lobes (bundle-friendly with M1; earlies Phase 2 readiness).

Plus the M2 follow-on as a quick PATCH ride-along:

- **[CS-386](https://linear.app/colobbo/issue/CS-386)** — installed-package-probe monorepo path-resolution bug (HIGH; ~0.3 session; agent S0 hyphen-normalize fix; ship as PATCH 2.46.2 before tackling M4-M6).

Phase-1 critical-path migration items remain:

- **SP-dump landing** (COL-305 prereqs #1+#2) — operator/Jignesh-owned external dependency. Sole structural blocker on the migration program. Worker LOCKS SP-body + MSSQL schema reads to `Colobbo-Web/db/`.
- **CS-367** api_node migration convention fork (Jignesh, due 2026-05-28). Decides PR #246 fate + worker's MOVE/KEEP default.

CS-370 PRs (#250 + #522) still operator-review-gated.

## Cross-references

- This session's pre-handoff commits chain: `23391cd21` (ADR-038) → `dbbb87bb7` (graphify) → `835c39dc3` (M2 post-tag-move) → `01acea3ef` (PR #29 merge — unrelated, pulled via rebase) → `8aa72c4fd` (graphify) → `7a9021e4d` (M3 clean ratify, HEAD)
- Tag `v2.46.0` @ `835c39dc3` (M2)
- Tag `v2.46.1` @ `7a9021e4d` (M3)
- Parent: [[../../raku-meta/cs-373-derive-lobes-result|CS-373 derivation result]] §7 Phase-1 sequence + §10.11 summary table (M2 + M3 rows added)
- Sibling bridge: [[2026-05-26T1300-CS-379|CS-379 M1.1]] (the precedent ship for tag-move discipline + 20-gate ratify cadence)
- Sibling bridge: [[2026-05-26T0720-CS-378|CS-378 M1]] (the foundation: coherence-check Layer-0 fragment + the 3-rounds-3-splits finding that prompted ADR-038)
- ADR-038: `wiki/decisions/adr-038-cs380-layer-0-structured-emit-deterministic-backing.md` (shipped this session)
- Active-plan: [[../../strategy/active-plan|active-plan]] (updated post-this-handoff with M2 ✅ + M3 ✅)
