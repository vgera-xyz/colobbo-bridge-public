---
title: "CS-147 — pre-filter fix (whole-file + fresh-base CI-conformance replay) shipped + validated end-to-end via api_node PR #282; CS-320 migration plan-state corrected"
tags: [bridge, session, cs-147, cs-320, col-390, loop-harness, ci-conformance, sentry-fix, lint-changed, whole-file-lint, migration, icloud]
last_updated: 2026-06-19
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-147
status: active
amended:
  - timestamp: "2026-06-19T09:43:00Z"
    reason: "post-handoff: CS-320 closed via Pattern X root-cause note; skipDangerousModePermissionPrompt captured for deploy"
---

# CS-147 — pre-filter fix shipped + validated; CS-320 plan-state corrected

## Summary

Two threads. **(A) CS-320 plan-state correction** after an unhandoffed migration session moved the Colobbo tree off the iCloud-synced Desktop to `~/dev/Colobbo`: verified `$COLOBBO_ROOT=~/dev/Colobbo` resolves and we operate from `~/dev`; **corrected the handover premise** (`active-plan.md` had *zero* literal Desktop paths — the stale paths live in README/CIBO-CHEATSHEET/`.cibo-env` header/etc.); committed the real path-patches (`deploy.sh` + `scan-component-inventory.js` + `deploy-safety.md`, `52d36648b6`, `+x` restored on deploy.sh). The move **satisfies CS-147 unattended-flip prereq (1) "non-iCloud run path"** and resolves CS-320 (git status now sub-second). **(B) CS-147 harness CI gap**: investigated the post-#278 pre-PR CI-conformance gate, found + fixed the real defect (the local replay wasn't faithful), **shipped the fix** (`61a295ed12`), and **validated it end-to-end** by running one real Sentry issue (NODE-API-PROD-J) through the loop → api_node **PR #282** → **real CI green on both required checks, zero local-vs-CI divergence**.

## (A) CS-320 — migration plan-state correction

- **Verified, didn't assume:** `$COLOBBO_ROOT=/Users/vipingera/dev/Colobbo` (from `~/dev/Colobbo/.cibo-env`, sourced by `.zshrc:141`); CWD is under `~/dev`.
- **Premise correction:** the handover said "active-plan.md still describes the old Desktop paths" — grep proved it has **no literal `~/Desktop` filesystem paths** (only conceptual "iCloud" mentions of the CS-320 problem class). Real stale Desktop paths are in `README.md`, `CIBO-CHEATSHEET.md`, `.cibo-env` header comment, `APP-SECURITY-DEEP-DIVE.md`, `codebase-maps/*`, `CHANGELOG.md` (historical — left). Flagged, not bulk-rewritten.
- **Committed** (`52d36648b6`, explicit-path): `deploy.sh` (4 repo paths `~/Desktop`→`~/dev`; also restored the executable bit `100755` the migration copy had dropped to `100644`), `scripts/scan-component-inventory.js` (1 path), `.claude/rules/deploy-safety.md` (4 paths).
- **Working-tree cleanup:** 9 tracked files that the migration copy had dropped (`lobes/mobile-lobe/agent.json` + 8 `screenshot-service/*`) were missing-from-worktree only (present in HEAD, no commit removed them) → **restored** via `git restore`. 2 untracked iCloud `" 3"` collision files in `graphs/cibo/.../wiki/` → **deleted**. Tree clean; `main` synced to origin.
- **4 product repos** re-cloned clean from GitHub (the migration copy had corrupted their `.git`); all fsck-clean on correct integration branches (web-angular→development-stage, api_node→development, Colobbo-Mobile→development, Colobbo-Web→dev). Operator-confirmed no unpushed work lost. (Not re-investigated this session — stated as resolved.)

## (B) CS-147 — pre-filter fix + end-to-end validation

### Investigation (verify-first)
- Read `9265a79890` + `ci-conformance.sh` + `builder-prompt.md`. Confirmed the gate was **pre-raise predictive only**; the loop's terminal step is `gh pr create` → STOP, with **no post-raise confirmation that real GitHub CI went green** (grep of `loop-harness/` for any poll/watch → none).
- Reconstructed PR #278's real CI timeline: `lint-changed` was **RED** on `06631e1a2` (original raise — predates the gate) and again on the `103a136c4` merge commit, both on the **same 4 errors** (`_kh` ×3, `NewApiKey`) in `api-apps.service.ts`; **green** after the conformance fix `6ad16a640`; merged. Honest correction: in #278 lint did **not** recur *after* the gate passed — the gate held; the original red predates it.
- **Root cause of the whole-file-debt class (operator's hypothesis, confirmed):** `ci-lint-changed.yml` selects files by `git diff --diff-filter=ACMR <base.sha>...HEAD -- 'apps/api-node/**/*.ts'` (`:35`) then runs `npx eslint $REL` on the **whole selected files** (`:62`) — no hunk filtering. Touching a legacy file pulls its *entire* pre-existing debt into the gate. Proved the 4 errors pre-existed via `06631e1a2^` (present before the fix). `typecheck` (`ci-typecheck.yml`) is whole-repo `tsc --noEmit`. Authoritative required set = **`typecheck` + `lint-changed`** (both locally reproducible; no cloud-only required checks).

### Fix — `61a295ed12` (agent-system main)
Rewrote `builder-prompt.md` step 8 to make the local replay **faithful** on two load-bearing axes: **(a) fresh base** — `git fetch origin <branch>` then `origin/<branch>...HEAD` three-dot diff (mirrors CI's `pull_request.base.sha` merge-base; names the base-drift failure); **(b) whole-file lint** — lint the whole selected file, never hunks (anchored to the #278 `_kh`/`NewApiKey` evidence), same working-dir + path-stripping so `eslint.config`/`tsconfig` resolve identically. Doc-only; generic (api_node only as cited evidence). Committed `77ab55b839`, rebased clean to `61a295ed12` over graphify commits (diff-hash byte-identical), pushed.

### End-to-end validation — NODE-API-PROD-J → api_node PR #282
- Sentry queue had 3 unresolved backend issues; only **NODE-API-PROD-J** viable (C already fixed/merged; A is a transient DB-connection → escalate-not-fix per filter). J = `NoSuchKey` (S3) on `GET /api/generateWorkOrderChecklistPdf`, `handled: yes`, Seer-medium with a concrete remediation.
- **Fix** (`workOrderService.ts:processImageUrls`): guard `getObjectBuffer` with the existing `s3Service.objectExists()` (house style), and suppress `NoSuchKey`/`NotFound` in the catch while still capturing genuine decode failures. Reuses an internal helper; no new aws-sdk surface; placeholder behaviour unchanged.
- **Conformance gate (the fix under test):** discovered required checks live, replayed on a fresh `origin/development` base — whole-file `eslint src/services/workOrderService.ts` → exit 0; `tsc --noEmit` → exit 0.
- **Result:** opened **draft** → promoted to **ready** (operator choice). **Real CI: `lint-changed = SUCCESS`, `typecheck = SUCCESS`** — local prediction matched CI exactly, **zero divergence**. `mergeable=MERGEABLE state=BLOCKED` (review pending — loop never merges). Label `UNVERIFIED — human test required` (S3, not vgera_ro-verifiable), reviewer @jthakkar1981. Advisory ensemble re-triggered on ready (non-required).
- **Honest scope:** this validated the **green path**; it did not re-stress (i) a touched file *with* pre-existing debt or (ii) base-drift — so it doesn't by itself prove the poll is needed.

### Decision — post-raise CI poll DEFERRED
Recommended (and operator-accepted) to **defer the post-raise poll to the unattended-flip work** as a thin bounded base-drift + mergeable backstop. On this evidence the faithful pre-filter is sufficient for supervised runs. If built: poll `gh pr view --json statusCheckRollup,mergeable,mergeStateStatus` (not `--watch`) with dual ceilings + escalate-never-merge.

## Commits (agent-system main)
- `61a295ed12` — `fix(loop-harness): faithful whole-file + fresh-base replay in CI-conformance gate [CS-147]`
- `52d36648b6` — `chore(migration): patch hardcoded ~/Desktop paths to ~/dev [CS-320]`
- (product-repo) api_node `cs147-sentry-fix-j` → PR #282; agent-system not involved.

## What's next
- Review/merge **api_node PR #282** (UNVERIFIED → needs the human S3 test described in the PR body).
- CS-147 **unattended-flip prereqs**: (1) non-iCloud run path ✅ (this session) — remaining **(2) launchd**, **(3) Sentry REST token in run env**. Stay supervised until both exist.
- **deploy.sh STILL pending its operator run** — `6ec4836fc9` (Linear-MCP allows + deny-rule fixes) + the loop CI-conformance work are on agent-system `main` but not propagated to `~/.claude/` + product repos.
- Build the post-raise CI poll at the unattended-flip milestone (deferred).
- **COL-390** (prod DB lock/deadlock monitoring, RDS MSSQL + PG) filed Backlog/High — parked; Siddhesh's infra lane.

## Addendum 2026-06-19T0943 — post-handoff: CS-320 closed via Pattern X root-cause note; skipDangerousModePermissionPrompt captured for deploy

Work that landed after the canonical handoff commit:

- **CS-320 → Done.** Wrote the owed root-cause note as **Pattern X — iCloud file-provider intercepts git I/O** in [[../../raku-meta/state-drift-patterns|state-drift-patterns]]: hosting the working tree under the iCloud-synced Desktop puts every `git status` syscall behind the file-provider (>30s latency); the same provider produces `" 2"/" 3"` collision copies and spontaneously evicts tracked files; CS-317's sync-toggle containment was insufficient (tree stayed under the synced path); durable fix = relocate off any synced path. Added Cleanup-ledger row + frontmatter tags. Commit `1f00a70bf6`. CS-320 transitioned **Done** (`completedAt 2026-06-19T09:23`) with a pointer-comment to Pattern X (closes the AC that named that exact file).

- **deploy.sh pre-run review (read-only; NOT run — deferred to next session boundary).** Computed the source-vs-live diff: the **only** pending delta is `settings/global.json` → `~/.claude/settings.json` (adds 4 Linear MCP write allows + retargets the hook self-protection deny from `./hooks/**` to `.claude/hooks/**`); all agents/skills/rules/hooks/patterns/monitors are byte-identical to live. **Correction to this entry's earlier framing:** `loop-harness/` is **not** a deploy target, so the loop CI-conformance work is not propagated by deploy.sh (it runs in-repo). Found one un-captured live-only drift — `"skipDangerousModePermissionPrompt": true` — and **captured it into `settings/global.json`** (`4e9e35a4f9`) so deploy installs a clean superset rather than dropping it. Deferred the run to a session boundary (settings load at session start; mid-session rewrite of the running session's own permission/deny rules is unsafe + unreliable).

## See also
- [[2026-06-18T0707-CS-147|prior CS-147 bridge — pre-PR CI-conformance gate added + #278 driven green]]
- [[2026-06-17T1518-CS-147|prior CS-147 bridge — loop proven supervised]]
- [[../../decisions/adr-034-cs353-plan-state-persistence-boundary|ADR-034]] — plan-state ownership boundary
- [[../../architecture/working-pattern|working-pattern]]
