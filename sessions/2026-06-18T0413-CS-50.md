---
title: "CS-50 — api_node weekly-security #279 MERGED + scan verified end-to-end; dependency-security coverage audit across 3 repos (Dependabot OFF everywhere; Mobile no Snyk; prod unmonitored)"
tags: [bridge, session, cs-50, weekly-security, dependabot, snyk, dependency-security, sca, github-actions, coverage-audit, ci]
last_updated: 2026-06-18
source: memory
confidence: high
lobe_version: 1.0.0
linked_issue: CS-50
status: active
---

# CS-50 — #279 merged + verified, and a dependency-security coverage audit

## Summary

Continuation of the [[2026-06-17T1609-CS-388|1609 CS-388/CS-50 session]]. Watched **api_node PR #279** (the weekly-security concurrency fix) until **Jignesh merged it to `development`** (2026-06-18T03:14:48Z), then **verified the scan now runs end-to-end** via a manual `workflow_dispatch` — run `27734369283` completed **success**, the *first-ever* successful run of that workflow (it had been an invalid workflow producing zero runs since 2026-04-15). With the api_node thread closed, ran a **dependency-security coverage audit across all three product repos**. Headline finding: **Dependabot is OFF on all three**, **Colobbo-Mobile has no Snyk at all**, and **production branches (`main` + `release-*`) are monitored by nothing** (cron + Dependabot + Snyk all key off the integration/default branch). Operator enabled Dependabot dependency-graph + alerts during the session; the rest (Snyk-on-Mobile, security-update CI, production coverage) is deferred to a dedicated CI/security session. **No agent-system code commits this segment** — read-only audit + GitHub verification.

## What was done

### api_node #279 — merged + verified (CS-50 weekly-security)
- **Merged** by Jignesh into `development` (merge at `2026-06-18T03:14:48Z`). The PR carried two commits: the `setup-node` v4→v6 bump *and* the headline fix — moving `concurrency` out of the workflow top level (where `${{ matrix.app }}` is an invalid expression) into the `security-scan` job.
- **Verified end-to-end:** triggered `workflow_dispatch` on `development` → run `27734369283` → **`conclusion: success`, 0 failed steps**. Job `security-scan (api-node)` ran the full pipeline (checkout → setup-node v6 → `npm ci` → `npm audit` → outdated → AI summary → **Post to Slack**). This is the definitive proof the fix works: pre-fix, the invalid workflow produced **0** schedule + **0** dispatch runs; now it runs green. The automatic Sunday schedule (first ~2026-06-21 20:00 UTC) should now fire too.
- **Report delivery (documented):** posts to Slack via the `SLACK_WEBHOOK_URL` secret (channel is webhook-defined; CS-50 design = the security channel) as a 2-part message — a headline (`*Weekly Security Scan — api_node/api-node* | <status> | @mentions`) + an AI-generated "Cibo" summary of the `npm audit` output. **@-mentions: Jignesh Thakkar (`U064CQYJQ7J`) + Jaydeep Sorathiya (`U07H57XLAD6`).**

### Dependency-security coverage audit (all 3 repos)
Per-repo (default branch = where cron/Dependabot/Snyk-import all key off):

| | api_node | web-angular | Colobbo-Mobile |
|---|---|---|---|
| default branch | `development` | `development-stage` | `development` |
| production branch(es) | `main`, `release-2608` | `main` + 5× `release_*` | `main` + 8× `release-*` |
| **Dependabot alerts** | ❌ DISABLED→(operator enabled this session) | ❌ DISABLED→(enabled) | ❌ DISABLED→(enabled) |
| Dependabot security-updates (auto-PRs) | ❌ off | ❌ off | ❌ off |
| Snyk PR-check (`security/snyk`) | ✅ | ✅ | ❌ **none** |
| Snyk monitors default branch | ❓ Snyk-side (unconfirmable from GitHub) | ❓ | ❓ (no Snyk) |
| weekly cron scan | ✅ fixed/verified (runs `development`) | ✅ (`development-stage`; #555 pending) | ✅ green (#366 pending) |

Three real gaps, ranked:
1. **Dependabot OFF on all three** — the free, native "continuous monitoring big companies rely on" was simply not enabled. Authoritative: GitHub returned *"Dependabot alerts are disabled for this repository"* on all three + `automated-security-fixes` returned `enabled=false`. **Operator enabled dependency-graph + alerts this session.**
2. **Colobbo-Mobile has zero SCA** — no Snyk PR-check (confirmed across PRs #363–366; api_node + web-angular have it). Mobile is the weakest repo.
3. **Production (`main` + `release-*`) monitored by nothing** — every tool keys off the default/integration branch, so what's actually deployed has no continuous dep-vuln monitoring on any repo. This is the real substance behind the "add `main` to the weekly cron" question.

**Unconfirmable from GitHub:** whether Snyk *continuously monitors* the default branch (the `snyk monitor`/imported-project feature) — no default-branch commit statuses on any repo (consistent with either monitor-invisible OR Snyk-PR-only). Authoritative check = **app.snyk.io → org `vgera-xyz` → Projects** (which repos + branches are imported). Side note: Snyk is wired to a **personal `vgera-xyz` org**, not a Colobbo team org (governance follow-up).

## Commits

- **colobbo-agent-system:** none this segment except this handoff (bridge + active-plan + daily-log + canonical_pages). The audit + verification were read-only; Dependabot enablement was operator-manual in the GitHub UI.
- **api_node:** #279 merged by Jignesh (not a CC commit).

## What's next

- **Carried from 1609 (still open):** web-angular **#555** + Colobbo-Mobile **#366** await @pjtech18 review/merge. Jaydeep to rotate the committed `.npmrc` @pqina token.
- **api_node #279: DONE + verified.** Optional double-check: confirm the *automatic* scheduled run lands Sun 2026-06-21 (the manual dispatch already proved validity).
- **Dedicated CI/security session (operator-scheduled):** (a) **add Snyk to Colobbo-Mobile** (the SCA gap); (b) **confirm Snyk monitor scope** for api_node + web-angular in the dashboard; (c) enable **Dependabot security-updates** (auto-fix PRs — operator did alerts only); (d) decide **production coverage** (Snyk-monitor `main`, add `main` to the weekly cron matrix, or scan the deployed artifact); (e) governance: move Snyk off the personal `vgera-xyz` org.
- **Follow-up (own assessment, not filed):** drift-detection gate for the still-shared `ai-review.yml` + `slack-user-map.json` (from 1609).

## Verification notes

- Merge: `gh pr view 279` → `state: MERGED`, `mergedAt: 2026-06-18T03:14:48Z`.
- Scan: `gh run view 27734369283` → `conclusion: success`, 0 failed steps; job `security-scan (api-node)` ran the full pipeline incl. the Slack post.
- Audit: Dependabot-disabled is authoritative (GitHub message + `automated-security-fixes` `enabled=false`). Snyk PR-check confirmed via `gh pr checks` on #279/#555 and its absence across Colobbo-Mobile #363–366. Snyk monitor scope is genuinely unconfirmable from GitHub (stated as such; needs the Snyk dashboard).

## See also

- [[2026-06-17T1609-CS-388|prior bridge — graphify guard + weekly-security ×3 fixes (this segment's predecessor)]]
- [[../../strategy/active-plan|active-plan]] — CI-cron track updated this handoff (#279 merged/verified + audit follow-ups)
- [api_node PR #279](https://github.com/Colobbo/api_node/pull/279) (MERGED) · [web-angular #555](https://github.com/Colobbo/web-angular/pull/555) · [Colobbo-Mobile #366](https://github.com/Colobbo/Colobbo-Mobile/pull/366)
- [CS-50](https://linear.app/colobbo/issue/CS-50)
