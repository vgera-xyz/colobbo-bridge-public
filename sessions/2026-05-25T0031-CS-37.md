---
title: "CS-37 — Cibo architecture keystone doc shipped; CS-372 filed (trace subsystem reconciliation)"
tags: [bridge, session, cs-37, cs-372, cibo-architecture, keystone-doc, raku-meta, dna-principle, brain-as-a-service, baas, filesystem-brain, chat-venue, lobe-domain-agnostic, glaze-per-domain-partition, wiki-per-domain-partition, cross-domain-filesystem-channel, raku-extractable, sales-cs-greenfield-confirmed, screenshot-service-agents-anti-pattern, audit-then-build, trace-subsystem-tension-deferred, gate-12-pass, gate-13-pass, glaze-shape-invariants-pass, parallel-graphify-refresh-rebase, single-commit-session]
last_updated: 2026-05-25
source: memory
confidence: high
linked_issue: CS-37
lobe_version: 1.0.0
status: active
---

# CS-37 — Cibo architecture keystone doc + CS-372 filed

## Summary

Session shipped one keystone doc (`wiki/raku-meta/cibo-architecture.md`) that codifies the Colobbo Ware's brain architecture and the DNA Principle substrate per CS-37: ONE filesystem brain (Cibo) at `colobbo-agent-system/`, MANY chat venues (P&E, Sales, CS, future verticals) that point at it, Lobes domain-agnostic in `lobes/`, Glaze partitions config per-domain (sales / client_success / website namespaces appear when their first Lobe lands), wiki partitions hold per-domain memory + `wiki/raku-meta/` holds shared methodology byte-identically across Wares, cross-domain learning via the filesystem channel (not chat memory which is partitioned per-Claude-project), Raku extraction = lift Lobes unchanged + swap Glaze with Colobbo Ware as the reference Ware. The doc is the canonical surface every Claude project's instructions now point at so the model is read-once at session start and cannot drift per-venue.

Session also filed **CS-372** (Backlog/Low) for trace-subsystem reconciliation post-ADR-037 — read-only audit at session start surfaced 7+ skill/agent references to `codebase-maps/traces/00-index.md` that may be stale (the active plan now anchors migration on Graphify graph refresh, not traces, but skills still gate on trace freshness). Filed for a dedicated future session per scope discipline; NOT executed this session per operator instruction. Trace freshness warning surfaced at `/cibo-start-session` is now scoped to CS-372 and explicitly ignored for the rest of this session.

Pre-write audit established greenfield baseline for Sales/CS/Website Lobes (none exist; no `glaze.sales.*` / `glaze.client_success.*` / `glaze.website.*` namespaces; no `wiki/sales/` / `wiki/client-success/` / `wiki/website/` partitions). Audit also surfaced the canonical anti-pattern the keystone doc codifies: `screenshot-service/agents/` houses 7 functional Node.js agents (cold-outreach, demo-prep, follow-up, lead-score, social-radar, bounce-ingest, outreach-cron) that ARE sales-functional code running in production but are NOT Lobes — no LOBE.md, no Glaze namespace, no compose.sh participation, no Raku portability. Surfaced as principle in the keystone doc; consolidation path explicitly out-of-scope for this session.

Single commit: `e4244cfd3` (after parallel-session rebase onto `46ad4c8c2` graphify cibo refresh) — 3 files changed, +200/-9 of substantive content + canonical_pages sort_by(.path) churn (4032-line array re-ordering, lossless).

## What was done — five workstreams

### 1. Pre-build audit (read-only) — CS [DO]

Operator-directed structured audit before any Sales/CS/Website Lobe consolidation work. Seven numbered questions executed in parallel via filesystem grep + Linear MCP. Findings table delivered inline; no commits, no changes. Anchor findings:

- **Lobes (`lobes/_registry.json`):** 11 registered, all engineering or cross-cutting. Zero sales/CS/marketing/website Lobes.
- **Glaze top-level namespaces:** 25 active (engineering Lobes + cross-cutting infra). `glaze.sales.*` / `glaze.client_success.*` / `glaze.website.*` confirmed absent.
- **Wiki partitions:** 10 existing (architecture, bridge, clients, decisions, migration, modules, patterns, raku-meta, strategy, team). `wiki/sales/` / `wiki/client-success/` / `wiki/website/` confirmed absent. `wiki/raku-meta/cibo-architecture.md` confirmed absent (this session's target).
- **In-repo services:** `screenshot-service/` intact (Dockerfile present; no in-repo `railway.*` config — Railway wiring via dashboard per CS-182 memory). `screenshot-edit/` is a scratch dir of input/output PNGs, not a service. Single real service in-repo.
- **`colobbo-website` (separate repo at `~/Desktop/Colobbo/colobbo-website`):** Next.js stack (next, MDX-driven content); Vercel-deployed via `.vercel/project.json` linking project `colobbo-website` (orgId `team_Sr9cVAEXjFH2510ZrLXPY1Wj`); no `vercel.json` at root (using Vercel defaults). Github remote: `github.com/vgera-xyz/colobbo-website.git`. Relocatable as a subfolder of agent-system but only at Vercel project re-link cost; recommended keeping separate-repo + Lobe-in-agent-system pattern.
- **Sales/CS remnants:** Zero partial Lobe scaffolding. Zero `glaze.sales.*` / `glaze.client_success.*` namespaces. ONE in-repo runtime cluster: `screenshot-service/agents/` (7 ad-hoc Node agents). Greenfield assumption HOLDS on Lobe substrate dimension; BREAKS on the sales-runtime dimension.
- **Relay.app footprint:** 10 files mention "relay" but 9/10 are false positives (English noun "communication relay" in working-pattern, ADR-034, bridge entries). Real Relay.app refs: 2 (`skills/global/research-digest/SKILL.md` + deprecated `auto-spec/SKILL.md`). No sales/marketing agents actually built on Relay.

Audit also flagged the trace-freshness symptom (`codebase-maps/traces/00-index.md` dated 2026-04-01, ~54 days old, over the 30d freshness threshold the start-session skills enforce). This led to filing CS-372 (workstream 3 below).

### 2. Operator-claim verification — migration-worker location

Operator framing: "migration worker was moved to graphify you can verify." Filesystem audit contradicted the claim — `agents/global/migration-worker.md` still at 66,045 bytes with mtime 2026-05-24T10:08, deployed copy at `~/.claude/agents/migration-worker.md` (also 66,045 bytes; deploy.sh ran today at 01:02). Git log shows two commits (`2077ca145` initial ship + `e1fce13e6` wiring refinement) with NO rename/move. Graphify skill (`skills/global/graphify/SKILL.md`) contains zero "migration" string matches.

Surfaced contradiction per `feedback_handover_state_drift` discipline (apply Phase A audit to handover claims about repo state; if audit contradicts brief, surface and ASK). Hypothesised three possibilities (behavioural integration vs different working tree vs misremembered artifact). Operator did not respond to disambiguation question (moved to next workstream), so the claim was treated as superseded by the operator's pivot to the next task. No file moved.

Discipline reinforced: even direct operator claims about filesystem state get verify-by-grep before action. Symmetric application of `feedback_subagent_counts_unverified` (which applies to subagent claims) — applied here to operator claims.

### 3. CS-372 filed — trace subsystem reconciliation

Operator-directed filing (no execution). Filed to the Colobbo Strategic (CS) team via `mcp__linear__save_issue` with full STEP-1 / STEP-2 / STEP-3 structure including:

- **STEP 1:** read ADR-037 + ADR-001 before any edit to determine whether traces were retired wholesale (status (a) live / (b) frozen-by-design / (c) retired) or whether the freshness threshold only needs tuning (status (d) — fourth option preserved against premature decomposition).
- **STEP 2:** verdict-conditional consequences (no-op refresh if (a); skill freshness-check exemption + `dotnet-to-node-migration` skill retirement if (b); full Graphify-migration of 7 skill refs + new ADR superseding ADR-001 if (c)).
- **STEP 3:** fleet-drift consolidation across 3 start-session variants (`cibo-start-session`, `start-session-v2`, `start-session`) — Layer-0 shared fragment vs CI-gated parity sync, decided per outcome of STEP 2.
- Verify-first discipline + cross-reference verification list (`wiki/migration/progress.md` frontmatter `source: trace`; active-plan §Critical-path #2 trace-index refresh framing).
- Scope discipline: filed for a dedicated session, not opportunistic resolution (cross-cutting nature = 3 ADRs + 6+ skill files + 1 likely-retired skill + 1 hardcoded Tier-1 HARD GATE + 1 wiki strategy page; surface-level edits would produce inconsistent state).
- Acceptance criteria as a 7-item checkbox list.
- Priority: Low (cosmetic-at-surface; correctness-risk underneath).

Auto-assigned **CS-372**. Trace freshness warning explicitly ignored for the rest of this session per operator instruction.

### 4. cibo-architecture.md keystone doc — authored, registered, committed, pushed

Filesystem verification first: confirmed `wiki/raku-meta/cibo-architecture.md` absent (was), `wiki/raku-meta/` directory present (with 21 existing pages), frontmatter-v1 schema understood (required: title/tags/last_updated/source/confidence/lobe_version; optional: linked_issue/status), canonical_pages registration pattern verified for raku-meta (entries DO register; paths are `wiki/`-relative without prefix), CS-37 ticket exists and is Done (the Raku Brain-as-a-Service vision; status `Done` as of 2026-03-22 by operator).

Doc structure (199 lines):

1. **What Cibo Is** — one filesystem brain at `colobbo-agent-system/`; table of surfaces (Lobes / Layer-0 fragments / Glaze / wiki / hooks / skills / schemas) with paths and roles; cross-link to ADR-013 (memory pattern) + ADR-032 (substrate layers) as orthogonal axes that should be read together.
2. **What Cibo Is Not** — three negations carrying weight: NOT per-Claude-project (chat venues, not brains), NOT per-domain (no engineering/sales/CS brain split), NOT per-Claude-Code-instance (git remote = canonical state).
3. **Claude projects are chat venues, not brains** — table mapping P&E / Sales / CS / future verticals to "is" / "is NOT"; 4-step convention for what every project's instructions look like (Cibo location + relevant wiki partition + point-at-this-page + point-at-working-pattern+ADR-034); chat memory is partitioned-per-venue by design (cross-link to §Cross-domain channel).
4. **Lobes are domain-agnostic and all live in `lobes/`** — current `lobes/` is engineering-heavy + cross-cutting `memory-lobe`; sales/CS Lobes are siblings when built (same `_registry.json` + `compose.sh` + 3-layer discipline). Anti-pattern codified: agents outside `lobes/` are not Lobes. Examples in current Ware: `screenshot-service/agents/` (7 ad-hoc Node agents — anti-pattern) + `agents/global/migration-worker.md` (deliberate throwaway exception per ADR-037, dies when COL-205 completes).
5. **Glaze partitions config per-domain** — three-row table: engineering Lobes live / cross-cutting infra live / future Sales+CS+Website (not yet present, add when first Lobe lands). One Glaze = one Ware's complete DNA per ADR-006. fintech.glaze.example.json is the existing standing portability proof.
6. **Wiki partitions hold per-domain memory** — full partition table (10 existing + 3 future); `wiki/raku-meta/` is the only structural partition (byte-identical across Wares).
7. **Cross-domain learning channel** — table comparing filesystem (durable, version-controlled, cross-project-visible, canonical) vs chat memory (persistent within project, partitioned across projects, local context only) vs operator working memory (volatile, lossy bridge). Discipline: anything that matters across domains gets written to the filesystem; bridge entries are written for the NEXT session in any Claude project to read.
8. **DNA Principle** — direct invocation of CS-37 language: same genome (Lobe substrate, Glaze schema shape, wiki-frontmatter-v1, 3-layer memory pattern, hook/skill/agent surfaces, compose pipeline, canonical-pages mechanism) + unique epigenetics per Ware (Glaze contents, wiki corpus, canonical_pages registrations, bridge history, Lobes built so far). BaaS architectural commitment.
9. **Raku extraction** — 6-step procedure: lift Lobes unchanged → author new Glaze → lift `wiki/raku-meta/` verbatim → author new Ware's wiki content → lift schemas/scripts/hooks/compose/CI unchanged → author new Ware's brain identity. Colobbo Ware = reference Ware. fintech compile-target proves portability mechanically.
10. **Anti-patterns** — codified table: separate sales agent system repo / per-domain chat memory / multiple Glaze files per Ware / agents outside `lobes/` (with deliberate single-file throwaway exception) / chat-authored plans / divergent project instructions.
11. **See also** — cross-links to ADR-013, agent-memory-pattern, ADR-032, lobe-substrate-layers, ADR-006, ADR-034, ADR-023, ADR-002, working-pattern, glaze-shape-invariants, raku-meta index, CS-37 Linear.

Verification before commit:

- Gate 12 (frontmatter schema): PASS — 199 pages conform.
- Glaze structural invariants: ALL STRUCTURAL INVARIANTS PASS (post-canonical_pages add).
- Gate 13 (wikilinks): one FAIL caught — stale `[[../../skills/global/cibo-start-session/SKILL]]` path (project-scoped skills live at `.claude/skills/`, not `skills/global/`). Fixed to `[[../../.claude/skills/cibo-start-session/SKILL.md|cibo-start-session]]`. Re-run: PASS — 1612 wikilinks across 199 pages all resolve.

Canonical_pages registration: atomic jq + temp-file + mv pattern per `~/.claude/rules/bash-jq-reflexes.md`; appended one entry + sort_by(.path) for stable ordering; 182 → 183 entries. Topic keywords: `cibo, brain-architecture, dna-principle, filesystem-brain, single-brain, chat-venue, lobe-domain-agnostic, glaze-partition, wiki-partition, cross-domain-channel, raku-extractable, baas, cs-37`.

Index update: added link as keystone (top of Pages list) with `**keystone**:` annotation. Updated `last_updated` 2026-05-18 → 2026-05-25. Added `cs-37, cibo-architecture` to frontmatter tags.

Commit: `e4244cfd3` `docs(raku-meta): cibo-architecture.md — keystone doc [CS-37]` — 3 files changed (`wiki/raku-meta/cibo-architecture.md` new + `wiki/raku-meta/index.md` modified + `glaze/colobbo.glaze.json` modified), +2209/-2009 (substantive +200/-9 + canonical_pages sort_by churn). Initial push rejected — parallel-session graphify cibo refresh `46ad4c8c2` had landed; rebased clean (one commit on top), force-push avoided, push fast-forwarded to `e4244cfd3` on origin/main.

Post-push verification: grep + git-show against both local working tree and `origin/main:wiki/raku-meta/index.md` confirmed cibo-architecture entry at line 18 (keystone position, top of Pages list); frontmatter tags + last_updated bumped on remote.

### 5. CS-37 in-session pointer-comment

Posted via `mcp__linear__save_comment` (comment `bc3c07ae-7c51-4993-a6b8-058810d3829e`) on CS-37 (Done). Body: pointer to the keystone doc (commit `e4244cfd3`, canonical_pages 183-entry total, all-three-gates pass), 1-paragraph summary of the model the doc codifies, closing note that every Claude project's instructions now point at this surface.

## Files changed

| File | Change |
|---|---|
| `wiki/raku-meta/cibo-architecture.md` | NEW — 199-line keystone doc |
| `wiki/raku-meta/index.md` | Link added at keystone position; `last_updated` 2026-05-18 → 2026-05-25; tags +2 (`cs-37`, `cibo-architecture`) |
| `glaze/colobbo.glaze.json` | `canonical_pages` 182 → 183 entries (+1 cibo-architecture entry); array re-sorted by path for stable diff |

Total: 3 files changed, 1 file created, +2209/-2009 line diff (substantive +200/-9; rest is canonical_pages sort churn).

## What's next

- **CS-372** (filed this session, Backlog/Low) — dedicated session needed; do NOT opportunistically resolve. STEP 1 = read ADR-037 + ADR-001 verbatim before any edit. Scheduled by operator at cadence.
- **Active plan** (`wiki/strategy/active-plan.md`) — Step 1a of THIS handoff updates it with: cibo-architecture.md shipped + CS-372 filed entry under §Recently resolved + add CS-372 to §Parallel tracks (Backlog watchpoint). Critical path unchanged.
- **Operator follow-up:** every Claude project's instructions should be updated to reference the new keystone doc. Out of scope for this session (Cibo is a chat-venue concern, not a filesystem concern; the doc is now ready to be referenced).
- **Trace freshness warning** stays surfaced at `/cibo-start-session` until CS-372 closes; ignored per operator scope discipline.
- **Audit findings worth preserving even though no work was prescribed** — the screenshot-service/agents anti-pattern is now codified as principle in cibo-architecture.md §Lobes (fix path: rebuild as Lobes in `lobes/`), and the colobbo-website Vercel-deploy coupling is documented (relocate-as-subfolder is technically possible but operationally costly; recommend separate-repo + Lobe-in-agent-system). Both are future-session concerns.

## Verification notes

- **Phase A audit (verify-by-grep) applied to operator claim** — "migration worker was moved to graphify" — claim falsified by filesystem evidence (file still at original path with original size + original mtime + no rename in git log + graphify skill has zero "migration" matches). Surfaced as contradiction per `feedback_handover_state_drift`; operator did not respond to disambiguation (moved to next workstream).
- **All three CI gates run pre-commit + pre-push** (Gate 12 frontmatter / Gate 13 wikilinks / verify-glaze-shape.sh invariants k+n+t+u+v+w+x+y+z). One Gate 13 fix caught pre-commit (stale-skill-path link). No tag-push this session (doc-only, not a substrate ship; no VERSION bump per amend-mode convention applied to single-file doc additions).
- **Parallel-session rebase clean** — `46ad4c8c2` (graphify cibo refresh) landed during the keystone-doc authoring window. Standard `git fetch origin main` + `git rebase origin/main` resolved; force-push avoided per `feedback_force_push_regex_spans_compound_command`.
- **Pre-write filesystem verification before EVERY non-trivial action** — file-absence confirmed before Write; ticket existence + status confirmed before pointer-comment; canonical_pages registration pattern verified for raku-meta partition before jq edit; remote-state confirmed via `git show origin/main:...` after push.
- **Discipline reinforced this session:** symmetric application of `feedback_subagent_counts_unverified` to operator claims (not just subagent claims) — when audit contradicts the brief, surface the contradiction and ASK; do not silently act on either side.

## See also

- [[../../raku-meta/cibo-architecture|cibo-architecture.md]] — the keystone doc shipped this session
- [[../../raku-meta/index|raku-meta index]] — updated with keystone link
- [[2026-05-24T1454-CS-371|Previous session — CS-371 fixture metadata convention ship]]
- [[../../decisions/adr-013-2026-agent-memory-pattern|ADR-013]] — memory pattern Cibo implements
- [[../../decisions/adr-032-lobe-substrate-layering|ADR-032]] — Lobe substrate model the doc references
- [[../../decisions/adr-006-top-level-glaze-namespace-policy|ADR-006]] — per-domain Glaze namespace policy
- [[../../decisions/adr-034-cs353-plan-state-persistence-boundary|ADR-034]] — Chat/CC boundary, plan-state ownership
- [[../../decisions/adr-037-cs368-migration-substrate-retirement|ADR-037]] — context for CS-372 trace subsystem question
- Linear: [CS-37 — Raku Brain-as-a-Service vision](https://linear.app/colobbo/issue/CS-37) (Done; keystone-doc pointer comment posted in-session) | [CS-372 — Resolve trace subsystem status post-ADR-037](https://linear.app/colobbo/issue/CS-372) (Backlog/Low; filed this session for dedicated future resolution)
